# 香橙派 v24 后端允许列表部署记录（2026-09-04）

## 1. 范围与结论

本次部署对应配置提交
`31ef455105dd09b19188e5f4c42fb94a7f42ecbf`。服务器侧只修改
`/etc/ecobin/runtime.env` 中的香橙派业务运行时机器验收允许列表，并重载已有应用；没有修改
数据库、OneNet、COS 或设备，没有重新构建后端/Web 镜像，也没有向设备发送更新命令。

最终结果为：

- 保留已经允许的 v13、v19、v21 和 v23 运行时；
- 新增待写卡验收的 `hardware-runtime-20260904-24`；
- v15～v18 和 v20 继续不在允许列表中。

允许列表接纳 v24 只表示后端能在设备实际报告该运行时后认可其软件版本。它不会把 v24 写入
TF 卡，不会执行镜像内的测试业务包，也不会开启远程业务更新。

## 2. 目标确认与变更前验证

部署前按照生产部署文档确认目标主机为 `VM-0-16-ubuntu`。一个本机保存的其他 SSH 别名曾被
用于只读预检；该主机不是部署目标且没有运行配置文件，预检立即失败，没有执行应用步骤，也
没有留下任何修改。随后所有预检、应用和独立核验均只在正确目标主机执行。

正确目标上的只读预检确认：

- 运行配置是唯一的 `root:root / 0600` 普通文件；
- 当前允许列表精确包含 v13、v19、v21、v23，不含 v24；
- 稳定部署锁没有被其他进程持有；
- 服务器账户具备本次配置重载所需的完整免密提权能力；
- 没有输出运行配置原文或任何秘密值。

仓库侧配置测试保留 v23 及更早仍受支持版本，并验证 v24 正例；`ecobin-module-device` 模块完整
236 项测试通过。

## 3. 可恢复性与并发保护

修改前创建的恢复备份为：

`/etc/ecobin/runtime.env.pre-v24-20260904T075059Z.uIKsBbLw`

备份及最终配置均核对为 `root:root / 0600`。部署使用稳定锁
`/run/ecobin-deploy/runtime-env.lock` 串行化同类配置修改，并在替换前后核对原配置和候选配置。
配置通过同目录临时文件原子替换；只有确认服务器当前文件仍是本次写入版本时才允许自动回退，
检测到并发修改会拒绝覆盖。备份有意保留，未经明确回退或保留期决策不删除。

## 4. 部署与独立核验事实

配置部署完成后，使用与部署脚本分离的只读校验器，于 `2026-09-04T07:52:14Z`（北京时间
2026-09-04 15:52:14）完成独立核验：

| 检查 | 结果 |
|---|---|
| 生产启动前检查，真实外部服务模式 | PASS |
| `ecobin-stage-runtime-secrets.service` | active |
| `ecobin-target-app.service` | active |
| 后端容器 | running / healthy |
| Web 容器 | running / healthy |
| 容器内允许列表：v13/v19/v21/v23/v24 存在，v15～v18/v20 不存在 | PASS |
| 后端容器内置健康检查 | PASS |
| 服务器回环 Web 请求 | PASS |
| 运行秘密隔离探针 | PASS；设备接入 `enrollment=true`、远程维护 `remote_support=true` |
| 修改前备份内容 | PASS；精确等于不含 v24 的部署前允许列表 |
| 修改前备份的属主和权限 | PASS；`root:root / 0600` |

部署和独立核验脚本都通过标准输入交给服务器 root shell，没有在服务器 `/tmp` 留下脚本副本。
稳定锁文件和恢复备份有意保留。本记录不包含运行配置原文、数据库密码、OneNet/COS/微信
凭据、服务器凭据、设备密钥或业务包签名私钥。

## 5. 剩余边界

v24 尚未写入 TF 卡。它仍需完成写后全范围回读、冷启动、厂家接入与封存、持久所有权切换、
永久通信代理链投递/清运、签名成功包安装、故意不健康包自动回滚，以及关键阶段断电恢复。
这些现场事实通过前，第五、六阶段和 v24 量产资格不能标记完成，第八阶段远程下发继续保持
关闭。
