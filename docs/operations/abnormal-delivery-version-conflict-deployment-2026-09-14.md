# 异常投递会话版本冲突热修复记录（2026-09-14）

状态：已提交并部署；2026-09-14 14:16:35（北京时间，服务器容器记录）开始激活，生产验证通过。部署和验证均未执行“结束异常投递”、禁用或报废操作。

## 故障与根因

2026-09-14 14:03，平台管理员提交独立的“结束异常投递”时返回 `COMMON.VERSION_CONFLICT`，响应中的当前会话版本为 `22178`。请求号为 `2ba86142-9063-498c-b455-16b004e2a627`，服务器日志确认请求进入 `/abnormal-terminations` 并返回 HTTP 409。

只读核对显示设备资产版本稳定为 4，而会话版本在约两分半内从 `22178` 增长到 `22188`，状态仍为 `IN_PROGRESS`，完成结果、订单和精确占用均未变化。对应命令保存了两条可信事实：设备报告接受的时间比后台接收时间早约 1 秒，设备报告物理开始的时间比后台接收时间早约 0.7 秒。业务会话按既定时钟规则保存后台接收时间，但历史补偿候选查询仍优先使用设备时间比较，因此每 30 秒误判一次并重复回放两条事实，每轮把会话版本增加 2。

## 修正方式

历史投递命令补偿查询现在与业务投影统一使用后台接收时间。已经按该时间保存的接受事实和物理开始事实不再进入补偿队列；真正缺少时间或状态变化的历史会话仍会被选择。结束异常投递继续核对客户端看到的会话版本，真实的新设备结果或并发处置仍会阻止旧请求。

回归用例按现网形态建立两条“设备时间早于后台接收时间、会话已保存后台接收时间”的事实。修正前调度器会错误调用补偿，修正后不再选择该命令。

## 发布身份与验证

- 主开发分支提交：`3aa6efe5`。
- 生产 V72 兼容提交：`32504237d0cb9cefe3d3a4ed51482bed3bc1ddfa`。
- 发布 ID：`20260914061433-32504237d0cb`。
- 后端镜像：`sha256:d6edb62819efb0a3f51dd0ce7d410f0974f714ccc33e68978143d7a8e58ec56b`。
- Web 镜像：`sha256:53bd9183023afb6a1b3949fe3ac5bc98e0f815c4030371cd163e8b58081cc79e`。
- 发布包 SHA-256：`cf2fedd3d7635d57f1b4de33403fe243ea889ee1764bea9791f31abaeb0cac95`。
- 正式构建记录 1049 项后端测试：979 项执行通过、70 项因环境条件跳过，零失败、零错误；9 个 Maven 模块、Web 类型检查和生产构建全部成功。
- 主开发分支设备模块完整回归 347 项通过；生产兼容分支补偿调度与异常结束定向回归 9 项通过。
- 激活前会话版本已被旧调度器推进到 `22222`。新版本激活后连续三次只读观测均为 `22222`，跨过两个 30 秒调度周期后更新时间仍为 `2026-09-14 06:16:08.451`（数据库时间），原始故障不再复现。
- systemd、后端和 Web 容器、后端健康检查、运行密钥探针、生产预检、日志检查和公网入口均通过；新进程没有应用错误或内存溢出日志。
- 原失败请求没有留下幂等操作记录，成功异常结束操作仍为 0。会话保持 `IN_PROGRESS`，等待管理员刷新页面后自行决定是否结束。

## 数据与回退

本次只修改补偿候选查询，没有数据库迁移；现网仍为 V72、72 条成功迁移和 134 张业务表。安装器保留的上一发布为 `20260914054917-646ab11784ae`，但回退到该版本会恢复周期性版本增长问题；如新版本出现问题，应优先前向修复。应用切换不需要回退数据库。

服务器保留不可变发布目录 `/var/lib/ecobin/releases/20260914061433-32504237d0cb/`。本机发布包、校验文件、Git bundle 和验证摘要保存在仓库忽略目录 `.ecobin/production/115.159.67.35/releases/20260914061433-32504237d0cb/`。
