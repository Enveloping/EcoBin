# 香橙派 v46 写卡、后端发布与允许版本记录

日期：2026-09-16。设备运行时：`hardware-runtime-20260916-46`；工厂镜像：
`0.1.0-single-card.20260916.46`。本记录区分镜像构建、TF 卡写入、应用发布和后台允许版本，
不把这些软件阶段冒充真实 HMI、UART、RS485、称重、机构或断电硬件在环验收。

## 镜像与 TF 卡写入

镜像源码固定为提交 `d76b16ab1862f9d577a6bac1ba56a186a8f53387`。最终受控工厂镜像大小为
2,571,108,352 字节，SHA-256 为
`56d66f29b4b2b616a62d2e4b29f0f527745ec63b1f79009da2f5eb77b1b342e1`。两次无秘密候选构建
逐字节一致；候选与工厂镜像差异审计以及断网、只读 ARM64 冒烟均通过。

写卡前重新列出 Windows 磁盘，唯一 USB 可移动目标为磁盘 1：`Mass Storage Device`，序列号
`121220160204`，容量 31,268,536,320 字节，在线、健康、可写，不是系统盘或启动盘且没有
Windows 盘符。负责人看到本轮事实后明确回复“确认覆盖磁盘1”。受保护入口再次核对磁盘号、
二次确认号、序列号、容量、USB 类型、非系统/启动盘及源镜像摘要后才打开物理磁盘。

完整写入和强制刷新 2,571,108,352 字节后，从物理磁盘起点回读相同范围；回读 SHA-256 与
源镜像完全一致，结果为 `PASS`。脱敏证据保存在 Git 忽略目录中的
`flash-v46-hil-disk1.progress.log`、`flash-v46-hil-disk1.result.json` 和
`flash-v46-hil-disk1.exit-code.txt`。

## 后端修改、构建与发布

后端/API 修改提交为：

- `d9afcd85944f56e31c6d991f94e9c62cff538134`：固定新配置使用原生
  `UART_V2_SIMPLIFIED` 档位，让 OneNet 明确携带该档位，并为设备详情增加当前袋重量满溢、
  最近投递和最近清运时间；
- `eff9af5ea9df52c675f9fdfd40b53d0b88205053`：把 v46 新增的
  `fullness_transition.py` 纳入后端业务包固定白名单并同步摘要。

第一次正式构建在提交 `d9afcd85` 停止，唯一错误为
`BusinessReleasePackageVerifierTest` 拒绝未同步到后端白名单的 `fullness_transition.py`；没有生成
或上传可部署包。修复后该专项 5 项通过，随后从干净工作树完成正式全量构建：Maven 各模块
合计 1,342 项，0 失败、0 错误、198 项环境跳过；Web TypeScript/Vite 生产构建通过。

最终应用发布为 `20260916083810-eff9af5ea9df`，源提交为完整 `eff9af5e...`，本地发布归档
SHA-256 为 `5db0d5b9cd498246717bebd506f5a96b0ac14cc8c24dbf0a7a75548c45c90ed6`。服务器安装器校验归档
后生成：

- 后端镜像：`sha256:bf7935267d990a9f66dbe19870326a495cf621ce647031ecaae6bd1810fb0243`；
- Web 镜像：`sha256:2bb54c5753499aa964c72df921854204b969e58ec8d4cab16ab4e6edeebb98e8`。

激活前生产仍为 `20260916025440-863bcc8cca74`，数据库为 `84:84`、138 张领域表，活动投递、
清运和设备软件部署均为 0，后端与 Web 健康且远程业务程序下发关闭。新发布经生产预检后由
systemd reload 激活；后端/Web 均为 `running/healthy`、重启次数 0，镜像标签、提交、发布号、
内置健康检查、秘密隔离探针、回环入口和公网 HTTP 200 均通过。没有新增数据库迁移，也没有
修改或备份业务数据库。

## 后台允许版本

目标版本从最终镜像结果精确取得为 `hardware-runtime-20260916-46`。持有部署锁后确认当前允许
列表只到 v45、目标发布与镜像未变化且远程业务程序下发仍关闭。修改前创建了权限为
`root:root 0600` 的备份：
`/etc/ecobin/runtime.env.pre-v46-20260916T084353Z.h2D4aWck`。

配置整文件修改前 SHA-256 为
`203c65bb009ebcae265003b6fc7294bd15187c9c02a8fbb5cf254ce7ed29d489`；只在允许列表末尾追加一次
v46 后，整文件 SHA-256 为
`9bd7310f8cfa85a1f8b2c9b03084b4e1515b3d0e79fce4fce00c8102b42777e2`。重新暂存运行秘密并 reload
后，新 SSH 会话独立确认配置文件和运行中容器的列表完全一致、v46 无重复、备份摘要与修改前
一致、服务健康，`businessReleaseRemoteDispatchEnabled=false` 保持不变。

服务器 `/tmp` 中本轮上传归档已在终检通过后删除；已安装发布目录、当前和上一版运行镜像均
保留。本地正式发布归档保存在 Git 忽略的 `release-output/`，镜像构建依赖缓存也继续保留。

## 尚未替代的现场验收

- TF 卡尚未装回香橙派进行冷启动；
- MCU 固件没有在本轮重新烧录；
- 未执行真实热点、注册、UART rc.27、HMI、RS485、称重、门机构、摄像头、OneNet 或断电 HIL；
- 后台认可只允许 v46 继续厂家验收，不补造验收证据，也没有开启远程业务程序下发。
