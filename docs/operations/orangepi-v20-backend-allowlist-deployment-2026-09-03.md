# 香橙派 v20 后端允许列表部署记录（2026-09-03）

## 1. 范围与结论

本次部署对应配置提交
`0da1a25c5f51b8394de7b6c0782fa1ec4289626c`。服务器侧只修改
`/etc/ecobin/runtime.env` 中的香橙派业务运行时机器验收允许列表，并重载已有应用；没有修改
数据库、设备、OneNet 或 COS，没有重新构建后端/Web 运行时镜像，也没有向设备发送更新命令。

最终结果为：

- 保留当前真机使用的 `hardware-runtime-20260903-19`；
- 追加 v20 HIL 镜像携带的 `hardware-runtime-20260903-20`；
- 保留既有 v13 和更早仍受支持的运行时；
- 确认 v15、v16、v17 和 v18 仍不在允许列表中。

允许列表接纳 v20 只表示后端能在设备真实上报该安装身份后继续执行机器验收。它不表示 v20
已经写入 TF 卡、完成冷启动、通过 MCU 真实烧录/回滚，或已经开放远程更新。

## 2. 变更前验证

本地先增加 v20 正例，同时保留未知版本和 v15～v18 的拒绝反例。结果为：

| 测试范围 | 结果 |
|---|---|
| 机器验收允许列表聚焦测试 | `13 passed` |
| `ecobin-module-device` 模块完整测试 | `227 passed` |

服务器只读预检随后确认：运行配置为单一普通文件，元数据为 `root:root / 0600`；允许列表精确
包含 v13 和 v19、不含 v20；稳定部署锁没有被其他进程持有。预检没有打印运行配置原文。

## 3. 可恢复性与并发保护

修改前创建的恢复备份为：

`/etc/ecobin/runtime.env.pre-v20-20260903T133514Z.BcfDkjpB`

备份及最终配置均核对为 `root:root`、`0600`。部署继续使用稳定锁文件
`/run/ecobin-deploy/runtime-env.lock` 串行化同类配置修改。脚本在替换前后分别核对原配置和
候选配置摘要；只有确认服务器当前文件仍是本次写入版本时才允许自动回退，检测到并发修改会
拒绝覆盖。v19 及更早部署留下的 root-only 备份继续保留，未经明确回退或保留期决策不删除。

## 4. 生产核验事实

独立核验完成时间为 `2026-09-03T13:36:22Z`（北京时间 2026-09-03 21:36:22）。结果如下：

| 检查 | 结果 |
|---|---|
| 生产启动前检查，真实外部服务模式 | PASS |
| `ecobin-stage-runtime-secrets.service` | active |
| `ecobin-target-app.service` | active |
| 后端容器 | running / healthy |
| Web 容器 | running / healthy |
| 容器内允许列表：v13/v19/v20 存在，v15～v18 不存在 | PASS |
| 后端容器内置健康检查 | PASS |
| 服务器回环 Web 请求 | PASS |
| 运行秘密隔离探针 | PASS；设备接入 `enrollment=true`、远程维护 `remote_support=true` |
| 修改前备份的属主和权限 | PASS；`root:root / 0600` |

上传到服务器 `/tmp` 的一次性部署脚本在独立核验后已删除；稳定锁文件和恢复备份有意保留。
本记录不包含运行配置原文、数据库密码、OneNet/COS/微信凭据、服务器凭据或任何设备密钥。
