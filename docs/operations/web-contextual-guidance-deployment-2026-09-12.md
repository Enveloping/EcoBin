# Web 页面信息减量调整部署记录（2026-09-12）

## 当前结论

**已上线。** 北京时间 2026-09-12 14:48:29 开始切换，14:49:03 完成激活与服务器验证。
公网 `https://www.jinshoubao.com` 的 76 个前端文件均与本次发布包的 SHA-256 一致，登录页正常渲染，无浏览器运行错误。

本次落实[页面信息减量与操作提示计划](../planning/web-contextual-guidance-plan-2026-09-12.md)，覆盖 21 个业务页面。用户明确授权部署后，按[正式部署手册](../deployment/application-redeployment-runbook.md)执行。

## 发布身份与范围

| 项目 | 记录 |
| --- | --- |
| 服务器 | `VM-0-16-ubuntu`，`115.159.67.35` |
| 发布编号 | `20260912064344-686a7febbfce` |
| 完整 Git 提交 | `686a7febbfce4f209bcf1642202291da514f8162` |
| 上一版本 | `20260910171749-1a65fcdd58a5` |
| 源码状态 | 独立干净提交，`SourceDirty=False` |
| 后端镜像 ID | `sha256:ab507f9a03693e2308ac63769effdd7a9843b4717daf90d695c3aede2ee8a668` |
| Web 镜像 ID | `sha256:35817d6449e72272f5db49bb9d2192fa9229d6ccd9fdeeb55816233dad110523` |
| 发布包 SHA-256 | `46b32087547ad80a24b958a090aae52ed21f19ac65eb81b37b9b16c21105c487` |
| JAR SHA-256 | `1a89364b06c789cd9181336b1a25463fe207cd7c678cb1f6185d7c44bdeb3371` |
| 公网首页 SHA-256 | `3a5daa1ae39ba52eea8f7fd17b5cb04c5974a19412b5bb97d04dcca5790dad80` |

发布以线上版本的完整源码为基线，只带入本次前端、测试及计划文档，共 42 个文件。修改前的前端、HTTP 契约、运行镜像和部署控制文件与线上基线一致；服务器安装器、预检、Compose 执行脚本和 Compose 配置也逐一核对一致。

按现有流程重新构建并发布后端/Web 两张镜像，后端业务源码沿用上一版。没有执行数据库迁移，V69 不变；没有包含主工作区其他会话正在实现的硬件和 UART 契约候选。运行配置在切换前后逐字节校验一致，没有改变设备更新、远程维护或资金开关。

## 验证证据

| 检查 | 结果 |
| --- | --- |
| Java 21 完整 Maven 构建 | 230 个测试套件；975 项中 905 通过、70 按原配置跳过，0 失败、0 错误 |
| Web 干净安装及正式构建 | `npm ci`、TypeScript、Vite 通过，未使用 `SkipBuild`、`SkipTests` 或 `AllowDirty` |
| 与实施验收制品比较 | 所有脚本及样式逐字节一致；首页和图标仅回车换行形式不同 |
| 本轮前端修改验收 | 40 项专项检查、40 项浏览器回归、6 项生产构建关键流程通过；详见计划文档 |
| 上传及安装 | 归档摘要、逐文件摘要、发布身份和镜像身份均通过校验 |
| 激活前后预检 | `production-preflight=PASS mode=real` |
| 应用状态 | systemd 活跃，后端/Web 容器均为 `healthy`，后端健康检查和回环 HTTP 通过 |
| 运行边界 | 运行秘密隔离探针通过，后端日志目录为 `10001:10001 0750`，日志正常写入 |
| 公网静态文件 | 76/76 文件 HTTP 200 且摘要与发布包一致 |
| 公网匿名会话入口 | 平台、租户两个会话接口均返回预期的 401 |
| 公网浏览器 | 登录页及账号入口切换正常，无页面运行错误 |

公网验证只读，没有登录真实业务账号或提交业务操作。本轮没有操作真实资金和设备。构建中的依赖审计、分包循环和体积提示仍保留，本次未调整依赖版本或打包策略。

## 保存位置与回退

- 服务器正式发布目录：`/var/lib/ecobin/releases/20260912064344-686a7febbfce/`。
- 上一版镜像及 `/etc/ecobin/deployment.env.previous` 保留；后者仍指向 `20260910171749-1a65fcdd58a5`，可按部署手册回退。
- 独立源码目录：`C:/tmp/ecobin-web-20260912`；原工作区修改没有被提交、清理或重置。
- 本地发布包、完整源码 bundle 和证据另存于 `C:/Users/24217/.ecobin/production/115.159.67.35/releases/20260912064344-686a7febbfce/`。
- 完整源码 bundle 已通过 Git 验证，SHA-256 为 `16eff85c9b927718923d1d95988491b02e77948bac85dd6cefa3a2bd4a8665cb`。
- 构建、安装、激活、公网校验和登录页截图的工作副本位于 `release-output/web-contextual-guidance-20260912/`，该目录不进入版本库。
