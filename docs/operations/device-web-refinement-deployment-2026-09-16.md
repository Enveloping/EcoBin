# 设备管理界面调整生产部署记录（2026-09-16）

## 结果

设备管理界面和列表满溢汇总已部署到生产。当前发布为
`20260916090730-99d26dc4338b`，源码提交为
`99d26dc4338bc764a939ffaeef2ca0cf5ea69c03`。后端与 Web 容器均为 `healthy`，重启次数为 0；
生产应用 systemd 服务为 `active`，公网首页返回 HTTP 200。

本次发布不包含数据库迁移，发布步骤也没有主动修改业务数据或创建备份。生产库沿用部署前已
确认的 V84、138 张领域表；新后端以 V84 目标版本正常启动并通过健康检查。

## 发布内容

- 左侧导航宽度缩小为原来的约四分之三，表格补齐左右外边界。
- 设备列表的设备编号默认显示前段字符，悬停时显示完整值；多投口区域固定总高度并按投口数
  平分，避免一台设备把整行撑高。
- 列表默认减少低频字段，优化地址、设备状态和重量/满溢的展示层级与颜色。
- 设备详情按状态概览、投口、配置和维护任务重新组织，减少常驻说明、重复字段和内部技术
  信息。
- 设备列表接口新增 `overallFull`，后端依据每个投口的当前袋和最新设备事实汇总整机是否满溢；
  OpenAPI 契约和前端生成类型同步更新。

详细设计与逐项处理记录见
[设备 Web 界面修整计划](../planning/web-device-interface-refinement-2026-09-16.md)。

## 构建与制品

Maven 全量构建共执行 1,342 项测试，0 失败、0 错误、198 项按环境条件跳过。Web 完成
`npm ci`、TypeScript 编译和 Vite 生产构建。第一次正式脚本运行在后端全部测试通过后，因本机
仍运行的 Vite 开发进程占用 `esbuild.exe` 而停止；结束该本地进程后，重新完成前端依赖安装和
生产构建，并使用同一次已通过测试的后端制品生成正式发布包。

发布归档 SHA-256 为
`1a09b441ff593830a50449a310c28852c2969c400d21bb0ada39caef74fe53a6`。服务器校验归档后生成：

- 后端镜像：`sha256:357fab9d19e02fe847ecd702d23c784a3ecf6b693fb2d02238fb24dbf317345a`；
- Web 镜像：`sha256:9344b8c28db799f224daf5d9636626e24ce12de8fe6b4d7fe5a21372299995d2`。

## 激活与核验

安装器先保存发布目录、构建两张镜像并更新部署参数，没有自动重启应用。运行时密钥重新暂存
后，生产预检返回 `PASS mode=real`，随后通过 systemd reload 切换到新镜像。

激活后核验结果：

- systemd 应用服务为 `active`；
- 后端和 Web 容器均为 `healthy`，重启次数均为 0；
- 后端内置健康检查和运行时密钥隔离探针通过；
- Web 回环入口和 `https://www.jinshoubao.com/` 均返回 HTTP 200；
- 持久日志目录仍为 UID/GID `10001:10001`、权限 `0750`，日志文件非空；
- 查询激活后的十分钟日志时间范围，后端 `ERROR/FATAL` 与 Web 错误日志计数均为 0；
- `/etc/ecobin/deployment.env` 中发布号及两张镜像标签均指向本次发布。

服务器 `/tmp` 中上传归档和解压目录已删除，受控发布目录
`/var/lib/ecobin/releases/20260916090730-99d26dc4338b` 保留。本地正式归档继续保存在 Git 忽略的
`release-output/`。本次只发布云端后端和 Web，没有修改、烧录或部署香橙派与 MCU 软件，也没有
执行真实设备操作。
