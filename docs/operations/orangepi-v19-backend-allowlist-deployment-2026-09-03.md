# 香橙派 v19 后端允许列表部署记录（2026-09-03）

## 1. 范围与结论

本次部署对应配置提交
`e67fd9ab654d89d0cce8b3dd2200a7c8b42d6d27`。服务器侧只修改
`/etc/ecobin/runtime.env` 中的香橙派业务运行时机器验收允许列表，并重载既有应用；没有修改
数据库、设备、OneNet 或 COS，没有重新构建后端，也没有向设备下发业务更新。

最终结果为：

- 保留既有允许版本 `hardware-runtime-20260831-13`；
- 用新镜像携带的 `hardware-runtime-20260903-19` 替换此前的 v17；
- 确认先前已经排除的 v15、v16 以及从未加入的 v18 均不在最终列表；
- 其他既有允许版本保持不变。

允许列表接纳 v19 只表示后端能在设备真实上报该安装身份后继续执行机器验收。它不表示 v19
已经写入 TF 卡、完成冷启动，或通过真实香橙派、MCU 和外设的硬件在环验收。

## 2. 变更前验证

本地对允许列表边界完成了两层回归：

| 测试范围 | 结果 |
|---|---|
| 允许列表聚焦测试 | `13 passed` |
| `ecobin-module-device` 模块测试 | `227 passed` |

实际部署脚本还在无网络容器中验证了启动前检查、应用、回退、并发写入保护以及
`HUP`、`INT`、`TERM` 中断处理，均为 PASS。最终静态复审没有发现 P1、P2 或 P3 问题。

## 3. 可恢复性与并发保护

修改前创建的恢复备份为：

`/etc/ecobin/runtime.env.pre-v19-20260903T075930Z.UwF6V34h`

备份及最终配置均核对为 `root:root`、`0600`。部署使用稳定锁文件
`/run/ecobin-deploy/runtime-env.lock` 串行化同类配置修改；该锁文件有意保留。脚本仅在确认
当前文件仍是本次部署写入的版本时自动回退，若检测到其他进程已再次修改配置，则拒绝覆盖
并明确报告并发变化。

历史 v17 部署留下的以下两份备份继续保留：

- `/etc/ecobin/runtime.env.pre-v17-20260902T211243Z`；
- `/etc/ecobin/runtime.env.pre-prune-v15-v16.9qxTOs`。

除非执行明确的回退或另行确定保留期，否则不应删除这些备份。本记录不包含运行配置原文、
数据库密码、OneNet/COS/微信凭证或任何设备密钥。

## 4. 生产核验事实

独立核验完成时间为 `2026-09-03T08:00:44Z`（北京时间 2026-09-03 16:00:44）。结果如下：

| 检查 | 结果 |
|---|---|
| 生产启动前检查，真实外部服务模式 | PASS |
| `ecobin-stage-runtime-secrets.service` | `active` |
| `ecobin-target-app.service` | `active` |
| 后端容器 | `running / healthy` |
| Web 容器 | `running / healthy` |
| 容器内允许列表：v13/v19 存在，v15/v16/v17/v18 不存在 | PASS |
| 后端容器内置健康检查 | PASS |
| 服务器回环 Web 请求 | PASS |
| 运行秘密隔离探针 | PASS；设备接入 `enrollment=true`、远程维护 `remote_support=true` |
| 修改前备份的属主和权限 | PASS；`root:root / 0600` |

部署使用的临时服务器脚本和上传文件已删除；只有稳定锁文件和有意保留的恢复备份仍在服务器上。
