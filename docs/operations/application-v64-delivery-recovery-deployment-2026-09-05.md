# V64 与投递恢复功能部署记录（2026-09-05）

## 1. 范围与最终结论

本次操作把生产数据库从 V62 前向迁移到 V64，并部署“现场确认投递从未开始后，安全结束
原投递并释放设备占用”的后台与 Web 功能。最终运行版本为：

| 项目 | 最终事实 |
|---|---|
| Release ID | `20260904172211-7a99e969ef46` |
| Git commit | `7a99e969ef4686f2b80c0765f9c1b6d250fe424e` |
| 后端 image ID | `sha256:5378a257ef0316b3afdeebd1b8a747f94d4fef6ae65f30ff9ee714f36344a01b` |
| Web image ID | `sha256:e07cf14383b255f9c9de1de7199c62572c4845fc33fcf8c8060d6bfe8b975326` |
| 数据库 | 64 条成功迁移，129 张领域表 |
| 应用状态 | systemd active；后端与 Web 均 running / healthy |

数据库迁移只新增软件兼容事实与业务程序发布控制面，没有清空、回退或重建现有业务库。
恢复操作本身没有代替现场人员执行；本次只读确认目标任务仍满足显示恢复入口的全部服务端条件。

## 2. 迁移前保护

迁移前停止应用写入并确认后端、Web 容器均已退出。V62 基线为 62 条成功迁移、119 张领域表，
V63/V64 的目标表均不存在，`ecobin_schema_owner` 与 `ecobin_trigger_definer` 两个高权限账号
均处于锁定状态。

加密备份运行 ID 为 `20260904T165130Z`：

- 服务器 root-only 原件：
  `/var/backups/ecobin/h02/20260904T165130Z/ecobin-h02-permission-catalog-20260904T165130Z.sql.gz.cms`；
- 仓库外操作机副本：
  `C:\Users\24217\.ecobin\production\115.159.67.35\backups\h02-pre-v63-v64\20260904T165130Z\ecobin-pre-v63-v64-20260904T165130Z.sql.gz.cms`；
- 两端密文 SHA-256：
  `104be7b7ea7ff11a17f6c32f8adbfc8bb95334210e1a29cc8b9ff28a3f6a4755`；
- 操作机使用独立恢复私钥流式解密并执行 gzip 完整性检查，结果为 PASS，没有生成明文 SQL 文件。

备份脚本在完成备份后因 PowerShell 管道末尾的 Windows 回车返回 127；随后独立核对文件元数据、
摘要和实际解密均通过，因此没有重复创建备份。临时传输副本已在最终上线后删除，服务器
root-only 原件和操作机副本保留。

## 3. 数据库迁移与权限证据

第一次启动数据库供应脚本时，本地 SSH 隧道端口 `13306` 无法绑定；第二个候选端口也在绑定
测试阶段被拒绝。两次都发生在数据库账号解锁和 Flyway 执行之前，复核数据库仍为 V62、两个
高权限账号仍锁定。正式迁移改用可绑定的本地回环端口 `15432`。

供应脚本最终成功，脱敏证据目录为：

`C:\Users\24217\.ecobin\production\115.159.67.35\evidence\h02-v64\20260904T165745Z`

脚本摘要与独立只读 SQL 的结果一致：

| 检查 | 结果 |
|---|---|
| Flyway | 64 条成功记录，最高版本 V64 |
| 领域表 | 129 |
| V63 新表 | 4 / 4 |
| V64 新表 | 6 / 6 |
| 权限定义 | 76 |
| schema owner | locked |
| trigger definer | locked |
| 运行账号授权矩阵 | complete |
| 当前业务行 | 119,852 |

供应流程还完成了应用账号允许/拒绝矩阵和备份账号完整导出探针。迁移期间应用保持停止，避免
请求落入半升级状态。

## 4. 两次失败激活与恢复

最初候选发布 `20260904164357-97864d86cb44` 对应提交
`97864d86cb4407e53538396aa98d65a017c1b9e2`。第一次激活时，后端启动门禁发现生产库仍是
V62，而候选要求 V63，因此健康检查失败。部署立即恢复到稳定发布
`20260831132139-977362cd603a`，回环网页、后端健康检查和运行秘密探针均恢复 PASS。

完成 V62 到 V64 迁移后再次激活该候选，数据库 epoch 已通过，但 Spring 在真实外部服务模式
创建 `CosBusinessReleaseArtifactStorage` 时发现两个构造函数且没有明确注入标记，错误寻找
无参构造函数。后端因此不健康，Web 没有接流量。应用再次回到上一稳定发布；V63/V64 是向前
兼容的新增结构，旧应用在 V64 上通过 systemd、回环和后端健康检查。

修复提交 `7a99e969` 明确标注生产双参数构造函数由 Spring 注入，并增加真实模式容器实例化
回归测试。聚焦测试 5/5 通过，正式发布脚本随后完成全量 Maven 测试和 Web 生产构建。

## 5. 最终激活与独立验证

最终归档 SHA-256 为
`a881a18ad756cee78112a5758ee8b67fdd2a3d8cc6fa6192e4f4b7ac80a38e6a`。
服务器再次校验归档后顺序构建镜像；由于 SSH 会话存在偶发主动断连，长构建改由 root-only
systemd 临时任务执行，任务最终 `Result=success`、`ExecMainStatus=0`。生产预检以 real 模式
通过后，通过 `ecobin-target-app.service` reload 激活，没有绕过 systemd 手工运行第二套应用。

独立部署后检查结果：

| 检查 | 结果 |
|---|---|
| systemd 应用单元 | active |
| 后端容器 | running / healthy；image、Release ID、commit 均匹配 |
| Web 容器 | running / healthy；image、Release ID、commit 均匹配 |
| 后端启动 | 16.189 秒；新容器日志无 `Application run failed` |
| 服务器回环 Web | PASS |
| 后端容器内健康检查 | PASS |
| 运行秘密隔离探针 | PASS |
| 持久日志目录 | `10001:10001 / 0750`；`ecobin.log` 非空且持续写入 |
| 公网入口 | `https://www.jinshoubao.com/` 返回 HTTP 200 |
| Web 恢复界面制品 | 包含“结束未实际开始的投递”，PASS |

服务器上传归档、摘要、临时安装脚本和冗余加密备份传输副本均已按精确路径删除。正式发布
目录、不可变镜像、服务器 root-only 数据库备份和操作机仓库外证据均保留。

## 6. 目标投递的只读资格核对

对可靠任务 `c8eb6ade-8ed1-4723-b601-ae364fc592d5` 和投递会话
`ccde9147-76e0-4a24-98a5-a3a7dc06aaa6` 的最终只读核对为：

- 任务为 BLOCKED，原因是后台等待设备证据超时；
- 会话为“物理结果待人工恢复”，版本为 1，原授权已过期；
- 设备只有这一条投递占用；
- 没有设备接受事件、物理动作、物理结果或投递订单；
- 服务端综合计算 `recovery_available=1`。

因此平台管理员刷新设备详情后应看到“结束未实际开始的投递”入口。执行时仍必须由现场人员
确认投递门从未打开、设备从未进入本次投递，并记录故障排除说明。系统随后结束原会话并释放
占用，不重发开门、不补建订单；用户需要重新扫码发起新投递。

本记录不包含数据库密码、运行配置原文、OneNet/COS/微信凭据、服务器凭据或设备密钥。
