# 香橙派 v37 后端认可版本部署记录（2026-09-10）

## 结论

用户确认后，只向服务器 `/etc/ecobin/runtime.env` 的
`ECOBIN_DEVICE_ACCEPTANCE_SUPPORTED_EDGE_SOFTWARE_VERSIONS` 追加
`hardware-runtime-20260910-37`，保留原有全部版本；未加入 v35/v36，未移除其他版本。
后台读取新的配置后，设备重新上报的云端机器验收于 **21:44:42.643（北京时间）通过**，
失败项为空，当前验收代次为 1。
21:44:44.173 设备已确认收到该代封存授权；查询时为 `ACKNOWLEDGED`，不据此宣称最终封存完成。

此前 21:33:53.300、21:39:18.261 两份报告均为 v37，唯一失败项是
`UNSUPPORTED_EDGE_SOFTWARE`（设备软件版本不在后台认可列表）。修改前服务器文件和容器
实际列表一致，最新仅到 v34；不是网页缓存或设备没有更新。两份失败证据仍原样保留。
后端按新报告自行将资产验收状态从 `FAILED` 改为 `PASSED`，没有直接修改数据库验收结果。

## 变更及验证

- 沿用既有部署脚本的独占锁、精确旧值校验、私有备份、原子替换与并发修改保护；
  已对比移除认可列表后的配置，其他内容完全一致。
- 重新暂存运行秘密、执行真实模式生产预检，再通过 `ecobin-target-app.service` reload
  重建配置变化的容器；没有重新编译、上传或切换应用发布包。
- 当前应用发布仍是 `20260907231251-adb66043a1da`；后端/Web 镜像 ID 与变更前完全一致。
- 变更脚本和独立复核均通过：生产预检、两个服务 active、双容器 healthy、容器实际列表、
  后端健康检查、Web 回环入口及运行秘密隔离探针全部通过。
- 业务程序远程下发继续为 `false`；本次不部署 V69、OneNet 新模型、后台代码或设备固件，
  不创建更新任务，不人为发起投递/清运动作。
- 新报告使原始拒绝现象消失；数据库只读复核确认新证据 `PASSED`、失败列表 `[]`，
  原两份失败证据仍在。这是现场复验，不只是配置字符串检查。

执行脚本 SHA-256：`b0e9b40049434f2d15b9c0a1c8df4c5ba7fa80dca6299bd11122dea3c6b090c5`。
部署及独立复核输出、脚本与非秘密证据已归档到
[v37 镜像证据目录](../../hardware/image-artifacts/evidence/hil-weight-report-validation-20260910-37/README.md)。

## 恢复材料与本机同步

服务器修改前备份：`/etc/ecobin/runtime.env.pre-v37-20260910T134328Z.a76ewakT`，
已核验为 `root:root 0600` 普通文件，内容与原配置完全一致。只有排除并发配置变更后，
才可使用该备份并重新执行暂存、预检和 reload；不得用旧备份覆盖后来修改。

本机仓库外受控 `etc-ecobin/runtime.env` 的同一字段已同步为服务器实际列表；该本机字段
此前停在 v31，因此同时补齐服务器原已认可的 v32/v33/v34。同步未上传或覆盖其他本机配置，
其他字节不变；私有目录内保留 `runtime.env.pre-v37-sync-20260910T1345Z`，访问范围仍为
当前 Windows 用户与 SYSTEM。普通日志及 Git 不包含配置原文、密钥或秘密摘要。
本次服务器 `/tmp` 下两个部署临时脚本已清理，备份有意保留。

## 边界与后续

机器验收通过不等于实际称重已校准，也不代表未知结果投递隔离的云端配套已部署。
实际 500 克读成约 28 克的问题仍须独立排查，不能通过填入错误参考重量证明精度。
后续制镜像交付应同时检查“该运行时在实际后台是否被认可”，明确记录是否已部署认可配置，
避免只完成写卡就把设备交给用户重走到同一阻断点。
