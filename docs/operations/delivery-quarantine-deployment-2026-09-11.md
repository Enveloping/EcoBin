# 投递异常隔离收口云端部署记录（2026-09-11）

## 当前结论

**云端已部署；经用户确认，v37 新卡永久动作账本已启用并通过更新器重启持久性核验。异常收口真机验收仍未执行。** 01:40（北京时间）已在用户导入后独立确认
OneNet 新模型完整一致；线上 V69 迁移及 H02 完整权限验证通过。服务器记录应用于
01:47:09 停止、02:04:56 完成新后端与 Web 激活及健康核验。完整加密备份已通过本机隔离恢复。
02:18 起只读核验发现旧直连模式、账本关闭；用户随后明确“确认启用账本”。02:34:24 停旧
业务，02:36:04 写入永久激活，02:39:27 新业务就绪；后台为兼容并允许接入。具体现场事实见
下方“新卡现场核验”及“确认后的保留数据切换与账本启用”。
本轮没有向设备发送异常收口或任何物理动作指令，不能将云端部署当作真机异常收口验收。

准备阶段通过 OneNet 官方只读物模型查询接口核查时，目标产品仍为 17 个服务、21 个事件，
缺少两个新增功能。当时没有可连接的浏览器会话，因此由项目负责人在原产品导入
[`onenet-thing-model.candidate.json`](../../contracts/onenet/generated/onenet-thing-model.candidate.json)。
导入后已逐项比对全部功能定义，结果为 **18 个服务、22 个事件、0 个属性，差异为 0**，
包括 `quarantineDeliveryRecovery` 服务和 `deliveryRecoveryQuarantined` 事件。

部署顺序继续遵循
[`物理结果未知投递的远程异常隔离收口`](../architecture/delivery-unknown-effect-quarantine-2026-09-10.md)：
先确认 OneNet 模型，再停旧后端写入、备份、前向迁移 V69，最后安装并激活匹配的后端和 Web。
准备期间旧服务保持运行；实际迁移阶段停写。运行配置和设备状态未修改，未执行设备动作。

## 本轮已完成的线上步骤

- 先使用安装器的独立暂存配置封装两张运行镜像，保留正式发布指针不变；暂存生产预检为 PASS。
- 后端镜像 ID：`sha256:b42e1718ab4d7b0cd33b55fd3d29f0c3129aaa91053281d1bb920793601596c0`。
- Web 镜像 ID：`sha256:319166972e9daf56a8ebaa930d9715a2c3fe44e616d37f8b630a041d40cfbe37`。
- 服务停止前再次确认当前设备没有业务占用、没有正在执行的可靠任务。历史 9 月 9 日的旧投递
  占用原样保留；没有把停机、升级或新卡当作旧业务已经收口。
- 迁移前备份目录：服务器 `/var/backups/ecobin/quarantine-v69-20260911/`；操作机在仓库外
  ACL 受限的生产备份目录保留第二份密文，恢复私钥没有上传服务器。
- 完整数据库备份包含结构、触发器、数据及迁移历史，流式压缩和 CMS 加密，不落地明文 SQL。
  密文大小 22,483,887 字节，SHA-256：
  `79473f218b467661ed9fcc9bede36c191178614c0466954921feb6919c7479ae`。
  解密后的 SQL 流为 108,507,800 字节，SHA-256：
  `af68c4af8013c72325de287086220ee7c83f45e9051f80c5d48ed42d6f2c9ebe`。
- 隔离恢复使用本机 MySQL 8.4.10、无网络和关闭事件调度的临时容器：132 张表（含迁移历史）
  的行数全部匹配，68 条迁移记录及校验和全部匹配；验证后清理本次临时容器和数据卷。
- 恢复验证中修复两项本地工具问题：仅检测数据库存活会过早认为初始化完成，改为等待认证
  查询；Windows 下未加引号的短参数地址被传成 `127`，改为明确的完整主机参数。已复现并用
  原完整恢复流程验证修复；没有改动生产应用、数据库密码或降低恢复标准。
- H02 使用独立干净提交 `1a65fcdd58a5a8b8b1fecba0fa0e94a4c5763a52` 的迁移来源，保留既有
  业务行，只从 V68 前向执行 V69。运行 ID：`20260910T175522Z`。
- 01:57:52 只读核对：69 条迁移成功，迁移账号及触发器定义账号均锁定，问题记录表为空，
  原有 131 张业务表行数均未改变。H02 随后完整通过全部权限探针，最终为 132 张领域表、
  69 条迁移、76 条权限定义、完整列级权限矩阵。迁移来源清单摘要为
  `be66590633de0cd195828cc0a9d951cd120f9e8bc17b72f68236c149126573bd`。
- 正式安装再次复核已构建镜像的不可变身份，原子切换发布指针；同步部署 V69 版只读业务更新
  检查工具，暂存运行秘密、执行生产预检，再通过既有 systemd 单元启动应用。运行配置原件
  与备份逐字节相同，业务更新下发总开关继续为 `false`。
- 生产预检、后端和 Web 健康、回环 HTTP、持久日志目录权限和运行秘密隔离探针均通过。
  只读业务更新检查亦通过：无活动计划、无活动部署，既有 `0.3.0-rc.3` 发布记录未改变。
- 外网首页和包含新异常收口页面/API 调用的 `assets/index-B4JPhm8x.js` 与本地发布包摘要一致：
  首页 `95df8eb324c7e623e5f9ab61e892741627eb92dfbd9425582e9714e1f4a8f4e3`；功能脚本
  `106bbaf8038602298363f3ed1d4c0c4ae3301f07e65cc12da582dcc68ff02b1f`。
- 上线后问题记录、异常收口任务均为 0；投递订单 35、清运记录 18、钱包流水 53、提现订单 14、
  自动提现决策 24，均与迁移前相同。当前设备占用数为 0，历史旧投递占用保留。
- 目标 MySQL 仍仅连接内部 Docker 网络，`HostConfig.PortBindings={}`，没有新增宿主机端口，
  迁移用本机 13306 隧道已清理。宿主机另有原生 `mysql.service` 自 8 月 28 日起监听
  `127.0.0.1:3306`，它不是目标容器映射；本轮未修改或停止该既有服务，不能写成全主机无 3306。

## 可复用的发布制品

- 发布编号：`20260910171749-1a65fcdd58a5`（编号时间为 UTC）。
- 完整 Git 提交：`1a65fcdd58a5a8b8b1fecba0fa0e94a4c5763a52`。
- JAR SHA-256：`638e8ef9e2465e2ae29785f636d20dedbdd75d388b61b0249766585d9781ce58`。
- 独立源码目录：`C:/tmp/ecobin-quarantine-deploy-20260911`；构建时 `SourceDirty=False`。
- 源码来自已经写卡的 v37 冻结快照 `c097673c23527a63e5c442a77cb19322e6774d42`，其
  bundle SHA-256 为 `17af3c06b4ef5bdad762b01c34c2c945193f5ac23c1760656d35fe331ac79ae8`。
  与主工作区当前 51 个云端部署相关变更文件逐一比较，标准化换行后全部一致。
- 发布压缩包：
  `C:/tmp/ecobin-release-output/quarantine-20260911/ecobin-release-20260910171749-1a65fcdd58a5.tar.gz`。
- 大小：90,587,582 字节。
- SHA-256：`8f6bd7cce8eb3c985159a0f06206a08ea89a7e5a475d3d2a5d4771f2b028603a`。
- 压缩包上传后的 `sha256sum --check` 通过；正式安装记录位于服务器
  `/var/lib/ecobin/releases/20260910171749-1a65fcdd58a5/`。
- 发布压缩包及完整源码 bundle 另存到仓库外、ACL 受限的生产发布目录
  `C:/Users/24217/.ecobin/production/115.159.67.35/releases/20260910171749-1a65fcdd58a5/`。
  `source.bundle` 通过 Git 完整性验证并包含部署提交的完整历史，SHA-256 为
  `82c3547dc4df82b828af23e36b7b81537f1322e122292d23d3d4e867b792b178`，不只依赖临时源码目录续作。
- 复用现有 Maven/npm 缓存，未跳过完整构建、测试或发布包校验，未提交或清理原有脏工作区。
  服务器封装设置 `ECOBIN_PULL_RUNTIME_BASE_IMAGES=false`：Web 基础层命中缓存，缺失的后端
  基础层按需补齐；既有 Dockerfile 的 SSH 客户端安装层仍重新执行，不能称为完全离线构建。

## 构建中修复的真实阻断

第一次完整构建在 `BusinessReleasePackageVerifierTest` 失败，错误为
`BUSINESS_ALLOWLIST_INVALID`。已用原专项测试复现：v37 在设备业务包中新增共享重量报告
校验模块 `factory_seal/weight_validation.py`，后端固定文件白名单和摘要漏同步。

最小修复只补入该文件并同步精确摘要：
`07c5ddef9e209b46f714dbd204b5115108ddf2e960bf777a9fe8df34d4fcfefc`。
新增“即使签名有效，缺少共享校验模块仍拒绝”的负向测试；永久更新器文件、额外文件/目录
及损坏签名的拒绝边界不变。修复已同时保留在主工作区，并只在独立源码目录形成上述新提交。
本次没有重新审批、重验或下发既有业务更新发布。

## 已完成验证

| 检查 | 结果 |
|---|---|
| 后端完整 Maven 构建与测试，Java 21 | 230 个测试套件；975 项中 905 通过、70 跳过、0 失败、0 错误 |
| 发布包白名单专项回归 | 5 项通过；包含缺失共享重量校验模块的拒绝测试 |
| Web TypeScript 与 Vite 正式构建 | 通过；仅有打包体积/依赖弃用警告 |
| Web 架构检查 | 19 项通过 |
| Web 运维治理检查 | 7 项通过 |
| 契约生成一致性 | 无漂移 |
| 契约验证 | 24 项通过、0 提示；使用 Python 3.11、Java 21 和 C 黄金样例 |
| 契约测试 | 69 项通过 |
| H02 最小权限目录静态契约 | 通过 |
| 独立 MySQL 8.4.10 的 F07 完整验证 | 通过；V69、132 张领域表、76 条权限定义、应用就绪、运行账号越权拒绝和错误配置拒绝均符合预期 |
| 发布压缩包服务器摘要复核 | 通过 |

详细本地日志位于 `C:/tmp/ecobin-quarantine-deploy-evidence-20260911/`，保留初次失败、
复现、修复后专项测试和完整重构建日志。日志不作为线上已完成迁移的证据。

F07 首轮已经进入真实 JAR 启动，日志显示数据库 V69 门禁接受，随后报
`Could not resolve placeholder 'jwtSecret'`。干净源码目录没有个人本地秘密配置；这是测试启动
环境缺项，不是数据库结构失败。重跑仅为测试进程生成随机签名值，结束时移除环境变量，
不复制生产秘密、不修改源码或发布包、不关闭数据库和外联安全门禁。完整重跑日志为
`mysql-v69-validation-configured.log`，首轮异常摘要以本段为准。

01:30（北京时间）完整重跑通过，制品摘要与上述待发布 JAR 一致。应用没有创建业务实例数据，
没有修改数据库结构或迁移历史；五类错误数据库、缺失数据库、显式 Flyway 开关、Fake 模式
真实凭证混用及绕过开关均被拒绝。临时测试容器、应用进程和脚本临时目录已清理，发布包未变。

01:25（北京时间）线上只读复核：数据库 V68 / 68 条成功迁移 / 131 张领域表 / 76 条权限定义，
目标设备占用数为 0、管理架构为 `LEGACY_DIRECT`。两个应用容器健康，回环 Web 返回 HTTP 200，
仍运行旧提交 `adb66043a1da6c4e9ea792f4405266a49b97f319`。

## 设备端续作边界

线上现为发布 `20260910171749-1a65fcdd58a5`，数据库 V69、132 张领域表、76 条权限定义。
旧 V68 发布和迁移前完整备份继续保留，不能把不兼容的旧 V68 应用直接回退到 V69 数据库。

当前设备已从旧直连模式受控切换到永久通信代理及低权限业务链，并完成永久账本首次激活。
身份、完整业务数据和既有永久库保留；后台实际兼容性已核对。账本没有历史物理动作行，因为
本轮没有执行物理测试，不能把“已激活”当成实际动作写账或异常收口的真机验收。
普通远程业务更新总开关及本机远程更新入口保持
关闭；本次不更换 TF 卡，不修改 MCU/HMI，不触发隔离、投递、清运或任何门动作。

本功能仅面向满足严格前置条件、物理结果未知的原投递；不是所有卡住业务的通用取消按钮，
也不能处理已经完成的投递/清运或撤销其既有订单与余额。真实异常收口验收尚未执行。

平台页面入口为设备详情中的“设备问题与安全恢复”，操作名为“隔离结束物理结果未知的投递”。
入口已随 Web 发布；设备账本已启用，但不要拿正常业务制造异常试点。发起时仍必须具备精确原会话和
永久动作账本、重新启动及 F3/F1 安全证据，并由现场重新确认六项事实；本次部署不代替该确认。

本轮脱敏日志继续保留在 `C:/tmp/ecobin-quarantine-deploy-evidence-20260911/`：重点包括
`onenet-import-verification.json`、`backup-full-restore-final.log`、
`production-v69-summary-public.json`、`server-activation.log`、
`postdeployment-readiness.log` 和 `postdeployment-business-state.txt`。恢复演练脚本的问题按
diagnose 的复现—定位—回归步骤处理；临时诊断输出已从可执行脚本移除，过程日志保留。

## 新卡现场核验（2026-09-11，02:18 起，北京时间）

用户提供当前新卡 `ECM0-CDAJ9HMSEB8EEN4CQPVB32A9AC` 的 22011 端口维护命令。
首次连接在跳板认证处被拒绝：本机默认短期证书仍属于旧卡且已过期。后台只读查询证明新卡
维护会话为 `OPEN / OPEN / ACTIVE`；取用该会话已签发的证书及登记主机公钥，在仓库外建立
独立 SSH 配置后连接成功。未重新签证、延长租约或关闭主机校验，未覆盖旧 SSH 配置或私钥。

| 检查项 | 设备实际结果 |
|---|---|
| 三个安装版本 | `hardware-runtime-20260910-37`、`communication-20260910-37`、`updater-20260910-37` |
| 当前业务服务 | `ecobin-hardware.service`，root 身份，运行中；业务接口为 `READY` |
| 实际管理架构 | `LEGACY_DIRECT`；云连接仍由业务程序持有；`jobPermitEnforced=false` |
| 永久通信代理 | 服务运行，但 OneNet 所有权、业务事件入口及更新路由均为 `DISABLED` |
| 永久更新器 | 服务为 `READY`；`stage4CandidateEnabled=false`、`jobPermitRpcEnabled=false` |
| 永久门禁 | `jobGateMode=DISABLED`、`jobGateState=LOCKED`、`candidateActivationState=REQUIRED`，原因为 `STAGE4_CANDIDATE_DISABLED` |
| 激活和物理记录 | `job_gate_control_operation=0`、`job_permit=0`、`physical_action_ledger=0`、`physical_action_unknown_effect_resolution=0` |
| 切换标记 | `pending.json`、`active.json` 均不存在；新业务目录的 `edge.db` 尚不存在 |
| 数据库 | 原业务库与永久更新器库均以 SQLite `mode=ro`、`query_only=ON` 检查，`quick_check=ok` |
| 当前作业 | 原业务槽为 `NONE`；原业务维护锁、永久维护锁和人工安全锁均无记录；后端资产 24 占用数为 0 |
| 上报与照片 | 106 条事件均为 `CONFIRMED`；24 条照片均为 `UPLOADED`；`edge_fault_state` 为空 |
| 后台交叉核对 | 资产 24 为 `LEGACY_DIRECT`、`transitioned_at=NULL`；异常隔离记录总数仍为 0 |

账本关闭不是当前投递/清运被锁住：旧直连业务不经过该永久门禁，当前没有业务占用。
但是本功能要求永久账本；代码在门禁未启用时会拒绝为 `JOB_PERMIT_MISSING`，该拒绝结论来自
源码与实际运行开关的交叉核对，本轮没有发送命令去触发拒绝。

五个板上关键文件逐字节 SHA-256 均与 v37 冻结源码一致：

| 文件 | SHA-256 |
|---|---|
| `work_manager.py` | `080e0fa85b0588f3601a45060c032ddac26d736b91229790eec26a8947b5116b` |
| `job_safety.py` | `a99ad87260e70d805628b1a75cd37fdb8d49b7df1fdd6894bd9d8e262c97c469` |
| `command_processor.py` | `4168e9897be57bc1822a0170d175d63a54205f0909b9d746de39ea5f272050ce` |
| `updater_agent.py` | `e7072be0863ee1889f4e4024191d6a766107539c3da281cb432fc793a2e57c23` |
| `updater_store.py` | `4cec84214a28e2fb18dc53e17bfc3ee9974c59a01afb6e18799f73f9c19a1ef6` |

本轮只查询现有进程和本机状态接口、只读 SQLite 及核对文件，不重启服务，不直接占用 UART，
不发送 F1/F3 或任何物理动作，不修改设备配置、业务库和账本。现场称重精度仍未验证。
此轮只读核查结束时，待确认的下一步是有备份、有空闲复核的受控切换和激活；它不能补造
旧直连时期没有记录的动作，也不能收口旧卡遗留投递或清运异常。完整真机异常隔离验收仍未执行。

## 确认后的保留数据切换与账本启用

项目负责人随后明确授权“确认启用账本”。本段全部设备时间为 2026-09-11 北京时间；授权只用于
数据保留、运行链切换、账本激活和必要验证，不包含门动作、真实业务、异常收口、MCU/HMI
烧录、重刷 TF 卡或远程业务更新。未更改后端、OneNet 物模型或生产远程下发总开关。

### 备份、切换和激活

- 再次只读核对资产 24 后端占用为 0，无待执行可靠任务；设备为指定 v37，切换目标无冲突，
  原业务空闲、没有 MCU 维护，永久层无许可、物理动作、人工锁和旧激活历史。
- 设备 root 私有备份目录：`/var/backups/ecobin/ledger-enable-v37-20260911/`。完整复制状态与
  配置，4 个 SQLite 库分别通过在线备份接口生成一致快照并执行完整 `integrity_check`。
- 备份加密文件 `pre-cutover.tar.gz.cms` 为 94,794 字节，SHA-256
  `810f1d97ef6597b2719e895d8a3d17c40bd8d8ae5177408e8c76c9695d2e9e55`；本机副本摘要一致，
  使用既有本机恢复私钥流式解密并通过 tar 全成员读取。私钥未上传设备，明文归档未写到本机。
- 切换操作编号 `9342340d-8ded-432f-a53c-74e2a2a13fec`；证据摘要
  `ffcd21c42ed4cad5fb2c6010a838bd84133d3bdba6647fb0697555d468a3d2ae`。
  使用镜像自带 `business_runtime_cutover.py prepare` 完成双重空闲检查、停旧链、快照迁移和
  活动标记提交；返回 `ACTIVE / ACTIVATED / runtimeDataVerified=true`。
- 原业务 24 张表行数全部一致。袋基准、配置、满溢、固定帧历史结果和照片表的完整行内容摘要
  均保持一致；设备身份、能力文件和原启动编号不变。新业务库 577,536 字节；24 条照片均已
  上传且已墓碑化，没有需要迁移的活动照片引用或现存照片文件，不是本次删除了照片。
- 激活操作编号 `afd3e77b-8066-42d9-8f90-019ab92b1089`；激活证据 SHA-256
  `271018d975ff259bf05f0391d76dea7e27338a50d08c0430666a7b00eb649b47`。
  02:36:04.754 返回 `ACCEPTED / INITIAL_ACTIVATION`，状态序号从 3 到 4，门禁从 `LOCKED`
  到 `OPEN`。激活前检查的是完整迁移、空闲与无未决账本；新 F3/F1 在正常业务启动后取得，
  没有把当时尚不可查询的新业务接口伪造成已取得物理安全证据。

### 仅账本模式的最小启动修正

镜像的候选服务默认同时开放远程业务更新，因此本轮先以三个独立的
`/etc/systemd/system/<服务名>.service.d/90-ledger-only.conf` 持久限定范围：

- `ecobin-communication-proxy` 只增加 `--enable-updater-event-reporting`，不启用远程更新路由；
- `ecobin-updater-candidate` 使用 `--enable-stage4-candidate --enable-business-update-candidate
  --enable-software-state-reporting`，不启用 MCU 更新或远程业务更新。业务更新仓库仅因现有软件
  实际状态报告依赖而初始化，本地 root 更新协调器仍存在；没有排队、下载、调用助手或执行更新；
- `ecobin-business` 的启动前检查显式选择 `--posture ledger-only`。

初次启动在预检查失败，错误为“communication proxy did not report permanent OneNet ownership”。
真实本机接口已经证明通信代理联网并持有 OneNet；按 diagnose 复现及核对代码发现，原检查还
把 `BUSINESS_RUNTIME_ONLY` 远程路由、MCU 更新候选和远程更新开关写成了必需条件。
因此没有为通过检查开放更新，而是在 `hardware/system/business_runtime_preflight.py` 增加
独立、明确的 `ledger-only` 模式：旧默认模式不变；新模式仍要求真实永久代理、已激活账本、
低权限用户、UART/双摄、身份、封存和目录检查，同时要求远程路由关闭、MCU 候选关闭、无活动更新。

板上唯一程序热修正为 `/usr/lib/ecobin/business_runtime_preflight.py`：原 SHA-256
`e42fc95396e7a392b0ae44740cc399ca72a64d559dd86dc944606e9dab55bed5` 与 v37 冻结源码一致，
原件保留在设备备份目录；新 SHA-256
`baaa81692055b5ef7fbf26f0fb4471eb3ee39190e553fc3367528ba20bb5dedc` 与工作区一致。
这是一项当前卡热修正，不是重新发布了 v37 镜像；后续镜像构建须包含该已留在工作区的改动。
新增正反例以及真实 CLI 路由回归，连同旧预检查和切换测试为 Python 3.11 **32 通过、9 跳过**。
跳过项为主机平台相关检查；板上实际低权限启动预检查随后为 PASS。

旧业务停止于 02:34:24，新业务就绪于 02:39:27，本次业务暂停共 5 分 3 秒。维护代理一直运行。

### 最终核验和限制

- 新业务 `READY / LOCAL_PROXY`，云连接由 `COMMUNICATION_AGENT` 持有，
  `jobPermitEnforced=true`；更新器 `ACTIVE / ENFORCED / OPEN`。
- 02:40:26 重启永久更新器后，激活状态和原操作记录保持，业务 PID 143139、运行实例均未改变。
  仅验证了进程重启，没有进行整机断电恢复测试。
- 02:41:37 最终查询：通信代理 6 条上行均已被平台确认，待发送、待交付回执均为 0；原业务
  106 条事件仍全部 `CONFIRMED`，24 张照片仍全部 `UPLOADED`，业务槽为空，五类原业务内容
  摘要仍一致；永久激活记录 1 条，许可、物理动作和未知结果隔离记录均为 0。
- 后台资产 24：`PERMANENT_V1 / FULLY_COMPATIBLE / ACCEPTING`，可信镜像内置基线
  `IMAGE_BRIDGE_BASELINE`，软件状态序号 7、业务 `RUNNING`、串口 `READY`、固定帧修订号 2。
  未伪造独立业务发布编号，后台占用和异常隔离记录均为 0。
- 经现有授权的更新器身份查询 MCU：F1 通信健康、`validFlags=3`、烟感正常、红外未遮挡，
  本次重量为 0 克；这不是称重精度验收。F3 模式 1、状态正常、固件 `1.0.1-hil.4`，
  `safeFlags=0x0B`，观察证据摘要
  `fadb6f9c5596ca2e2cbe4f956deaef11a47d33c01bb26c439e9620b1c8fea0c9`。
  按 MCU 源码，缺少的是 `0x04`：“方向命令为零且两个驱动输出均关闭”的联合证明；
  不能据此断言电机正在运动，也不能把它当作异常收口需要的 `0x0F`。
  本轮未复位、未发 F2、未改变继电器、未降低收口门槛。真正收口仍须符合原投递条件、
  完整断电后的新启动和新 F3/F1 证据、现场六项确认；不能据账本激活直接开始收口。
- 本机持久归档：`C:/Users/24217/.ecobin/hardware/ledger-enable-v37-20260911/`，ACL 限定
  当前操作员与 SYSTEM。保留加密备份、前置事实、两个受控执行脚本、三个配置覆盖文件、
  热修正源码和现场证据包。证据包为 8,733 字节，SHA-256
  `03193b0d69caea508486b21d6f0b544986b49949d99c673a5a01e93a7daef6e6`，成员读取校验通过。
- 下载现场证据后，当前短期维护证书自然到期；最后的临时文件清理连接被拒绝，因此本次设备
  `/tmp` 下的辅助脚本、公开证书、加密备份副本及脱敏证据副本未清理。它们不含签名私钥，
  归档副本权限为 0600；下次用户重新开放维护时可按本轮精确文件清单清理，不延长旧租约或
  绕过证书校验。正式备份、运行配置及永久账本必须保留。
