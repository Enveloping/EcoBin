# 香橙派 v45 写卡与后台允许版本记录

日期：2026-09-16。对象：`0.1.0-single-card.20260916.45`与
`hardware-runtime-20260916-45`。本记录只证明指定TF卡写入和生产后台认可，不代表设备已经
通过真实HMI、RS485、称重、机构、断电或厂家验收。

## TF卡写入

写卡前重新读取Windows磁盘清单，唯一目标为磁盘1：USB `Mass Storage Device`，序列号
`121220160204`，容量31,268,536,320字节，在线且健康，不是系统盘或启动盘，没有Windows
盘符。项目负责人在看到本轮事实后明确回复“确认覆盖磁盘1”。

源镜像为`ecobin-orangepi-zero3-0.1.0-single-card.20260916.45-hil-factory-login.img`，大小
2,571,108,352字节，SHA-256为
`cfdb73990bf4d1433f6892d503996f897360f24dcd1106b13e7d698f3c33c9a1`。受保护写卡入口自身摘要
与已审查版本一致；管理员进程再次核对磁盘号、二次确认号、序列号、容量、USB总线、非系统/
启动盘、无挂载盘符和镜像摘要后才打开物理磁盘。

写入2,571,108,352字节并强制刷新后，从物理磁盘起点完整回读同样字节数。回读SHA-256为
`cfdb73990bf4d1433f6892d503996f897360f24dcd1106b13e7d698f3c33c9a1`，结果为`PASS`。脱敏证据
保存在Git忽略目录的`flash-v45-hil-disk1.progress.log`、`result.json`和`exit-code.txt`中；
没有自动重试，错误文件不存在。

## 生产后台认可

目标版本精确为`hardware-runtime-20260916-45`。变更前只读检查确认生产发布仍为
`20260915160954-736a9e1adeef`，后端和Web容器健康，远程业务程序下发为关闭；允许列表精确
截止到`hardware-runtime-20260915-43`，v45尚不存在。

持有部署锁后再次确认上述事实，创建`root:root 0600`备份
`/etc/ecobin/runtime.env.pre-v45-20260916T023347Z.EtAinHLk`。变更前配置整文件SHA-256为
`fe59137c8a5f41bb38552f9c448141895b618e17cae2c183e8716aedb7224aad`；只在允许列表末尾追加一个
v45值并原子替换，其他配置逐字节不变。变更后整文件SHA-256为
`203c65bb009ebcae265003b6fc7294bd15187c9c02a8fbb5cf254ce7ed29d489`。

重新暂存运行秘密并reload既有应用入口后，生产预检、运行秘密探针、systemd、后端/Web容器
健康、后端内置健康检查和回环Web入口均通过；应用镜像与发布号未变化。随后使用新的只读SSH
连接独立复核：当前列表严格等于原列表加v45、无重复，备份摘要与原配置一致，远程业务程序
下发仍为关闭。

后台认可只允许该运行时继续设备验收，不补造验收证据、不自动下发业务程序，也不表示设备
已经通过现场验收。

## 尚未完成

- MCU固件没有烧录；
- TF卡尚未装回香橙派进行冷启动；
- 热点、注册、UART rc.27、真实串口屏、RS485、称重、门机构和断电恢复仍待现场验证；
- 清运仍不能用投递结果替代真实硬件在环验证。
