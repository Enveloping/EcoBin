# 香橙派 v27 后端允许列表部署记录（2026-09-05）

## 1. 范围与结论

本次部署对应配置提交
`4f96e9ada6254b8fdf19bb72f9502b1d1670d3e2`。服务器侧只修改
`/etc/ecobin/runtime.env` 中的香橙派硬件运行时机器验收允许列表，并重载已有应用；没有修改
数据库、OneNet、COS 或设备，没有重新构建后端/Web 镜像，也没有向香橙派发送更新命令。

最终结果为：

- 保留已经允许的 v13、v19、v21、v23、v24 和当前设备使用的 v26 运行时；
- 新增已经通过两套离线镜像审计、等待写卡真机验收的
  `hardware-runtime-20260905-27`；
- v15～v18、v20 和未完成候选资格的 v25 继续不在允许列表中。

允许列表接纳 v27 只表示后端能在设备未来实际报告该运行时后认可其软件版本。它不会写入
TF 卡、改变当前香橙派、执行本地业务更新或开启远程业务更新。

## 2. 目标确认与变更前检查

本机 `myserver` SSH 别名仍指向既有部署记录中提到的非目标主机。本轮只在该别名执行了主机名
和非交互式提权能力读取，没有访问运行配置或执行修改。发现主机名不符后，所有允许列表预检、
应用和独立核验均改用精确生产目标；目标主机名再次确认为 `VM-0-16-ubuntu`。

正确目标上的只读预检确认：

- 运行配置是唯一的 `root:root / 0600` 普通文件；
- 当前允许列表精确包含到 v26，不含 v27；
- 稳定部署锁可以取得，没有同类部署并发；
- 服务器账户具备本次配置重载所需的完整非交互式提权能力；
- 没有输出运行配置原文或任何秘密值。

仓库侧示例配置、部署文档和测试均保留 v26 并追加 v27；
`TrustedDeviceAcceptanceEvidenceServiceTest` 聚焦测试 13 项全部通过，明确验证 v27 正例和未知
版本拒绝。

## 3. 可恢复性与并发保护

修改前创建的恢复备份为：

`/etc/ecobin/runtime.env.pre-v27-20260905T092851Z.QuNMnaxD`

备份及最终配置均核对为 `root:root / 0600`。部署使用稳定锁
`/run/ecobin-deploy/runtime-env.lock` 串行化同类配置修改，并在替换前后核对原配置和候选
配置。候选通过同目录临时文件原子替换；只有服务器当前文件仍是本次写入版本时才允许自动
回退，检测到并发修改会拒绝覆盖。备份有意保留，未经明确回退或保留期决策不删除。

## 4. 部署与独立核验事实

配置应用后，部署脚本完成生产预检、运行配置分发、应用重载和第一轮健康检查。随后使用与
部署脚本分离的只读校验器，于 `2026-09-05T09:30:05Z`（北京时间
2026-09-05 17:30:05）取得退出码 0：

| 检查 | 结果 |
|---|---|
| 生产启动前检查，真实外部服务模式 | PASS |
| `ecobin-stage-runtime-secrets.service` | active |
| `ecobin-target-app.service` | active |
| 后端容器 | running / healthy |
| Web 容器 | running / healthy |
| 容器内允许列表：v13/v19/v21/v23/v24/v26/v27 存在 | PASS |
| v15～v18/v20/v25 不在允许列表 | PASS |
| 后端容器内置健康检查 | PASS |
| 服务器回环 Web 请求 | PASS |
| 运行秘密隔离探针 | PASS；设备接入 `enrollment=true`、远程维护 `remote_support=true` |
| 修改前备份内容 | PASS；精确等于不含 v27 的部署前允许列表 |
| 修改前备份属主和权限 | PASS；`root:root / 0600` |

部署和独立核验脚本都通过标准输入交给服务器 root shell，没有在服务器 `/tmp` 留下脚本
副本。稳定锁文件和恢复备份有意保留。本记录不包含运行配置原文、数据库密码、OneNet/COS/
微信凭据、服务器凭据、设备密钥或业务包签名私钥。

## 5. 剩余边界

v27 最终受控 HIL 镜像仍在本机受控制品目录，尚未写入 TF 卡。下一次只有在项目负责人能够
插拔卡并明确确认目标磁盘后，才能执行裸盘写入和全写入范围摘要回读；随后还需完成空白
冷启动、热点接入、封存、永久服务、双摄、正常投递和正常清运复验。

v27 固化的是已在 v26 当前卡上通过真机更新、回滚和断电恢复的正式修复，但离线镜像审计和
后端允许列表都不能替代 v27 自身的写卡与冷启动证据。正式私有 COS、正式业务签名根、后端
实际安装事实闭环和第八阶段远程下发仍未完成。
