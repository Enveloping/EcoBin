# V66 单设备业务更新收尾部署记录（2026-09-06）

## 1. 结论

生产后端和 Web 已切换到应用发布 `20260906044541-fdd13f0ea782`，对应干净源码提交
`fdd13f0ea782b327192382da19e2cee90e45d522`。本次没有数据库迁移；现场数据库在激活前后均为
V66、131 张领域表。

本次上线修复两项单设备远程更新边界：

- 计划只有一台验证设备、没有后续批次时，设备成功后计划直接进入“更新计划已完成”，不会
  错误停在等待灰度放行；
- 安全取消命令在每次真实发送时刷新 5 分钟接收窗口，同时保持更新编号、部署编号、控制序号
  和稳定业务摘要不变，避免设备长时间离线后只收到已经过期的取消命令。

真实远程下发总开关在部署前、激活过程和部署后均为 `false`。没有创建计划、设备部署或
OneNet 下行命令。

## 2. 制品身份

| 项目 | 值 |
|---|---|
| Release ID | `20260906044541-fdd13f0ea782` |
| Git commit | `fdd13f0ea782b327192382da19e2cee90e45d522` |
| 源码状态 | 干净 |
| 发布归档 SHA-256 | `85fa1cd4b2cd8f5087e5a9aa66db193403fdf3430a7d74ebb7e1bb11f0a2f52e` |
| 后端镜像 ID | `sha256:5a8f08ed5df8b2b67590fc6c0f1c55620458331c55b8ffa721b4c8f2c868b35c` |
| Web 镜像 ID | `sha256:b664856f2a4161a2adc845200134cab2d70d9033d7398c6f6973f5c43b3aadaa` |
| 上一应用发布 | `20260905225449-e8e5bb4db1b5` |

上一应用发布继续由 `/etc/ecobin/deployment.env.previous` 指向，数据库结构没有倒退或删除。

## 3. 发布前验证

- 多模块依赖先执行 `mvnw install -DskipTests`，九模块构建成功；
- 正式发布构建完整执行 Maven 测试：946 项，0 失败，0 错误，70 项按环境跳过；
- Web 重新安装锁定依赖并完成生产构建；此前同一修改的架构测试和 API 类型检查也已通过；
- Linux/Python 3.11 完整硬件套件为 `2029 passed, 11 skipped, 5 subtests passed`；
- 远程更新、systemd、维护和镜像专项为 `258 passed`；
- 跨端契约为 24 项通过，17 条命令、21 类事件、38 份线级样例一致；
- 三份新增 Bash 工具通过语法检查，生产只读门禁和下发关闭核验已在服务器实跑。

## 4. 激活与生产核验

发布安装器先校验归档摘要，再顺序构建后端和 Web 镜像；安装阶段没有重启旧容器。随后重新
暂存只读运行配置、执行生产预检，并由 systemd 重载应用。结果为：

| 检查 | 结果 |
|---|---|
| 生产预检 | PASS，真实外部服务模式 |
| systemd 应用服务 | active |
| 后端 / Web | running / healthy，镜像 ID 与发布清单一致 |
| 容器内后端健康检查 | PASS |
| 运行秘密隔离探针 | PASS |
| 持久日志目录 | `10001:10001 0750`，新容器可写 |
| 公网首页 | HTTP 200 |
| 激活后 5 分钟 ERROR/FATAL | 后端 0，Web 0 |
| 数据库 | Flyway `66/66`，131 张领域表 |
| 健康测试发布 | `0.3.0-rc.3` 精确唯一且可下发 |
| v30 / v29 | v30 已认可，v29 已移除 |
| 更新计划 / 活动部署 | 0 / 0 |
| 真实远程下发 | `false` |

服务器同时安装了 root 管理的
`/usr/local/sbin/ecobin-business-remote-readiness` 和
`/usr/local/sbin/ecobin-business-release-dispatch`，均为 `root:root 0755`。前者只读检查
生产条件；后者只有在明确传入单设备测试确认参数时才允许开关下发，并会在失败时恢复配置。
仓库外本机规范副本中的 `deployment.env` 也已从服务器同步到同一 Release ID，替换前副本
保留为 `deployment.env.pre-fdd13f0e`；两侧文件 SHA-256 一致，未输出文件内容。

## 5. 尚待现场取得的事实

本次部署只完成服务器侧准备，不能替代以下现场证据：

1. OneNet 控制台确认 17 个同步服务、21 个事件已实际导入；
2. v30 写卡、全范围回读、冷启动、接入、封存、普通投递和清运；
3. 设备只读门禁通过后，受控验证离线重试、等待空闲、安全取消、下载授权续期、正常更新、
   永久服务重启、后端事实延迟和故意不健康发布自动回滚；
4. 所有场景结束后再次关闭并核对下发总开关。

操作顺序以
[v30 远程业务更新验收手册](orangepi-business-runtime-remote-update-readiness-2026-09-06.md)
为准。后续灰度批次不属于本轮实现或验收范围。
