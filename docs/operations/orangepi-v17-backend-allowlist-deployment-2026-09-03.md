# 香橙派 v17 后端允许列表部署记录（2026-09-03）

## 1. 范围与结论

本次只修改服务器 `/etc/ecobin/runtime.env` 中的香橙派业务运行时机器验收允许列表，并重载
既有应用容器；没有修改数据库、设备、OneNet 或 COS，也没有重建后端发布包。

最终结果为：

- 保留当前设备实际运行的 `hardware-runtime-20260831-13`；
- 保留下一次写卡候选 `hardware-runtime-20260903-17`；
- 移除从未写卡的 `hardware-runtime-20260902-15`；
- 移除缺少最终更新器私有目录用户组修复、已降为历史候选的
  `hardware-runtime-20260903-16`；
- 其他原有允许版本保持不变。

保留 v17 只表示后端可以在设备未来真实上报该身份时继续机器验收，不表示 v17 已经写卡、
冷启动或通过真实设备资格验收。

## 2. 可恢复性

服务器在每次修改前都保存了仅 root 可读的 `0600` 配置备份：

- 首次追加 v17 前：`/etc/ecobin/runtime.env.pre-v17-20260902T211243Z`；
- 移除 v15/v16 前：`/etc/ecobin/runtime.env.pre-prune-v15-v16.9qxTOs`。

最终配置仍为 `root:root`、`0600`。本记录不包含运行配置原文、数据库密码、OneNet/COS/微信
凭证或任何密钥摘要。

## 3. 服务器核验事实

最终核验时间为 `2026-09-02T21:27:22Z`（北京时间 2026-09-03 05:27:22）。核验结果：

| 检查 | 结果 |
|---|---|
| 生产启动前检查，真实外部服务模式 | PASS |
| `ecobin-target-app.service` | `active` |
| 后端容器 | `running / healthy` |
| Web 容器 | `running / healthy` |
| 容器内允许列表：v13/v17 存在，v15/v16 不存在 | PASS |
| 后端容器内置健康检查 | PASS |
| 服务器回环 Web 请求 | PASS |
| 运行秘密隔离探针 | PASS；设备接入和远程维护均为真实模式 |
| 修改前备份存在 | PASS |

部署使用的临时脚本已从服务器 `/tmp` 和开发机忽略目录删除。服务器上的两份恢复备份有意
保留；如无明确回退或保留期决定，不应删除。
