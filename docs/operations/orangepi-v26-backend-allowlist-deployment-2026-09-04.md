# 香橙派 v26 后端允许列表部署记录（2026-09-04）

## 1. 范围与结论

本次部署对应配置提交
`589ca52b379f75a5f14fa6ac037866fcf44d5adf`。服务器侧只修改
`/etc/ecobin/runtime.env` 中的香橙派业务运行时机器验收允许列表，并重载已有应用；没有
修改数据库、OneNet、COS 或设备，没有重新构建后端/Web 镜像，也没有向香橙派发送更新命令。

最终结果为：

- 保留已经允许的 v13、v19、v21、v23 和 v24 运行时；
- 新增已经通过离线镜像审计、等待写卡真机验收的 `hardware-runtime-20260904-26`；
- v15～v18、v20 和未完成候选资格的 v25 继续不在允许列表中。

允许列表接纳 v26 只表示后端能在设备实际报告该运行时后认可其软件版本。它不会写入 TF 卡、
改变当前香橙派、执行 HIL 测试业务包或开启远程业务更新。

## 2. 变更前检查

目标主机确认为 `VM-0-16-ubuntu`。只读预检确认：

- 运行配置是唯一的 `root:root / 0600` 普通文件；
- 当前允许列表精确包含 v13、v19、v21、v23、v24，不含 v26；
- 稳定部署锁没有被其他进程持有；
- 服务器账户具备本次配置重载所需的完整免密提权能力；
- 没有输出运行配置原文或任何秘密值。

仓库侧配置和测试保留仍在服役的旧版本、排除 v25，并验证 v26 正例；
`TrustedDeviceAcceptanceEvidenceServiceTest` 聚焦测试 13 项全部通过。

## 3. 可恢复性与并发保护

修改前创建的恢复备份为：

`/etc/ecobin/runtime.env.pre-v26-20260904T123134Z.UU1NmNh1`

备份及最终配置均核对为 `root:root / 0600`。部署使用稳定锁
`/run/ecobin-deploy/runtime-env.lock` 串行化同类配置修改，并在替换前后核对原配置和候选
配置。候选通过同目录临时文件原子替换；只有服务器当前文件仍是本次写入版本时才允许自动
回退，检测到并发修改会拒绝覆盖。备份有意保留，未经明确回退或保留期决策不删除。

## 4. 部署与独立核验事实

配置应用后，部署脚本完成生产预检、运行配置分发、应用重载和第一轮健康检查。随后使用与
部署脚本分离的只读校验器，于 `2026-09-04T12:32:52Z`（北京时间 2026-09-04 20:32:52）
取得退出码 0：

| 检查 | 结果 |
|---|---|
| `ecobin-stage-runtime-secrets.service` | active |
| `ecobin-target-app.service` | active |
| 后端容器 | running / healthy |
| Web 容器 | running / healthy |
| 容器内允许列表：v13/v19/v21/v23/v24/v26 存在 | PASS |
| v15～v18/v20/v25 不在允许列表 | PASS |
| 后端容器内置健康检查 | PASS |
| 服务器回环 Web 请求 | PASS |
| 运行秘密隔离探针 | PASS；设备接入 `enrollment=true`、远程维护 `remote_support=true` |
| 修改前备份内容 | PASS；精确等于不含 v26 的部署前允许列表 |
| 修改前备份属主和权限 | PASS；`root:root / 0600` |

第一次传送独立校验脚本时，PowerShell 在脚本末尾保留了 Windows 回车；全部检查已经输出
PASS 后，远端 shell 因尾部空行回车返回退出码 1。该过程只读且没有服务器修改。去除回车后
使用同一校验器重跑，得到上述干净退出码 0。

部署和独立核验脚本都通过标准输入交给服务器 root shell，没有在服务器 `/tmp` 留下脚本
副本。稳定锁文件和恢复备份有意保留。本记录不包含运行配置原文、数据库密码、OneNet/COS/
微信凭据、服务器凭据、设备密钥或业务包签名私钥。

## 5. 剩余边界

v26 最终 HIL 镜像仍在本机受控制品目录，尚未写入 TF 卡。下一步必须由项目负责人先断电并
把卡插入电脑，重新枚举并明确确认目标磁盘后，才能执行裸盘写入和全写入范围摘要回读。随后
还需完成空白厂家接入、封存、当前双摄角色、历史摄像头回退、投递、清运、永久服务就绪与
蜂窝解析重试展示的真机验收。

这些事实完成前，v26 仍是单卡开发/HIL 候选，不是量产版本；后端允许列表也不代表第五、
第六阶段完成或远程 URL 下发已经开放。
