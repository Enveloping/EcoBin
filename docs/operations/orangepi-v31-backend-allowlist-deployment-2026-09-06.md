# 香橙派 v31 后端认可版本部署记录（2026-09-06）

## 1. 结论

生产后端的厂家验收认可列表已把原始镜像存在启动缺陷的 v30 精确替换为 v31：

- 已移除：`hardware-runtime-20260906-30`；
- 已加入：`hardware-runtime-20260906-31`；
- v29 及其他已退役候选仍未重新加入；
- 业务程序远程下发总开关仍为关闭；
- 没有创建发布计划、设备部署或 OneNet 下行命令。

认可版本只决定后端是否接受设备厂家验收时上报的软件身份，不会写入 TF 卡或更新设备。

## 2. 替换原因

原始 v30 首次真实冷启动发现永久更新器漏打包三个运行依赖，日常业务服务读取软件事实时缺少
必要身份，并且蜂窝页面无法区分运营商注册过程与后台域名解析故障。修复已先在真实香橙派上
复测，然后由提交 `756ff54e78f1b28270f75eb5f0f17fc21481bf6d` 固化为 v31 镜像。
因此 v30 不再具备新设备验收资格，v31 则需要先被后端认可才能进行下一次热点厂家验收。

镜像身份及当前尚未取得的现场事实见
[v31 启动就绪修复镜像证据](../../hardware/image-artifacts/evidence/hil-startup-readiness-20260906-31/README.md)。

## 3. 变更与回退

变更脚本取得稳定部署锁，确认服务器当前列表精确包含 v30 且不包含 v31，然后创建 root-only
备份、在同目录原子替换配置、重新暂存运行秘密、执行真实模式生产预检并重载现有应用。任一步
失败只会在当前文件仍是本次写入版本时自动回退，避免覆盖并发变更。

保留的服务器回退文件为：

`/etc/ecobin/runtime.env.pre-v31-20260906T094409Z.7i4mjcoq`

它是 `root:root 0600` 普通文件，内容精确保存变更前的 v30 列表。本机仓库外受控副本
`C:\Users\24217\.ecobin\production\115.159.67.35\etc-ecobin\runtime.env`
也只修改了同一认可列表。

生产只读门禁工具也已原子替换为校验 v31、拒绝 v30/v29 的版本；安装前副本为
`/usr/local/sbin/ecobin-business-remote-readiness.pre-v31-20260906T0948Z.JBX0t6Xg`，新工具
SHA-256 为 `49f90d31ee26f8b5bb30e8b080cb0f9d1611ed6b12b15ef9f1a71db20d3b2dd3`。

## 4. 独立核验

第二套只读核验于 `2026-09-06T09:45:20Z` 完成：

| 检查 | 结果 |
|---|---|
| 服务器认可列表精确包含 v31、不含 v30/v29 | 通过 |
| 后端容器实际获得同一列表 | 通过 |
| 远程业务程序下发仍为 `false` | 通过 |
| 生产预检和运行秘密隔离探针 | 通过 |
| systemd 应用与秘密暂存服务 | active |
| 后端与 Web 容器 | running / healthy |
| 后端容器内健康检查与 Web 回环请求 | 通过 |
| 变更前备份内容、属主和权限 | 通过 |
| 已安装的 v31 生产只读门禁完整执行 | 通过；V66/131 表、健康发布、空计划、空活动部署 |
| 后端机器验收聚焦测试 | 13 项通过；v30 拒绝、v31 接受 |

该结果只是后端接纳准备，不构成 v31 写卡、冷启动、封存、投递清运或远程更新成功证明。
