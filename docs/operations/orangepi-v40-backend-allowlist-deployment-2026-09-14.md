# 香橙派v40后台认可版本部署记录（2026-09-14）

## 结论

项目负责人明确确认后，只向生产服务器`/etc/ecobin/runtime.env`的
`ECOBIN_DEVICE_ACCEPTANCE_SUPPORTED_EDGE_SOFTWARE_VERSIONS`追加
`hardware-runtime-20260914-40`。v37和其他既有认可版本全部保留，没有修改机器验收结果、
业务数据或应用代码。

服务器文件及运行中后端容器均已读取新值。生产预检、运行秘密隔离探针、后端健康检查、Web
回环入口和后端/Web双容器健康检查通过；远程业务程序下发继续为关闭状态。

## 变更边界

- 服务器：`VM-0-16-ubuntu`，`115.159.67.35`。
- 应用发布继续为`20260914064458-82e72e0dca16`。
- 后端镜像继续为`sha256:9b40cec445f90501c8306dc580db2877c6885ba8851af4442aa76e48261740d8`。
- Web镜像继续为`sha256:d32e68aeca898979714b9560c57b7a87fb44c7873eaf120325f39097d62602da`。
- 修改前建立root私有备份
  `/etc/ecobin/runtime.env.pre-v40-20260914T073335Z.EkKXYF5e`，元数据为
  `root:root 0600`普通文件。
- 部署使用独占锁、精确旧值校验、原子替换、并发修改保护和失败自动回滚；临时上传脚本已删除。

本次只改变后台对边缘运行版本的认可，不部署本地V83候选、UART合同或OneNet映射代码，不发起
设备更新、投递、清运、结算、余额或提现操作。

## 与TF卡的关系

同日制作的受控v40 HIL镜像已经包含厂家接入和热点所需输入，并写入指定TF卡后完成全范围回读。
后台认可列表只消除云端机器验收的“软件版本不受支持”门槛，不证明香橙派已经冷启动，也不证明
热点、注册、UART、HMI、RS485称重、门机构或断电恢复正常。这些仍需装卡通电后真实验证。

脱敏结果见
[v40受控现场验收镜像证据](../../hardware/image-artifacts/evidence/hil-uart2-rc26-v40-factory-login-20260914-40/README.md)。
