# 香橙派 v23 后端允许列表部署记录（2026-09-04）

## 1. 范围与结论

本次部署对应配置提交
`68ffebcfb0a09e6ba229a23dcc26b3dee6fe80a6`。服务器侧只修改
`/etc/ecobin/runtime.env` 中的香橙派业务运行时机器验收允许列表，并重载已有应用；没有修改
数据库、OneNet、COS 或设备，没有重新构建后端/Web 镜像，也没有发送任何更新命令。

最终结果为：

- 保留已经在用的 v13、v19 和 v21 运行时；
- 新增待执行第六阶段真机验收的 `hardware-runtime-20260904-23`；
- v15～v18 和 v20 继续不在允许列表中。

允许列表接纳 v23 只表示后端能够在设备实际报告该运行时后继续厂家验收。它不会启动候选
通信代理或更新器，不会执行镜像内暂存的业务包，也不表示远程业务更新已经开放。

## 2. TF 卡写入前置事实

项目负责人明确确认覆盖 Windows 磁盘 1，序列号为 `121220160204`，容量为
31,268,536,320 字节。v23 受控 HIL 镜像实际写入和全范围回读均为 2,571,108,352 字节，
回读 SHA-256 为
`c9c4318d91f03f7be3d91033cbad3fdfb29fd223e178d0b54056a0a6ec9be07c`，与源镜像一致。机器可读
结果见
[`flash-result-disk1-121220160204-v23.json`](../../hardware/image-artifacts/evidence/hil-stage6-business-runtime-update-20260904-23/flash-result-disk1-121220160204-v23.json)。

## 3. 变更前验证

服务器只读预检确认：

- 运行配置是唯一的 `root:root / 0600` 普通文件；
- 当前允许列表精确包含 v13、v19、v21，不含 v23；
- 稳定部署锁没有被其他进程持有；
- 没有输出运行配置原文或任何秘密值。

仓库侧将 v23 加入允许列表正例，并继续将 v15～v18、v20 和未知版本作为拒绝边界。聚焦
`TrustedDeviceAcceptanceEvidenceServiceTest` 13 项通过，`ecobin-module-device` 模块完整
236 项测试通过。

## 4. 可恢复性与并发保护

修改前创建的恢复备份为：

`/etc/ecobin/runtime.env.pre-v23-20260904T030837Z.eCP0Stuc`

备份及最终配置均核对为 `root:root / 0600`。部署使用稳定锁
`/run/ecobin-deploy/runtime-env.lock` 串行化同类配置修改，并在替换前后核对原配置和候选配置。
只有确认服务器当前文件仍是本次写入版本时才允许自动回退；检测到并发修改会拒绝覆盖。备份
有意保留，未经明确回退或保留期决策不删除。

## 5. 部署与独立核验事实

配置部署完成后，另一次独立核验于 `2026-09-04T03:09:44Z`（北京时间
2026-09-04 11:09:44）完成：

| 检查 | 结果 |
|---|---|
| 生产启动前检查，真实外部服务模式 | PASS |
| `ecobin-stage-runtime-secrets.service` | active |
| `ecobin-target-app.service` | active |
| 后端容器 | running / healthy |
| Web 容器 | running / healthy |
| 容器内允许列表：v13/v19/v21/v23 存在，v15～v18/v20 不存在 | PASS |
| 后端容器内置健康检查 | PASS |
| 服务器回环 Web 请求 | PASS |
| 运行秘密隔离探针 | PASS；设备接入 `enrollment=true`、远程维护 `remote_support=true` |
| 修改前备份的属主和权限 | PASS；`root:root / 0600` |

部署和独立核验脚本都通过标准输入交给服务器 root shell，没有在服务器 `/tmp` 留下脚本副本。
稳定锁文件和恢复备份有意保留。本记录不包含运行配置原文、数据库密码、OneNet/COS/微信
凭据、服务器凭据、设备密钥或业务包签名私钥。

## 6. 剩余边界

v23 尚需完成断电冷启动、厂家接入、永久通信代理链普通业务回归、第一份签名包成功迁移、
第二份故意不健康包自动回滚，以及多个关键阶段的断电恢复。通过前，第六阶段和 v23 量产资格
都不能标记完成，第八阶段远程下发也不能实现或启用。
