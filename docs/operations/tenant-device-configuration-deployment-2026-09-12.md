# 租户统一设备配置部署记录（2026-09-12）

状态：已提交并部署；19:41:08（北京时间）完成服务激活，部署与公网验证通过。设备应用确认仍有待办，详见下文。

## 发布内容

配置管理提供“设备配置”：平台维护默认单价、满溢规则和重量减少异常阈值；租户主体账号和拥有租户级设备配置权限的工作人员可以统一设置旗下所有机构、设备和投口，或恢复跟随平台默认。仅机构级授权不能修改租户统一配置。常用值直接展示，其余说明和参数按需展开。

当前默认值保留为 0.4500 元/千克、50.000 千克或红外任一满足、重量减少异常阈值 500 克。已有订单和已经开始的投递、清运保留原配置与单价；只有设备确认应用后才记为已应用，离线设备等待联网，新投递仍受现有配置确认门槛约束。

## 发布身份

- 代码提交：`b1be0e4a47197eb81b575ecd5029e2f651843f1a`。
- 发布 ID：`20260912112713-b1be0e4a4719`，来源工作区干净，Java 21 正式构建，未跳过构建或测试。
- 上一发布：`20260912094429-2d6c6c938e1c`。
- 服务器：`ubuntu@115.159.67.35`；入口：`https://www.jinshoubao.com`。
- 后端镜像：`sha256:641c592eb92585579a5966801e4ca149a2a4d1ca8085e53e3fba31c95025033a`。
- Web 镜像：`sha256:5da343bff0d51d4611c7ec80e0ae04b83c086176ac2fbf8cf0a2393fd2a0c8e2`。
- 发布包 SHA-256：`d7b6843334826a905167f9f55d36239fb4bfe19d90781a659c7aba37ebb88e72`。

## 备份、迁移与回退

19:29:39（北京时间）停止旧应用，确认没有应用数据库连接后生成完整加密备份，包含表结构、数据、触发器、例程、事件和 Flyway 历史。备份保存在服务器 `/var/backups/ecobin/tenant-policy-v71-20260912/` 和开发机仓库外的 `C:/Users/24217/.ecobin/production/115.159.67.35/backups/h02-pre-v71/`。完整备份 SHA-256 为 `17cec44dc1e05787afe43e4c02b1da93c1ab53a5aa32806a2cef619c444cc265`。

在无网络、关闭事件调度的本地 MySQL 8.4.10 中流式解密并恢复，未将明文 SQL 写入磁盘。134 张表（含迁移历史表）的行数与 70 条迁移记录均匹配；在恢复库预演 V71 成功，原满溢值及原有表行数保留。

V71 将默认表改为 `dev_device_default_policy`，增加租户规则表，形成 134 张领域表、71 条成功迁移、76 条权限定义及授权清单 38。迁移先停止旧后端，成功后直接启动本次后端；旧 V70 应用不能连接 V71 数据库运行。需要回退时必须恢复匹配的 V70 数据库备份及上一应用版本，并先处理上线后新增事实；不能仅切换旧镜像或修改迁移版本号。

## 验证证据

正式构建后端共 1,014 项：944 通过，70 因外部环境条件未启用而跳过，失败与错误均为 0。实施阶段设备规则 SQL 集成 29 项、租户授权 SQL 5 项、前端源检查 114 项、相关浏览器检查 18 项、最终配置页面浏览器检查 9 项通过。正式 Web 构建与资源大小门槛通过，入口压缩后 272.0 KiB。

- V71 现网迁移、最小权限完整矩阵、备份账号只读导出及禁止写入验证全部通过，迁移账号保持锁定，数据库未开放宿主机端口。旧 `dev_fullness_policy` 的表级与列级残留授权均为 0。
- 停机期间逐表核对原有 133 张业务表行数；旧设备配置、投口快照与订单的完整内容摘要均保持一致。启动后再次核对旧配置与投口快照摘要，保持一致。
- 9 个部署控制文件与来源逐字节匹配，本次更新检查数据库版本的就绪脚本。正式预检、服务与容器健康、回环页面、运行秘密隔离、持久日志权限与写入验证全部通过。
- 公网 82 个静态文件全部与本次发布包摘要相同；两个匿名会话入口均为 401，登录页无脚本错误。
- 真实平台登录 201、设备配置读取 200、退出 204、退出后 401。旧满溢页面地址跳转至设备配置；单价、满溢值、编辑窗口及更多设置的默认折叠/展开均验证通过，未发布新的业务设置。首次浏览器脚本未计入展开箭头的无障碍名称，并在弹窗关闭动画期间误命中同名控件；修正测试定位后复核通过，产品代码未作额外修改。
- 租户授权、隔离、发布与恢复默认流程由真实 SQL 集成测试和浏览器用例验证；本轮公网页面使用平台账号核对，没有更改线上租户配置。
- 13 台适用设备均已生成 V2 默认规则配置；截至 19:44，12 台待应用，1 台阻塞，已应用 0 台。阻塞记录对应服务器既有虚拟测试设备（资产 6，镜像 `ecobin-virtual-device:20260830-c2245a18d08a`）：新设备配置版本 3 已保存，未回传控制板同步或完整应用确认，任务因 `DEVICE_EVIDENCE_TIMEOUT`（设备应用确认超时）被阻塞。该设备此前版本 2 已应用。本轮仅完成状态核对，没有修改虚拟设备、人工补造确认或清除占用；这条设备确认仍需后续处理，不能把配置已生成视为已应用。
- 临时数据库隧道、临时迁移凭据、备份下载中转及发布包上传副本已清理，正式加密备份与不可变发布目录保留。

## 证据位置

实现与测试见[计划](../planning/tenant-device-configuration-2026-09-12.md)。忽略的验证目录为 `release-output/tenant-device-config-20260912/`；私有发布包、源码 Git bundle 和证据副本保存在 `C:/Users/24217/.ecobin/production/115.159.67.35/releases/20260912112713-b1be0e4a4719/`。迁移完整授权证据另存于同一服务器私有目录下 `evidence/tenant-policy-v71/20260912T113125Z/`。

本批从隔离工作树提交并发布；主工作区中的硬件、MCU 和其他并行改动保留。运行配置及设备业务程序远程下发开关沿用原值。
