# MCU与香橙派精简业务实施计划

日期：2026-09-13。状态：S0检查点已保存；S1实际MCU/Pi原生入口、云端配置、后台永久账本等待与正常收尾已本地接通；S2已接MCU重启问题收口、配置重装、投递末重失败/新读数恢复、控制通信失败、人工解除通信阻断，以及清运中断后的两袋人工确认与独立空袋皮重。S3连续离线600秒释放与原结果补报已完成本地候选；S4发布清单、现行业务/历史兼容测试迁移已经收口。当前增量已推进为UART rc.26、OneNet2.4.0、EdgeStore schema40、数据库V83的本地跨端候选，新增真实MCU能力位、统一HMI原子批次、原生工厂投影和投递通信失败无业务价值终结。硬件全量曾执行4261通过/123环境跳过/5子测试通过/2项旧固件身份断言失败，修正断言后两项定向复跑通过，不能合并称为单次全绿。ARM64容器内镜像安装、导入与合成数据smoke，以及v40两次独立构建字节一致性已经完成；真实香橙派启动/安装、串口屏、RS485、换袋、断电HIL和实际部署仍未完成，尚未发布。

依据：[D01-D15逐项决定](../architecture/mcu-edge-simplified-recovery-discussion-2026-09-13.md)。
用户已确认称重恢复只接受新业务，原已上报失败保持失败，并允许无其他核心分歧时进入计划实现。
经代码和既有决定核查，没有必须另行决策才能开始的核心阻断；技术细节由实现与测试确定，不继续扩大业务问卷。
本计划取代旧计划中不符合新共识的恢复目标，旧计划及候选证据保留，不整库回退、不删除用户已有工作。

## 1. 目标与范围

先让正常投递、连续投递、清运换袋在完整主入口跑通，再验证少量明确的失败出口。
MCU负责本地按钮、门控制、防夹、称重、业务结束；Pi负责发起、必要记录/照片、结果保存和云端上报。
不恢复Pi逐动作证明或跨重启多代关门机制，不把所有故障都展开成可恢复的机械流程。

只做本地代码、契约和测试。部署后端、变更线上数据库、烧录MCU/HMI、写TF卡及驱动真实机构另行取得操作授权。
复用现有Python 3.11、Java 21、工具链及依赖缓存；不为每批变更重新下载环境。

## 2. 冻结业务边界

| 场景 | 目标处理 |
| --- | --- |
| 正常业务 | MCU本地完成，Pi可靠保存完整结果后确认；保留原业务/用户归属、必要首末重量、重复处理保护 |
| MCU重启 | 放弃旧未完成执行，自行设置关门/锁定控制；Pi已有可靠完整结果优先，否则保留问题记录，不据过程记录重建成功 |
| Pi单独重启 | 找回同一业务，查询/接收原结果，不重发开门、不因自身重启直接判失败 |
| 控制通信故障 | 当前未完成业务失败/问题记录，拒绝新业务并交人工；不猜测机械位置 |
| 称重始终不可读 | 本次上报失败；新有效读数到来前停接单并提示人工，恢复后只允许新业务，不改原失败 |
| 烟感/测距/相机采集故障及烟雾报警 | 真实记录和页面提示，不因它们单独阻断；正常时继续采集，不把缺测伪造成正常 |
| 仅断网，MCU及存储正常 | 原业务完成保存待补报，不接新业务；连续离线超过10分钟释放用户/设备使用占用，保留原结果和归属 |
| 必要存储损坏 | 停接单、人工处理，不做多库备用重建，不承诺保存或上报一定成功 |
| 清运中断且袋状态不明 | 人工确认当前袋，必要时重新换袋/空袋称重；不恢复旧机械过程，不虚构清运重量 |

PB5仅在关门时防夹，触发暂停、解除继续；PB4不承担门位检测。门关闭是控制状态，不是实测到位。
称重250毫秒目标、单次最长5秒、既定稳定均值/超时中位数规则及清运前重减后重保持。
现有设备启用、授权、单业务互斥和已知满溢规则不因模块容错取消。

### 沿用而不重新裁决的资金边界

- 真正非重启的最终称重失败：沿用既有设计的系统异常订单，人工审核前不产生资金效果；
  设备原失败事实不改，人工审核认定独立记录，不等于称重恢复后自动恢复旧业务。
- MCU重启缺最终包以及已经问题归档的业务：只供排查，不产生正常结算、余额或自动提现；后到结果只补证。
- 仅断网待补报不等于问题归档；10分钟释放占用不使原完整结果失效或转为零价值。
- 不修改既有审核/提现配置，不在此任务中增加任何新的自动资金路径。

## 3. 保留、停用与改接清单

| 范围 | 处理 |
| --- | --- |
| 新称重读取/测量算法、独立时钟、执行器/防夹控制 | 保留并接完整目标，验证与当前接线一致 |
| 原业务身份、启动区分、最终结果保留/查询/保存确认、既有可靠上报 | 复用；只保留避免串单、重复执行、重复结算所需的核对 |
| 逐按钮/逐阶段重量/逐输出精确交接 | 不再作为MCU继续执行或Pi接收完整结果的前置条件；诊断记录不建立新的可靠补传义务 |
| Pi多代恢复关门、动作撤回/继任/旧效果推演 | 停止后续扩展，不接入新的正常流程；先列引用关系再停用，历史代码和测试证据保留 |
| 永久设备管理、远程维护/更新所有权和业务互斥 | 保留必需能力，不因裁剪机械恢复而整体删除管理层 |
| 后端/网页/小程序已有独立功能 | 保留，不通过Git整库回退撤掉其他工作 |
| 原固定帧适配 | 保留作旧版本明确运行模式及回退对照；新模式不借其伪造新协议事实，不并行猜测两种协议 |

## 4. 实施顺序及完成条件

每批必须改到实际调用路径并有针对性测试；不以新增一个脱离入口的框架或局部测试很多来宣称整套完成。

### S0：冻结新范围并核实保留能力

- 更新决定、入口文档及本计划，旧方案明确被替代而非继续自动推进。
- 保存工作区现状及基线证据；任何实质裁剪前先完成保留/停用文件清单和可恢复备份，不执行整库reset/clean。
- 运行既有最终结果持久化、Pi单独重启查询和称重失败报告的有限基线测试；结果仅证明保留组件，不证明新方案已接通。
- 完成标准：新旧目标不混用，已有缺口明确，第一批的文件/测试边界可核对。

### S1：接通最小的正常投递与完整结果

- 先确定开始/受理、当前业务查询、完整最终结果/已保存确认、必要故障和采集配合的数据清单。
  复用现有字段和编码工具；不要求Pi接收每次按键或引脚变化，完整最终包自身携带必要首末重量和结束原因。
- 同步 `contracts/uart/uart-registry.yaml`、生成器/守卫与实际MCU执行器、结果保留接口，生成文件只用既有生成器更新。
- 从 `mcu_delivery_execution.c` 等真实路径解除过程保存确认对本地执行的阻塞；Pi结果接收不再依赖独立过程/动作证明。
- 将正常投递接入MCU主入口/工程和Pi主入口，不通过旧恢复专用入口假称业务已接通。
- 完成标准：一次正常投递/继续投递可到最终结果可靠保存；中间诊断消息不影响本地执行，结果重复交接不重复处理，Pi重启不重发开门。

### S2：清运、最低运行条件和失败出口

- 清运本地重开锁/结束、最终首末重量和人工完成语义接通，正常结果使用前重减后重并更新袋/皮重。
- 对齐MCU/Pi的必要通信、称重、存储/联网准入；烟感、测距、照片与烟雾报警只提示，不漏掉其他真正阻断原因。
- 称重失败形成失败结果；持续按受控读取取得新读数后恢复新业务准入，原失败不被改写，不增加修复旧业务阶段。
- 通信故障按既有明确超时配置失败收尾；仅查询健康，不自动重放机械启动。其他故障人工恢复复用现有运维入口，并只检查最低运行条件。
- 照片采集失败使用已有缺失状态；必要存储无法保存不能被当作普通相机故障放行。
- 完成标准：每个已确认故障场景仅走明确出口；正常清运、异常清运人工袋确认和下一笔投递不串袋，不要求非必需模块全部健康才接单。

### S3：后端失败处理、10分钟释放与补报

- 接通现有设计的最终称重失败异常订单入口；目前Pi可生成该结果，但后端正常完成校验仍拒绝缺测，这是实现缺口，不是新业务决定。
- 基于后台确认的连续离线计时，超过600秒释放原用户/设备占用；重复离线事件不刷新本次起点，计时不从业务开始算。
- 明确表示占用已释放而结果仍可补报，同步活跃用户查询、设备占用、原结果接收和重复处理，不只删除占用行。
- Pi保留原身份及待上报结果；重连优先完成旧结果上报/收尾再允许本设备的新业务，用户在其他在线设备的使用不继续被阻断。
- 完成标准：10分钟后用户可使用其他正常设备；原设备离线仍拒单；补报归原用户、不误删新占用、不覆盖新袋，不把离线补报与问题归档混淆。
- 数据库修改使用新前向迁移，不改已部署迁移；Java多模块集成测试前先按项目规则install，真实MySQL验证不以H2替代。

### S4：页面、热点验收、完整构建与现场交付

- 复用已有故障和缺照片显示，体现“可继续/停接单/已失败/结果待补报”，不新增不必要的监控大页面。
- 复用现有HMI页面，不自动更改布局。完成/返回同为05时先核对原脚本语义；若确需区分，提供最小修改表，由用户操作上位机。
- 热点验收接新硬件接口，保留手填克数和实测数据；运行时容错不擅自放宽厂家验收标准。
- 完整MCU工程验证ROM/RAM/栈/时序；历史核心探针不算完整固件。核验真实RS485读数及250毫秒目标，不做自动校准。
- 完成相关契约/Python/Java/前端检查、发布清单与本地数据库版本对齐后，再申请具体部署/烧录/现场动作授权。
- 完成标准：成对软件有可交付制品、正常与已确认异常用例证据、真实现场验证和回退说明；未过这些条件不称为完成上线。

## 5. 协作与防止复杂度再次膨胀

主代理负责契约边界、整合和最终审查。可并行分工互不覆盖的后端占用/补报、MCU执行和界面测试；
共享协议先统一再改消费者，子代理不自行扩展未确认业务规则，不部署或操作硬件。

新增状态、表、协议消息或恢复阶段之前，先说明其保护的已确认需求；现有简单终态/记录能表达就不另建机制。
不能用“所有罕见故障都自动恢复成功”替代本计划。真正需要改变资金、业务含义、硬件接线或现场操作时才再询问用户。

## 6. 本轮执行记录

- 已固化D12只恢复新业务，检查确认没有新的核心业务分歧阻止计划开始。
- 已建立本精简计划并梳理第一阶段保留/停用范围；业务代码尚未在本轮改动。
- 使用已有Python 3.11环境运行有限结果交接/重启查询/失败报告基线：**18项通过，13.15秒**。
  范围：完整结果提交后重开数据库及去重、Pi重启后的同启动查询等待、结果交接客户端、真实C末次称重超时的失败报告。
  JUnit保存于本机临时目录 `ecobin-simplified-plan-baseline.xml`；不是全量测试，不证明新正常主循环或新故障策略已经实现。
- 独立只读复核通过：资金边界、D12/D13、旧恢复停用范围和现场操作授权清楚；没有新的核心业务分歧阻止开始。
- 下一步S1先完成裁剪前现状保全及精确引用清单，再对真实正常业务路径改接；本轮未创建代码快照或删除旧模块。
- 未整库回退、删除候选代码、连接设备、变更线上数据或部署。

### 检查点后的S1实施（2026-09-13）

- 用户要求先提交原改动，已创建本地检查点 `3178a994`，535文件；没有推送。
  `hardware_mcu.zip`内含本机配置和构建产物，留在本地而不入库。该提交包括未完成候选，不是可发布版本。
- 精确改接边界：`mcu_work_preparation`、`mcu_delivery_execution`、`mcu_clean_execution`及
  `mcu_opening_gate`去掉正常业务对二次授权/逐过程确认的依赖；保留`mcu_session`、
  `mcu_work_state`、`mcu_result_slot/builder`和真实`weight_measurement/actuator_runtime`。
  完整`main.c`、USART1/USART2、HMI及Keil工程并行改接；不挂`mcu_safe_close_execution`。
- Pi的`native_result_evidence`新正常分支直接读取完整`WORK_RESULT`的首末测量，
  `native_result_report`生成v2报告，不再要求独立按钮/动作事件或逐动作永久确认。
  仍验证原START、启动/业务身份、配置、完整结果冲突及原业务许可；v1已创建报告保留只读历史校验，
  不把旧报告重写为新报告。`main.py`完整新正常入口尚待接通，不能据此宣称已可接单。
- UART rc.23保持199字节最终结果布局。MCU正常终结/纯末称重超时的结束语义保证最近CLOSE已经下发；
  Pi据此显示关门控制，不宣称物理到位。控制丢失/停止用CANCELLED，不能混为纯称重超时。
  正常结束必须包含独立有效首末重量；清运完成须锁断电及真实人工结束意图，无锁反馈的健康写UNKNOWN。
- Pi新专项6项通过：无过程/动作记录及丢START回复仍唯一上报、投递/清运重量投影、末秤超时缺值不补零、
  原身份不符拒绝、归档前保存结果优先、归档后迟到仅补证。测试使用模拟线级输入和真实SQLite，非实机。
- 扩大结果/交接/契约测试27项通过、26子用例通过、1项失败：旧`test_v18_upgrade_is_atomic_and_preserves_existing_business`
  构造v18库时遗漏新native表，断言“全部native表已移除”得到10而非0；尚未处理，不宣称全绿。
- 完整MCU构建、Pi完整主入口、正常照片与二维码显示、所有故障/重新准入、后端S3及现场交付仍待完成。
- 后续复核修正：通用线级解码继续接受历史缺测完整包，不以新质量规则破坏旧库启动；
  新v2正常报告在分类时要求有效首末重，已保存缺测包仍是“完整结果已取得”，不误当重启丢失。
  从检查点真实C和旧Pi生成的v1清运缺测样本已加入冷启动测试，4项通过，原报告及原摘要保持不变。
- 已补报告存储入口的原ACTIVE业务许可快照核验（ACTIVE表示永久管理层已确认该原作业开始），
  防止误传其他许可号锁死原报告，不恢复逐动作证明。新版结果专项现8项通过。
- 上述v18测试夹具已修正：从新空库构造旧库时删除全部新增native表，并证明未删除任何业务数据；
  当前结果交接/结果策略/契约专项重新运行**30项通过、26子用例通过（13.22秒）**。
  JUnit：本机临时目录`ecobin-simplified-result-s1.xml`。这是相关子集，尚非全量回归。

### S1本批统一集成结果（2026-09-13）

- 实际MCU主入口、串口中断、RS485称重、HMI及自主执行器已经接入默认完整工程；
  首次START之后，无须Pi逐按钮、逐动作确认即可完成投递/连续投递/清运。
  仍保留最终结果直到Pi精确保存确认，不把“去掉过程确认”扩大成“结果可以丢弃”。
  GPIO关闭控制不冒充真实门位反馈，PB5关门防夹不变。
- 新增真实C到Pi的3项纵向回归：Pi原配置/START字节真正送入C，故意丢弃START回复，
  C生成完整结果；Pi经原业务查询、真实结果交接和SQLite提交，生成唯一报告。
  独立数据库连接确认发送RESULT_SAVED之前已经提交；中途及结果保存后三次重开数据库，
  下行没有第二次START，过程/动作表均无记录。云端命令来源与物理传感器仍为测试夹具，
  不证明正式Pi主循环、后端接收、相机、真实串口或整机联调完成。
- 主代理最终统一运行以下8个测试文件：**102项通过，34.04秒**；
  `test_native_mcu_board_entry`、`test_mcu_weight_run`、`test_mcu_simplified_execution`、
  `test_simplified_mcu_pi_business`、`test_native_simplified_result`、
  `test_native_legacy_result_compatibility`、`test_native_result_handoff`、`test_mcu_result_handoff_client`。
  JUnit：本机临时目录`ecobin-simplified-s1-integrated.xml`。
  109份契约生成物重新生成并一致性检查通过，完整跨Python/Java/C契约校验**25项通过**。
- 主代理在独立输出目录复编译完整MCU工程成功：ROM54,600/65,536字节，
  RAM11,824/20,480字节（含4,096字节栈、零堆）；BIN摘要与子代理构建一致。
  构建、栈分析、HMI限制及现场门槛见[完整MCU入口记录](../../hardware/docs/review/mcu-native-full-entry-s1-2026-09-13.md)。
  输出仍是本地验证制品，不是可直接正式烧录的发布包。

### 紧接本批的实际入口改接

下列是现有代码缺口，不是要求用户重新确认业务规则；不另建逐动作恢复机制。

1. 保留`EcoBinEdge`现有云端、照片和设备管理骨架，原生模式单独接启动与业务轮询。
   `CommandProcessor`可以注入新业务处理对象，但旧`boot_sequence`、UART事件线程及旧恢复钩子必须分流；
   原生串口的创建、握手、START和查询由同一线程负责，不能两个线程并读或把UART v1帧伪装成v2。
2. 同步配置生产与消费：后端`DeviceConfigurationCanonicalizer`及Pi现有配置入口仍使用v1摘要和旧参数；
   原生配置使用v2摘要且字段更多，不能只改START或悄悄补参数后声称旧摘要已应用。
   应明确冻结配置协议档，给原生设备产生新配置版本；旧配置内容、摘要、历史报告和旧运行模式保持不变。
   实际命令名为`APPLY_CONFIGURATION`，OneNet字段校验、Java/Pi编码和MCU字节必须一起验证。
   固定工程参数不必增加管理员可调表单；200毫秒响应期限、750毫秒采样年龄等不是用户保证的硬件事实，仍须实测。
3. 最终报告后的必要收尾尚待接：按原报告/后端确认完成原命令和永久业务许可，
   清运按本次后重更新原定新袋皮重，然后只释放匹配的原业务槽，不生成第二个完成事件。
   称重/通信故障准入、S3离线10分钟释放、热点验收、二维码/固件管理和现场交付继续按本计划推进。

因此S1尚未整体完成，S2/S3/S4也不能因本批组件测试或完整MCU编译通过而标为完成。

### 原生完整业务入口与配置增量（2026-09-13）

详细代码边界、验证与明确缺口见[原生业务入口记录](../../hardware/docs/review/native-business-entry-s1-2026-09-13.md)。
以上“紧接本批”的三项正常路径已推进，但不是整个计划完成：

- `main.py`显式uart-v2分支接单一串口所有者；正常投递/连续投递/清运由MCU自主完成。
  原始完整结果提交后才确认保存；准确后端确认后，完成原永久作业、应用原新袋皮重、释放原占用，重复不覆盖后来业务。
- Java实际配置生产、OneNet2.3.0显式档位、Pi v2摘要与真实C字节贯通；旧v1保持不变。
  配置落盘但指针未写的重启切点已修复，原分片不重新编号或重发；不允许新配置覆盖旧待办。
- 状态查询250毫秒，采样年龄按原请求与真实采样时间累计，750毫秒内允许跨查询间隙使用；新启动号不复用旧缓存。
  已保存且已释放的MCU旧结果标记不再误拦下一单。
- 原生照片走已有异步队列，10秒阶段期限后缺失也继续，UART持续运行；原子退役防止迟到照片串阶段。
  云连接、远程维护查询、热点进度和运行快照移到非UART线程，复用原快照配置频率及故障显示状态。
- 主代理最终统一20文件回归：**276项通过，68.78秒**。包含18项真实C运行入口、25项正常收尾、12项配置恢复、
  5项照片阶段到期、8项main路由/线程/进度及其他称重/交接/旧模式相关测试。
  JUnit：本机临时目录`ecobin-simplified-native-entry-integrated.xml`。
- 主代理复跑Java21离线定向测试并install：**83项通过，12.194秒**；协作代理真实MySQL8.4.10配置发布/切档15项通过，
  临时测试环境已清理；全工程离线install -DskipTests已通过。没有线上数据库操作或迁移。
- 主代理复核111份生成物完全一致，完整跨语言契约校验**25项通过**。既有运行快照投口结构仍有34成员待整改，
  本批配置投口维持19字段；本地绿色不代表全部物模型已通过OneNet实际导入。
- 随后修正等待后端确认时反复查询永久账本：先本地检查原BUSINESS_APPLIED，没有就继续串口轮询。
  此修正与真实C入口/正常收尾联合**44项通过，18.78秒**（与276集合重叠，不能相加）。
- 原确认测试34个失败根因为旧夹具先要求已取消的二次开门授权，已换成专用自主C夹具，35项原断言通过。
  v31升级与旧清运缺测使用检查点真实v1快照，不改生产确认代码或共享旧执行夹具。
  主代理最终确认/入口/收尾/v1历史兼容联合**83项通过，26.12秒**；JUnit为本机临时目录
  `ecobin-native-result-confirmation-integrated.xml`。此集合与上述回归有重叠，仍不是全量硬件套件。

### 本轮起点：入口慢IPC与S2明确失败出口

本地IPC是Pi业务程序与永久管理程序之间的请求，不是MCU串口。已复核一次永久账本请求最多两次5秒等待，
本轮起点的START授权、首次报告和完成回执仍同步调用，健康MCU可能因此被延迟轮询；
以下是当时任务拆分，当前完成范围见后面的提交检查点记录。

1. 保持一个UART线程；仅将账本请求放到单个后台执行器，结果绑定原操作、原许可和原START/配置分片。
   本地槽、报告与完成事务仍由UART业务线程核验/提交，不把整个命令消费者放到线程、不在发送检查内等待Future。
2. START先保存原身份并返回等待；真实ACTIVE许可返回后再次核对新鲜启动号、配置、读数和有效期，然后单次发送。
   配置维护检查、报告许可读取、永久完成请求同样分段；保留现有准确结果/许可校验，不默认相信缺失快照。
3. 已登记发送只查询；尚未发送即Pi重启的原业务需明确失败/释放出口，不能永远占用。
   再接通信故障/已确认MCU重启/全程称重不可读的已确认出口、仅恢复新业务，以及清运袋人工确认。
4. 后端S3实现拆分见[连续离线释放占用审计](mcu-edge-offline-occupancy-s3-audit-2026-09-13.md)。
   先保留原归属/袋预留/最终结果资格，再释放使用占用；不另建MCU恢复流程，不让重复包覆盖新占用。

不需要用户当前操作。后端部署、MCU/HMI烧录、写TF卡和真实机构测试仍须到相应阶段另行授权。

### 提交检查点：后台账本调用已接，S2组件与完整流程区分（2026-09-13）

用户要求先提交已有修改；当前停止扩展实现，先保存本地检查点，不推送、不部署。
本轮细节见[原生入口检查点记录](../../hardware/docs/review/native-business-entry-s1-2026-09-13.md#提交检查点账本后台等待与问题收尾组件2026-09-13)。

- 上述第1、2项已经接入实际原生运行入口：单个后台账本请求不阻塞串口；发送/本地事务仍由原线程负责，
  实际许可、原身份、配置、重量与期限校验不省略。回复丢失重试不换身份，Pi重启后复用原完成摘要。
- 问题归档收尾的本地准备/应用组件已实现，38项专项通过；仅核对原归档确认和原永久取消回执，
  精确释放原槽，不写袋或皮重。**运行入口和后台账本调用尚未接入该组件**。
- 主代理冻结协作文件后统一8文件：**129项通过，43.45秒**，JUnit为本机临时目录
  `ecobin-native-checkpoint-20260913.xml`；111份契约生成物一致性检查通过。与此前专项重叠，不累计。
- 第3、4项仍未完成：控制通信/称重失败、重启归档及配置恢复、未发送START退出、清运袋人工确认、
  S3离线占用与原结果接收继续推进；S4及现场交付仍待后续。该提交不是可部署或可烧录版本。

### 检查点后的MCU重启、配置重装与末重失败（2026-09-13）

详细实现、验证和未完成边界见[本批S2记录](../../hardware/docs/review/native-reboot-and-weight-failure-s2-2026-09-13.md)。

- 已确认MCU重启且归档前没有可靠完整结果时，实际Runtime自动建立原投递问题；归档事务最后重查完整结果，
  避免把已经落盘的原结果误判丢失。归档后迟到结果只关联原问题，不恢复订单、资金、袋或后续状态。
- MCU新启动后从原已应用云端配置重装相同设置；Pi重启只查询原重装分片，MCU再次重启另建新启动分片。
  重装本身不直接开放接单，仍须满足全部最低运行条件。
- 真实MCU投递末重等待5秒仍不可读时，Pi上报原完整失败包；后端建立末重、净重、金额为空的系统异常订单，
  不注册自动审核、不改余额、不触发提现或容量投影。永久失败回执到达后才释放原本地槽。
- 新鲜有效称重只清除对应称重故障并恢复后续新业务；旧失败记录保持失败，也不清除通信/存储等其他阻断。
- Python统一**235项通过（90.69秒）**；Java 21离线安装通过，定向及真实MySQL 8.4.10两条事务共**9项通过**。
  测试库为V79/137张领域表/运行授权清单40，隔离容器已清理。集合与此前测试重叠，不累计。
- 控制通信失败的原业务收尾和未发送START退出已由后续批次完成；清运中断人工袋确认、S3连续断网600秒释放及S4仍待继续。
  本批没有部署、烧录、联网修改OneNet或操作现场机构。

### 控制通信失败收口（2026-09-13）

详细实现和验证见[控制通信失败收口](../../hardware/docs/review/native-control-communication-failure-s2-2026-09-13.md)。

- 默认10秒整链路超时保持；新增原START自己的10秒响应/查询期限，其他设备事实回复不能掩盖该命令长期无回应。
- 超时先原子冻结命令失败、可靠观察事件和原槽收尾凭据，再按永久许可的不存在、已授权或已开始状态精确放弃/失败，
  取得读回凭据后只释放原槽。完整最终包若已先落盘则优先；失败后迟到包只保存，不恢复正常业务或结算。
- 真实通信失败建立设备级UART阻断故障，不自动恢复接单；未写START在Pi实例更换后明确开始前失败，但不冒充MCU通信故障。
- 新增5项，相关统一**151项通过（78.79秒）**；后端既有投递/清运命令观察决策**11项通过**。
  未改UART/OneNet版本；未部署、烧录或操作设备。
- 人工解除通信阻断、清运中断人工袋确认、S3连续断网600秒及S4仍待继续。

### 连续离线占用释放与原结果补报（S3，2026-09-13）

详细实现、验证和保留边界见[S3专题记录](mcu-edge-offline-occupancy-s3-audit-2026-09-13.md#8-s3-本地实现与验证结果)。

- 后台只以可信 OneNet 首次连续 OFFLINE 的数据库接收时间计时；重复 OFFLINE 不刷新，ONLINE 清空。严格超过600秒后，投递或清运释放器在同一短事务写释放标记并精确删除原设备占用。
- 释放不结束原业务、不判失败、不动可靠任务、袋、皮重或Pi本地待补报槽。原用户可去其他在线设备；原设备在原结果完成前整机仍拒绝新投递/清运，查询页面明确显示待补报。
- 迟到完整结果仍按原用户、原配置、原袋和原业务编号校验并正常处理；若出现其他业务占用，绝不删除新占用或覆盖新袋。真实未开门/未解锁失败及未派发过期出口也支持已有释放标记后的精确零行删除。
- 若投递结果长期不返回，既有人工“确认从未开始”或“未知物理结果异常隔离”仍可在原安全确认和可靠任务条件下结束待补报；离线释放本身不会自动触发这些动作，问题型隔离仍不产生订单、余额或提现。
- 新增V80，领域表保持137，运行权限清单41。完整V1→V80迁移、最小权限、真实MySQL事务、Java 21模块安装、recycling全量171项、OpenAPI/Web/小程序检查均通过。
- device全量334项中333项通过；唯一既有失败是发布包固定文件白名单未纳入此前S1/S2硬件新增文件，留给S4发布清单收口，不能据此称整套可发布。
- 未部署、未连接设备或共享数据库、未烧录。S4以及人工解除通信阻断、清运中断人工袋确认仍待继续。

### 人工解除控制通信阻断与发布清单收口（2026-09-13）

- 新增本机 `root` 专用状态/恢复入口。现场原因已排除、当前无活动业务、当前MCU启动号下有新鲜真实通信，并且提交的故障编号仍精确匹配时，才原子记录恢复事件并清除这一项通信阻断。
- 偶然收到一帧不会自动恢复；解除通信阻断后仍重新检查称重、配置、其他控制故障和业务互斥。原失败业务保持失败，不借恢复入口重新结算。
- 发布包固定清单已补入S1/S2实际运行文件、新恢复CLI以及审计发现的6个原生主链真实依赖：业务应用59个文件、schema 25、Python 3.11权威摘要 `c351806f8656137c3c90bef2e7df4354a701e8818aafecf53b6540e265ca40bd`。
- Python相关84项、Java 21发布清单5项通过。详细条件见[控制通信失败收口](../../hardware/docs/review/native-control-communication-failure-s2-2026-09-13.md#人工确认恢复增量)。未部署、未连接设备或执行现场恢复。
- 清运中断审计确认：投递的MCU重启问题归档不能直接套到清运；现有空袋基准只接受厂家初始袋，不能把普通清运新袋伪装为厂家袋。按D15先做中断归档和袋状态锁，再接人工扫码确认实际袋及该袋独立皮重测量，不恢复旧机械动作。

### 清运中断第一片（2026-09-13）

- 已确认MCU重启且原清运没有可靠完整结果时，原开始命令以准确重启缺结果原因失败，后台进入人工处理；不冒充Pi重启、不生成正常清运记录或重量。
- 清运重启或控制通信失败先在原失败事务中锁住投口袋状态，再完成永久许可失败并释放准确原槽。通信恢复入口不清袋锁，新业务在实际袋和皮重确认前仍拒绝。
- 完整结果在失败冻结前已可靠保存时仍优先正常完成；袋锁写入失败会整体回滚，不留下“业务已释放但袋未锁”的切点。
- 详细边界和35项Python、两组Java验证见[清运中断第一片](../../hardware/docs/review/native-clean-interruption-s2-2026-09-13.md)。人工扫码确认实际袋和普通清运袋独立皮重是下一片，当前尚未部署。

### 清运中断人工袋确认与独立皮重（2026-09-14）

详细实现、并发边界和验证见[本批S2记录](../../hardware/docs/review/interrupted-clean-bag-recovery-s2-2026-09-14.md)。

- 后台V81只允许原清运员确认原旧袋或该清运已预留的新袋；第三只袋必须现场换回，不增加第三套恢复状态。
  旧袋核对成功后释放预留、占用和后台阻断；新袋必须显式确认空袋，并在独立系统基准成功后原子切换当前袋与皮重。
- 原清运始终保持`ABORTED`，不补建正常清运记录；迟到结果只补充证据，不覆盖袋、皮重或后续业务。
  基准失败回到`BASELINE_REQUIRED`，保留旧袋、旧皮重、占用和阻断，修复后由人工重新扫码发起新测量。
- MCU实现250毫秒策略校验、5秒上限、稳定均值/超时中位数及可靠结果释放；Pi只发一次测量，重启后只查询。
  云端等待、本地命令和工作槽同一SQLite事务提交，成功/失败/超时均以持久凭证退休原命令。
- 小程序从待恢复清运进入，只能扫码，不提供手填；新袋二次确认空袋。请求在发送前保存完整请求体和幂等键，
  网络不确定复用同一请求；等待基准只查询，未知动作或`CONTACT_SUPPORT`不会被误显示为恢复完成。
- V1→V81真实MySQL通过：81/81迁移、138张领域表、运行权限目录42；Java设备335项、回收178项，
  完整契约588项/1720子用例，前端121项，主代理选择的Pi相关185项，MCU严格C11专项及完整构建均通过。
  各集合存在重叠，不累计为总测试数。
- 本批未部署、未烧录、未连接现场设备或共享数据库。旧逐动作MCU集成夹具仍需在S4按rc.23精简START边界更新或退役，
  此后还需真实RS485、换袋、断电和整机HIL，才能形成可部署发布结论。

### S4发布清单与隔离包保管（2026-09-14）

详细问题、边界和验证见[S4记录](../../hardware/docs/review/simplified-release-custody-s4-2026-09-14.md)。

- 发布声明从schema25对齐当前`EdgeStore` schema40；补入恢复候选入口、运行时和旧关门效果隔离三个
  真实依赖。业务包现为62文件、摘要`f45f4370…`，完整运行包为73文件；永久层排除边界不变。
- 后台包验证器同步文件集合、schema和摘要，未来不一致的签名包仍拒绝；本批没有更新任何现网后台或设备。
- 24项发布隔离测试改用真实START自主执行，隔离重开不新增命令、不提前释放占用，也不把清运末重失败
  伪造成正常完成。发布相关92项及后台验证器5项通过。
- 更大范围的旧共享逐动作夹具仍在分类和迁移，S4尚未整体完成；现场HIL、Linux/ARM64安装、热点、
  固件管理、制品、部署和烧录门槛保持。

### S4旧夹具第一批迁移（2026-09-14）

- MCU投递/清运执行测试改为START后自主开关门、PB5关门防夹、清运锁脉冲、本地按钮和最终结果；
  两条已退役逐动作命令继续明确拒绝且不写GPIO，不恢复旧入口。
- Pi工作恢复、投递问题归档/上报/确认改用同一自主C流程，不创建逐动作许可；完整结果优先、缺包
  问题归档、迟到结果仅补证和冻结v1兼容边界保持。
- 修复恢复许可核对：当前流程无旧动作绑定时，以工作槽在START前持久保存的完整永久许可为权威；
  另一许可、投口、业务类型、START身份或发送状态不能冒领原业务。该拒绝不会归档问题、释放占用
  或改变准入。
- 主代理合并相关自主MCU、Pi恢复/归档、v1兼容、纵向业务及发布保管共**265项通过**；详细见
  [S4记录](../../hardware/docs/review/simplified-release-custody-s4-2026-09-14.md#旧夹具第一批迁移)。
- 兼容测试帮助函数暂留给下一批历史用例；生产入口不依赖它。未部署、烧录或操作现场设备。

### S4旧夹具第二批迁移（2026-09-14）

- 投递开门前、关门后称重、继续/结束/中断和清运首重/中断六个测试文件，从117项旧过程
  回执组合收敛为38项当前START自主行为测试；保留称重质量、5秒失败/中位数、门时序、防夹、
  当前按钮、取消和不可变结果等业务边界。
- Pi结果完成、证据和上报测试改为完整`WORK_RESULT`权威，不要求独立按钮/动作表；保留原
  START/许可/配置/首末测量、SQLite损坏、冲突、报告原子性、幂等、照片和价格校验。
- 投递末重失败继续形成无净重的异常报告；清运末重失败保持本地失败，不产生正常清运完成或
  当前袋/皮重写入。测试本身不再从旧结果执行模块借连续投递动作帮助函数。
- 一份现行测试夹具补齐真实入口已有的工作槽原许可；新许可完整性校验不放宽。主代理扩大合并
  回归**437项通过（107.46秒）**。历史动作账本和恢复关门兼容测试仍待单独收口。

### S4旧夹具第三批迁移（2026-09-14）

- START准入、清运本地按钮和结果执行测试已改为一次START后由MCU自主推进；首重失败零动作，
  重开/结束不再等待Pi保存过程回执或二次确认，废弃动作命令只验证明确拒绝且零GPIO效果。
- 完整结果自身作为现行执行事实，正常判断不依赖动作绑定、过程行或输出事件；仍保留原START
  身份、轮次、测量顺序、摘要、同身份冲突、SQLite重开/损坏和重复保存幂等校验。
- 本批及直接消费者组合**200项通过（40.47秒）**。交叉运行发现旧业务内采样组过程交接套件
  17项仍绑定旧编译夹具；它进入下一批现行结果事实迁移，不通过恢复旧命令或跳过测试处理。

### S4旧夹具第四批与历史兼容迁移顺序（2026-09-14）

- 业务内采样组改为现行自主结果与可选诊断分离：`WORK_RESULT`不增加满溢字段，称重结果不等
  过程回执；超声波真实样本、降级、中断、快照和多轮不串组继续独立验证。
- 现行恢复wire移入中立自主夹具，解除`action reconciliation → clean action → work recovery`
  的测试依赖环；兼容名称保持，且wire禁止旧首开、旧清运解锁及`SAFE_CLOSE`写出。
- 主代理恢复/问题/结果/采样组合**261项通过（61.99秒）**，MCU采样相关另行**44项通过
  （10.01秒）**；硬件套件4475项可完整收集。
- 历史303项按固定顺序迁移：先将两份动作账本测试改为checkpoint-v1只读兼容，再建立冻结
  recovery-close输入，随后让恢复候选只查询/确认/退休/撤回/隔离既有记录，最后迁移九个共享
  消费者并退役主动创建或发送新`SAFE_CLOSE`/successor的断言。生产旧记录读取模块保留。

### S4旧夹具第五批：冻结动作与规范化恢复输入（2026-09-14）

- 旧首开/清运动作动态测试由101项收敛为34项真实checkpoint-v1只读兼容；原历史表行、受理、
  输出、测量、完成及v1报告保持，身份/摘要/载荷/回执/时序损坏仍拒绝且不修补。
- 新增18项恢复关门永久账本规范化输入。准确命名为“ARMED但Edge认领未知”和“CONFIRMED有
  输出”；不把永久账本没有的`write_claimed`事实伪造成已认领或未认领。该输入不是SQLite导出，
  来源与局限写入fixture。
- 两动作文件与全部结果系列**209项通过（42.42秒）**；本批冻结动作、永久输入和v1兼容由主
  代理合并复核**56项通过（6.97秒）**。余下约202项恢复候选共享测试按既定顺序继续迁移。

### UART v2二维码URL可靠应用纳入发布门槛（2026-09-14）

详细现状、可信事实、协议形状和实施切片见
[UART v2设备二维码URL可靠应用计划](../../hardware/docs/review/uart2-device-entry-url-plan-2026-09-14.md)。

- rc.23的`SYNC_DEVICE_ENTRY_URL`只可靠证明香橙派保存；uart-v2没有发送入口，发送失败还会被
  warning吞掉并完成后台命令。rc.24本地候选已关闭这一缺口，旧验收字段仍不得冒充屏幕已应用。
- 用户已确认信任边界：MCU把完整、无截断的`page0.qr0.txt`指令原子交给UART3发送队列，即视为
  串口屏已经显示；不追踪USART发送完成，也不增加屏幕回执。现发送函数会静默丢溢出帧，必须先
  检查整帧空间，做到零字节或整帧入队并返回明确结果。
- 后续对抗审查确认，旧普通页面写在溢出时还会清空此前队列，能撤销已经确认的二维码。现所有
  高层串口屏命令都改为整条原子入队；新命令放不下时只拒绝新命令，不删除任何已排队显示指令。
- 单帧242字节装不下60字节命令身份、192字节URL和32字节摘要，rc.24已使用BEGIN/PART/COMMIT/
  APPLY_RESULT分片事务且不扩大全协议帧上限。Pi按URL变化、MCU新启动号和UART重连可靠重发，
  同一启动/连接已有结果则不再60秒盲发，业务中延迟到空闲。
- 后台SYNC与整机验收均已改为等待匹配结果落SQLite；旧`Stored`字段保持“只在Pi保存”的历史含义，
  原生验收证据版本5新增应用摘要、MCU启动号和原子入队依据。香橙派为云端SYNC产生可靠应用结果
  事件，后台不再把OneNet接受下行当成屏幕已应用；数据库候选推进至V82，OneNet映射候选2.4.0。
- Pi重启后，验收命令必须等当前启动编号和当前UART连接的二维码应用证据；启动尚未绑定、重写
  未完成或旧结果迟到时持久等待，不提前生成验收失败证据。
- 硬件全量回归4185通过、123项按环境跳过、5个子测试通过；25项契约检查和Keil完整重建通过。
  固件Flash 59,676/65,536字节、RAM 12,688/20,480字节。真实串口屏、现场RS485、部署和烧录
  仍是发布门槛。

### UART rc.25身份、终态失败与工厂恢复收口（2026-09-14）

详细实现和发布边界见
[rc.25收口记录](../../hardware/docs/review/uart2-rc25-terminal-factory-closure-2026-09-14.md)。

- rc.24二维码链保持原语义；rc.25增加只读固件身份查询和当前MCU启动下最高已消费命令序号，
  用于热点验收、香橙派命令序号续接及工厂到生产入口的安全交接。查询不驱动机构、不称重、
  不创建业务，也不把工厂动作冒充生产动作。
- 工厂验收先用持久`actionToken`准备准确START身份，再只允许同一身份写出一次。香橙派和MCU在
  START写出、结果保存及确认之间掉电时，只查询原命令和原结果；`NOT_SEEN`才允许退役原动作，
  `BOOT_CHANGED`先持久记录后转人工确认，任何恢复分支都不生成替代START。
- MCU已释放的最后一条工厂结果只通过一次性工厂启动高水位标记交给全新生产库；只有准确匹配
  启动编号、结果序号和工作编号时才允许建立生产基线。它不转移业务、用户、资金或命令所有权，
  也不允许重放工厂动作。
- 投递完整结果为取消或失败时，后台按设备中止结束，准确释放本次占用，不创建正常订单，不增加
  余额、不触发提现。清运初始称重不可用在开锁前结束并释放预留袋；已经可能开锁后的末重失败、
  取消或失败则保留新袋和人工恢复联锁，不写正常清运完成。
- 烟感、超声波满溢等非核心采集异常仍只产生告警和可靠问题事实，不单独阻止业务；只接受当前
  MCU启动、当前身份和新鲜的设备事实，旧启动或迟到回复不能覆盖现状。PB5仍只在关门方向执行
  防夹，不单独作为设备故障。
- 串口屏的可信事实保持已确认边界：完整、未截断的显示指令原子写入UART3软件发送队列即视为
  已显示；不等待USART硬件发送完成，不增加屏幕回执或读回。队列不足时必须零字节生效并报告
  失败或忙，后续命令不能清空、覆盖或撤销此前已经入队的完整命令。
- 当前工厂原生动作日志使用schema 1，因为该文件是本轮新增且从未部署，没有线上迁移来源；若
  将来存在旧版落盘文件，必须先新增保守迁移，不能直接放宽校验。活动动作、迟到回复和已完成
  动作均有界保存，损坏或内部映射不一致时拒绝使用。
- 本地验证为：UART注册表2.0.0-rc.25共73种消息；完整契约598项及1748个子测试通过，113份生成
  文件一致；硬件4250项通过、123项按环境跳过、5个子测试通过；工厂原生恢复35项通过；真实
  MySQL 8.4从V1迁移至V83，83个迁移、138张领域表、权限目录版本42及终态失败约束通过；Java
  九模块完整测试1320项、0失败、0错误、195项按外部环境跳过，随后本机Maven缓存安装成功；
  另补真实C端工厂清运释放到生产首次START纵向测试，相关两文件43项通过；完整MCU构建0错误，
  Flash 60,288/65,536字节、RAM 12,696/20,480字节。
- 以上只是本地候选证据。真实串口屏二维码、RS485称重、门与清运机构、掉电组合HIL、Linux/
  ARM64安装、镜像、部署和烧录仍是独立发布门槛，本轮未操作设备或线上环境。

### UART rc.26能力、HMI批次、工厂投影与投递终结（2026-09-14）

详细实现、测试数字和未完成门槛见
[rc.26收口记录](../../hardware/docs/review/uart2-rc26-capability-hmi-factory-projection-terminal-closure-2026-09-14.md)。

- `DEVICE_IDENTITY_REPLY`携带真实MCU能力位；香橙派和发布包校验要求覆盖`0x8100`，不再按版本
  或协议模式猜测能力。
- 用户确认统一HMI边界：完整页面/初始化/动态更新或二维码指令组原子进入UART3软件发送队列
  即视为已显示。队列不足时零字节发布、不推进页面或样本状态并重试；不等待USART完成、
  串口屏回执或读回。
- P7使用`uart-v2 / 115200 / 单投口`原生路径，热点报告展示MCU启动号/高水位/能力位、称重
  样本、满溢距离和阈值、投递关门控制依据及清运人工关门确认，不伪造机械到位反馈。
- 原生投递`FAILED / MCU_COMMUNICATION_UNAVAILABLE`由后端以`DEVICE_ABORTED`无业务价值终结，
  只释放原占用且不产生订单、余额或提现；清运仍保留袋状态和人工恢复联锁。
- 当前仍是`MCU_REVIEW_REQUIRED / SIMPLIFIED_BUSINESS_INTEGRATION_NOT_RELEASED`。真实HMI、
  RS485、机构动作、掉电HIL、ARM64安装和部署未完成，本轮没有现场操作。

### rc.26侧向审查补口（2026-09-14）

- 投递/清运/基准START收到MCU明确`REJECTED`后，按原回复终结对应业务并释放准确占用；不把
  明确拒绝误判为控制通信故障，也不为尚未执行的清运建立袋状态联锁。
- MCU完整业务结果释放后，可选普通过程诊断不再阻止新的空袋基准；未确认保存的基准结果仍受
  保护，不能被该清理入口丢弃。
- 香橙派主循环只锁存第一个运行阻断故障，后续异常不得覆盖原人工处理身份；清运占用因连续
  离线600秒释放但原结果待补报时，小程序继续查询同一操作，普通人工恢复状态不无限轮询。
- P7手册正文、首次启动说明和写卡清单已改为原生身份/能力、配置、设备事实、一次START、完整
  结果保存和`RESULT_SAVED`；旧fixed-frame名词只作为历史兼容边界。
- Python 3.11硬件全量实际执行4261通过、123环境跳过、5子测试通过、2项因测试硬编码旧固件
  身份失败；改为读取当前生成身份头后，两项单独复跑通过。最终提交前继续运行相关扩大子集。
- 基于`f5427911`生成的首个ARM64无秘密候选只证明缓存构建链，不含这些补口；必须从新提交重建，
  不得作为部署或写卡对象。

### rc.26补口后本地发布候选（2026-09-14）

- 补口已提交为`5b2f7fde`；从该提交重新生成v39完整源码包、ARM64无秘密候选和配套MCU
  `1.0.2-hil.6 / 10006`人工烧录产物。旧v38与hil.5保留，未被覆盖。
- v39镜像摘要为`bb44f07e…d44d8`，manifest精确绑定完整提交
  `5b2f7fde1f675d9408c9b68c22d0de1ae0c103fb`；源码包摘要为`808DC58D…5D93`。
  构建复用锁定缓存，APT下载量为0字节。
- 构建结束后另行再次运行候选镜像验证器并通过；这不是第二次独立构建，也不构成生产要求的
  双构建一致性或签名封存。ARM64、断网、只读smoke验证Python 3.11、UART rc.26/73消息/
  `0x8100`、EdgeStore schema 40、OneNet 2.4.0、二维码入口、首次故障锁存和称重网页四种
  参考值。固件定向83项通过。
- 该镜像仍为`UNSIGNED_NO_SECRET_CANDIDATE`，不是已封装设备密钥的生产镜像。未写TF卡、
  未烧录或部署；取得新的明确授权后，只能写入明确识别的实验卡并人工烧录hil.6用于受控HIL，
  不能投产。生产部署前仍需完成真实热点、HMI、RS485、机构与掉电HIL以及签名、封存和发布
  审批，S1—S4总目标尚未因本地候选生成而完成。

### v40镜像双构建可复现性收口（2026-09-14）

- 对v39使用相同提交、版本和锁定输入重新构建后，运行时签名压缩包完全一致，但软件载荷锁和
  raw镜像摘要发生变化。只读逐文件和4 KiB块比较确认，没有业务源码、依赖版本、权限、属主、
  链接或扩展属性差异；原始差异只有两类可再生构建缓存。
- 第一类是`stage_signed_runtime_payload.py`每轮重新安装17个锁定wheel时，`uv 0.12.5`在
  `*.dist-info/uv_cache.json`写入当前秒/纳秒，并把该文件摘要写入对应`RECORD`。第二类是
  `/var/cache/ldconfig/aux-cache`中的辅助索引顺序和stat数据；真正供动态链接器使用的
  `/etc/ld.so.cache`两轮一致且继续保留。
- 提交`156a45e3`使全部不可变虚拟环境在权限加固前删除`uv_cache.json`及`RECORD`中的唯一
  精确对应行；缺失、重复、非普通文件、硬链接、属主不符或畸形`RECORD`均拒绝构建。提交
  `31a6b6fc`把`ldconfig`辅助缓存纳入候选清理和最终镜像拒绝检查。
- 从完整提交`31a6b6fca319c252b30574a2cad5bdfdfcf50a76`生成源码包
  `repository-v40-20260914.bundle`，SHA-256为
  `8EF19065B4C752726E56F21BDCF314DA234A9BE16CDBA4DFC89E2AEEA1D08283`。同一源码、同一
  `0.1.0-single-card.20260914.40`版本和同一输入在两个隔离受控目录各完整构建一次，所有APT
  依赖轮次下载量均为0字节。
- 两轮运行包逐字节一致，SHA-256均为
  `b1528daf53f8cd588aac9a105f0a699b638c88cb4519d7d45e7948a5d590fde4`；软件载荷锁逐字节
  一致，SHA-256均为`5456fb02a6e01ae3644452cd4b5ebb533102bd226d30d983ac95f411b137d614`；
  manifest与2,571,108,352字节raw镜像也分别逐字节一致，镜像SHA-256均为
  `94e1bf8314310379c579ea469ae742949b7c121fefd8c6455ab0ca9869f2b84d`。
- 两份输出目录分别是`/var/lib/ecobin-image-factory/hil-v40-20260914-repro1`和
  `/var/lib/ecobin-image-factory/hil-v40-20260914-repro2`。构建后两份软件载荷中的
  `uv_cache.json`均为0个，镜像内`aux-cache`由验证器确认不存在；`/dev/sdc`只临时退出WSL
  交换分区并已恢复，没有写卡。
- v40只收口镜像可复现性，MCU业务固件仍配套既有`1.0.2-hil.6 / 10006`，没有重新烧录。
  v40仍是`UNSIGNED_NO_SECRET_CANDIDATE`；双构建一致只证明本地构建可复核，不替代真实香橙派
  启动/安装、热点、HMI、RS485、机构、掉电HIL、设备秘密注入、正式签名、封存和发布审批。
