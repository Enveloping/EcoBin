# MCU—香橙派最小事实协议与跨端改造方案

> 最新候选 [P1BT：已授权未登记发送的恢复关门撤回](../../hardware/docs/review/uart2-recovery-close-withdrawal-p1bt-2026-09-13.md)：业务库先封住旧发送，永久层另存撤回事实并保留原授权历史；独立继任仅豁免准确祖先，候选循环已接自动核对，不发新动作、不启动云端、不恢复接单，业务39/永久3不变。
> 撤回专项53项与运行入口19项分别通过，最终集成/扩大回归见实施记录，完整契约25通过。已登记可能发送的未知效果、跨新启动号新动作、完整准入/云端/正常业务与main切换仍待接，P4/P5未完成。
> 未部署/烧录；OneNet2.2.0/UART rc.22/MySQL V79/权限40及发布25/39门槛不变。外部刷写/HIL不保证共用串口锁；RS485/HMI/完整固件容量保留。两项投递取舍已确认，下方历史“待确认”不再适用。

> 最新候选 [P1BH：后台确认可靠交接](../../hardware/docs/review/uart2-native-confirmation-p1bh-2026-09-13.md)：原始确认、报告与回执原子绑定，schema32；322项相关测试、25项完整契约检查通过。
> 投递重启缺最终包只归档、归档后迟到结果只追加证据，两项已获用户确认，不再待裁决；实现继续按P4/P5推进。确认交接不释放占用、不修改当前袋或准入，main尚未切换。
> 未部署/烧录；发布25/32、RS485与HMI等剩余门槛保留。以下为历史记录，历史“P4待确认”不再适用于上述两项决定。

> 最新硬件候选 [P1BG：原生上报与编号落库](../../hardware/docs/review/uart2-native-report-persistence-p1bg-2026-09-13.md)：实际 C 多轮/5 秒中位数/末重超时及照片快照已接可靠报告；缺测不补零，清运量为本次前重减后重。
> V78 只对齐四个业务测量编号，原质量/范围/空值约束保留；启动/供应目标同步，134 表/授权39不变。真实 MySQL 297 项、相关 Java 总计399项、Pi/隔离包50项、完整契约25项通过。
> 未部署/烧录；主程序、后端确认消费、当前袋/准入与完整异常仍待接。发布25/31、RS485、完整固件容量及 P4/HMI 待确认项保留，P3/P5/P7未整体完成。下方为历史。

> 2026-09-13 用户最新决定：清运量为本次清运前减清运后，后值仍作新袋皮重；缺任一次不代入旧皮重/零值，历史不重算。[P1BF实现与限制](../../hardware/docs/review/uart2-business-report-p1bf-2026-09-13.md)：Pi与后端同步，新增原生结果可靠上报入口（schema31/OneNet2.1.3），未接完整main/当前袋/后端确认消费，未部署，非整体完成。以下为历史。

> 最新硬件[P1BE原动作与结果核对](../../hardware/docs/review/uart2-result-execution-custody-p1be-2026-09-13.md)：Pi核对原许可/真实受理回复、正常首轮/继续投递/清运重开锁各自的输出与按钮顺序；命令与输出同时缺失也不漏掉原重开锁步骤。缺证据保留完整结果，不重放动作。
> 新增42项；专项与隔离包50项通过；扩大回归1722通过/35跳过/1失败（既有发布schema25/30不一致），不是全绿。契约574项/1694子用例及25项完整校验通过；rc.22、V77/授权39、OneNet2.1.2/schema30/3不变。
> 本批只读核对，不确认账本/上报/释放占用/产生资金；完整中断与恢复业务核对、持久消费/当前袋转换、完整准入/原生云端/HMI/main仍待接。P3/P5未整体完成，P7不可发布。
> 未改MCU源码，核心ROM63780/RAM6688、仅余1756字节ROM且不含完整固件的限制保持，不可烧录；P4裁决、RS485实测、发布25/30及SQLite1546问题保留。未部署/烧录，无需现场操作。下方为历史。

> 最新硬件[P1BB业务内采样组交接](../../hardware/docs/review/uart2-work-fullness-custody-p1bb-2026-09-13.md)：实际投递关门后/清运末重与原超声波组一起冻结并交接SQLite；时间各自保留，中断不伪造未满，丢确认不重采。清运仍需人工关门确认。
> rc.22，新增155项；契约574项/1694子用例及25项完整校验通过，扩大回归1320项通过。核心ROM63760/RAM6688，仅余1776字节ROM且不含完整main/HMI/UART，不可烧录。V77/授权39、OneNet2.1.2/schema30/3不变。
> 实际业务证据交接已接通；Pi原配置核对/当前袋转换、完整准入/原生云端/业务分类/HMI/main仍待接，P3/P5未整体完成、P7不可发布。P4裁决、RS485、发布25/30与SQLite1546问题保留；未部署/烧录，无需现场操作。下方为历史。

> 最新硬件[P1AY真实秤读取事实](../../hardware/docs/review/uart2-scale-observation-p1ay-2026-09-13.md)：投递/清运真实测量已同步最近原始读数与读取状态；保留实际采集/超时时刻，失败不复用旧值，阶段取消不冒充秤超时，不改历史测量。
> rc.21，新增27项/定向185项、契约441项/1694子用例及25项完整校验通过；扩大回归见记录。核心ROM54812/RAM6264仅链接探针，不可烧录。V77/授权39、OneNet2.1.2/schema30/3不变。
> 实际RS485归属、满溢采集、完整准入/原生云端/业务分类/HMI/main仍待接，P3/P5未整体完成、P7不可发布；P4资金取舍、发布25/30与SQLite1546问题保留。未部署/烧录，无需现场操作。下方为历史。

> 最新硬件[P1AX接单前环境事实](../../hardware/docs/review/uart2-environment-facts-p1ax-2026-09-13.md)：原生只读快照增加烟感/满溢来源与采集时间，真实烟感状态机已接发布接口；缺测不冒充正常，查询不刷新旧读数。实际满溢采集/准入/main仍待接。
> rc.21，新增26项/定向70项、扩大回归1199项通过；契约441项/1694子用例及25项完整校验通过。核心ROM54472/RAM6240仅链接探针，不可烧录。V77/授权39、OneNet2.1.2/schema30/3不变。
> 原生云端/业务分类/HMI及完整准入仍待接，P3/P5未整体完成、P7不可发布；P4资金取舍、RS485与发布25/30、SQLite1546问题保留。未部署/烧录，无需现场操作。下方为历史。

> 最新硬件[P1AK首轮动作账本核对](../../hardware/docs/review/uart2-action-reconciliation-p1ak-2026-09-12.md)：发送前保存原许可/动作/回执与命令，实际首重及两条输出核对后持久确认；Pi重启自主找回待办，不重发开门、不清业务占用、不产生资金。
> rc.20/schema28，新增72项专项，最终108项定向及25项完整契约验证通过；扩大回归1802通过/1失败/5跳过/1636子测试通过，失败为既有Windows SQLite强杀恢复1546，已留证但未解决。
> 首次正常动作核对已本地接通；再次清运开锁/异常与恢复许可/分类/HMI/main仍待接，P3/P4未整体完成。未部署/烧录，当前无需用户操作。下方为历史。

> 最新硬件[P1AJ清运中断事实交接](../../hardware/docs/review/uart2-clean-interruption-p1aj-2026-09-12.md)：更新停止/到期/控制状态丢失/受理后未通电分别留因，保留原输出与末重；Pi精确保存后仍维持原清运恢复占用，不推定门关闭。
> rc.20/schema27，517项相关及25项完整契约验证通过；扩大回归1731通过/5跳过/1636子测试通过/零失败，ROM51648/RAM6096仅核心探针，历史SQLite偶发1546未宣称修复。恢复执行/账本/分类/HMI/main仍待接，P3未整体完成；未部署/烧录，无需当前用户操作。下方为历史。

> 最新硬件[P1AI清运解锁前称重失败](../../hardware/docs/review/uart2-clean-preunlock-failure-p1ai-2026-09-12.md)：首重无结果/中断精确保存后冻结FAILED；零解锁步骤、末重未采集、不伪造人工确认，Pi重启/丢回执不重放、不清占用，上一单数据不串单。
> rc.19/schema27不变，300项相关及25项完整契约验证通过；扩大回归1668通过/5跳过/1635子测试通过/零失败，ROM50548/RAM6064仅核心探针，历史SQLite偶发1546未宣称修复。已解锁异常/账本/恢复/分类/HMI/main仍待接，P3未整体完成；未部署/烧录，当前无需用户操作。下方为历史。

> 最新硬件[P1AH清运人工确认与结果交接](../../hardware/docs/review/uart2-clean-confirmation-p1ah-2026-09-12.md)：当前末重与人工关门确认分别精确保存后冻结结果；重开锁不复用旧重量，Pi重启/丢确认不重放、不清占用，中断结果保留失败。
> rc.19/schema27，176项相关及25项完整契约验证通过；扩大回归1631通过/5跳过/1635子测试通过/1失败（既有Windows SQLite强杀恢复1546，未修复），ROM50196/RAM6064仅核心探针。完整清运异常/账本/恢复/分类/HMI/main仍待接，P3未整体完成；未部署/烧录，当前无需用户操作。下方为历史。

> 最新硬件[P1AG清运意图与独立末重](../../hardware/docs/review/uart2-clean-intent-p1ag-2026-09-12.md)：原按钮请求精确保存后再次单次开锁/独立称重；重新开锁作废旧候选但保留原文，Pi重启/丢确认不重放，不清业务占用。
> rc.18/schema26，126项相关及25项完整契约验证通过；扩大回归1533通过/5跳过/1635子测试通过/零失败，ROM48488/RAM6048仅核心探针。人工关门确认/最终结果、完整异常/账本/恢复/分类/HMI/main仍待接，P3未整体完成；未部署/烧录，无需当前用户操作。下方为历史。

> 最新[P1AF首次清运开锁](../../hardware/docs/review/uart2-clean-first-unlock-p1af-2026-09-12.md)：原命令单次通电/独立断电及Pi双记录交接已接，两个固定执行器共享业务入口；重启/丢确认不重放，不推断门位或清Pi占用。
> 27项专项及25项契约检查通过，扩大回归见记录；rc.17/schema25，ROM45828/RAM5952仅探针。再次开锁/完成/清运末重/HMI、完整异常/账本/恢复/main待接，未部署/烧录。下方为历史。

> 最新[P1AE关门后中断交接](../../hardware/docs/review/uart2-postclose-interruption-p1ae-2026-09-12.md)：独立预留并记录更新停止/关门控制上下文丢失，原输出/已有测量/选择精确保存后FAILED；Pi重启只查询，不补造数据或清占用。
> rc.17/schema25，192项相关及25项完整契约检查通过，扩大回归1412通过/5跳过/1635子测试通过/零失败；ROM44072/RAM5744仅探针。清运/HMI、完整异常/账本/恢复/分类/main待接，不可部署/烧录。下方为历史。

> 最新[P1AD测量中断交接](../../hardware/docs/review/uart2-interrupted-measurement-p1ad-2026-09-12.md)：新增独立中断测量，已有正常终态不覆盖；实际C/Pi首末测量精确交接后失败收尾，重启不重放、不释放占用。
> rc.16/schema25，176项定向与25项完整契约检查通过，ROM42832/RAM5720仅探针；扩大回归/静态告警见记录。自动关门后异常触发/原因事件、清运/HMI/账本/恢复/分类/main未完成，不可部署/烧录。下方为历史。

> 最新[P1AC动作中断交接](../../hardware/docs/review/uart2-delivery-abort-p1ac-2026-09-12.md)：CLOSE前中断以独立事件保留原因/原轮次/真实OPEN事实，输出及中断精确保存后组装FAILED；不补造末重或释放Pi占用。
> rc.15/schema25，234项定向及25项完整契约验证通过，ROM42244/RAM5720仅探针，扩大回归见记录。完整异常/清运/HMI、账本/恢复/分类/main待接，未部署/烧录。下方为历史。

> 最新[P1AB首次称重失败收尾](../../hardware/docs/review/uart2-delivery-preopen-failure-p1ab-2026-09-12.md)：原失败测量精确保存后冻结零轮次/末重未采集结果；实际C/Pi/SQLite重启及确认丢失已验证，不清Pi占用或产生资金。
> 11项新增专项、140项相关回归通过；rc.14/schema25不变，ROM41308/RAM5712仅探针。扩大回归/SQLite取证见记录，历史异常未宣称修复；清运/HMI、动作异常/恢复/分类/main待接，未部署/烧录。下方为历史。

> 最新[P1AA连续投递](../../hardware/docs/review/uart2-delivery-local-continue-p1aa-2026-09-12.md)：MCU本地后续轮次、逐轮异常累计与本地来源动作交接已验证；遵守5.2，不伪造新Pi指令/永久账本。
> rc.14/schema25，82项相关组合、25项契约验证通过，ROM40952/RAM5712仅探针。最终扩大回归1249通过/5跳过/1625子测试通过/1失败（既有Windows SQLite强杀恢复1546错误，未修复）；完整异常终态/清运/HMI、账本/恢复/分类/main待接，未部署/烧录。下方为历史。

> 最新[P1Z](../../hardware/docs/review/uart2-delivery-finalization-p1z-2026-09-12.md)：C原配置选择窗口/按钮/超时、首次投递最终结果和Pi原文精确交接本地通过；末重失败明确记录，PB5不单独判故障，重启/丢确认不重测。
> 19项新专项、92项相关回归通过；rc.13/schema24不变，ROM40340/RAM5672仅探针。下一轮动作/逐轮累计、HMI、清运、账本/恢复/分类/main仍待接，Pi占用保持；未部署/烧录。下方为历史。

> 最新[P1Y投递选择精确交接](../../hardware/docs/review/uart2-delivery-selection-custody-p1y-2026-09-12.md)：原命令/业务/轮次/配置与已保存关门后重量严格关联，C保留与Pi提交后精确确认已接通；冲突跨重连保留。
> rc.13/schema24，52项定向、25项契约验证通过；ROM39024/RAM5640仅探针。选择产生/HMI、继续/结束与完整结果、清运、账本/恢复/main仍待接，占用保持；未部署/烧录。下方为历史。

> 最新[P1X关门后称重](../../hardware/docs/review/uart2-delivery-postclose-p1x-2026-09-12.md)：按原配置等待实际CLOSE后启动独立5秒窗口，原START/轮次重量精确保留并交接，不复用初始重量或因丢确认重测。
> 10项新专项、152项相关组合通过；rc.12/schema23不变，ROM38616/RAM5640仅探针，未部署/烧录。选择/完整结果、清运/HMI、账本/恢复/main仍待接，原业务占用保持。下方为历史。

> 最新[P1W首次投递执行](../../hardware/docs/review/uart2-delivery-execution-p1w-2026-09-12.md)：真实C首次授权执行、100ms双断开/中断自动关门与Pi持久命令/账本/原文交接已联合验证；不等待确认关门，不重复旧动作。
> 11项执行专项、53项定向组合通过。rc.12/schema23不变，ROM38048/RAM5544仅探针；关门后测量/完整投递、清运/HMI、账本核销/恢复/main仍待接，未部署/烧录。下方为历史。

> 最新[P1V动作记录精确交接](../../hardware/docs/review/uart2-actuator-handoff-p1v-2026-09-12.md)：实际C查询原文→Pi提交→按完整摘要精确释放已本地验证；锁通断分别交接，断电不等待确认。
> rc.12/schema23，117项相关测试及25项契约验证通过；ROM35852/RAM5272仅核心探针。报告中的命令/业务号尚待原账本对照，保存不推进业务或产生资金。
> 下一步真实命令关联、100ms换向/单次输出/自动关门/再次开锁；完整业务/main/采集/云端未完成，未部署/烧录。下方为历史。

> 最新[P1U共享事件编号额度](../../hardware/docs/review/uart2-actuator-credits-p1u-2026-09-12.md)：控制入口统一持有记录池和编号，预留的断电记录额度不被其他生产者占用；216项相关测试通过。
> rc.11/schema22，ROM33604/RAM5272仅核心探针；未部署/烧录。尚需原命令关系、串口/Pi精确交接，再接换向/单次输出/自动关门，不等确认才断电。下方为历史。

> 最新[P1T动作记录预留核心](../../hardware/docs/review/uart2-actuator-journal-p1t-2026-09-12.md)：C八条容量预留、原文保留与逐条确认释放通过45项模块/196项相关测试；不覆盖锁通断记录。
> 仅RAM核心，未接全局事件编号额度、串口/Pi交接、原业务关系及实际动作；补齐后再做100ms换向/单次执行/自动关门。rc.11/schema22不变，未部署/烧录。下方为历史。

> [P1S动作结果完整校验](../../hardware/docs/review/uart2-actuator-events-p1s-2026-09-12.md)：投递门、安全关门、清运锁供电消息已完整校验；rc.11/schema22，44条完整守卫。
> 仅接收校验，不证明执行/保存/放行。后续需独立动作子事件范围与有界多记录预留，锁通断分别保留，断电不能等确认；再接100ms换向、单次输出、自动关门/再次开锁。
> 最终154项局部测试及三语言38帧/1167轨迹/5摘要通过；完整业务/恢复/main/真实采集/云端未完成，未部署/烧录。下方为历史。

> [P1R首次开门前置核对](../../hardware/docs/review/uart2-opening-gate-p1r-2026-09-12.md)：投递首次授权/清运首次解锁核对原业务、配置、精确重量交接和原期限；只读不动作。
> 机械结果守卫/可靠交接、100ms换向等待、单次执行、自动关门及再次开锁仍待接；没有取消清运再次开锁规则。
> rc.10/schema22不变，未部署/烧录；完整业务/恢复/main/真实采集/云端仍未完成。下方为历史。

> [P1Q业务准备](../../hardware/docs/review/uart2-work-preparation-p1q-2026-09-12.md)：实际配置/START报文、原业务前置称重、首份过程记录和SQLite精确交接已本地验证。
> 不直接开门；没有真实配置应用事实时拒绝开始；重复指令/重连保留原参数及时间。无值留失败证据，旧重量不因延迟发布变新。
> 完整消费者/首次授权解锁/正常业务与恢复/main/真实采集/云端仍未完成；rc.10/schema22，未部署/烧录。下方为历史。

> 最新[P1O过程精确交接](../../hardware/docs/review/uart2-process-handoff-p1o-2026-09-12.md)：rc.10/schema22，实际C保留/原步骤查询/精确释放与Pi原文+关系同事务保存已实现。
> 59条消息，41条完整内容校验；35帧/1054流轨迹/5摘要，104生成物。确认不释放业务/最终结果或产生资金。
> 核心链接探针ROM27352/RAM4096字节，不是固件/完整栈证明；配置消费者/采集/业务/main/云端仍待接线，未部署/烧录。
> 旧制品冻结、旧后端清单/schema18仍拒绝候选；下方保留历史批次，当前无需用户操作。


> 最新实施进度见[P1N](../../hardware/docs/review/uart2-process-measurements-p1n-2026-09-12.md)：rc.9/schema21，六类过程称重统一，
> C四类业务事件组装与Pi原字节证据持久化已实现；不自动ACK/开门/放行或产生资金。
> 过程保留/精确确认、实际采集/业务/main/云端同步尚未接；ROM24652/RAM3840是核心链接探针，未部署/烧录。

> 最新实施进度见[P1M](../../hardware/docs/review/uart2-config-runtime-budget-p1m-2026-09-12.md)：rc.8，配置RAM切换/去重/互斥已验证；
> 共享校验修复核心64KB超限；只链接探针ROM21192/RAM3840字节。实际消费者/采集/业务及云端同步待做。
> schema20保留数据；未接完整业务/main/Keil，未部署/烧录。继续按后续分批计划自主推进。

> 日期：2026-09-11；状态：第一版评审草案；2026-09-12 已按后续分批计划授权本地实施，未授权部署、烧录或设备动作。
> 本文把[逐项共识](mcu-edge-minimal-facts-consensus-2026-09-11.md)整理为实施方案，不增加
> 已部署能力。用户已确认的业务规则以该共识为准；本文的模块划分、新协议版本、消息名、
> 实施分段和 HMI 修改建议属于待评审方案，不因写入本文自动成为已批准实现。
> 范围：生产 MCU、香橙派、永久动作账本接入、OneNet/后端接收及必要的界面调整。
> 保留现有数据、设备身份、袋关系与动作账本；不清库、不刷卡、不改远程维护或更新开关。
>
> 2026-09-12：已整理[契约对齐与分批实施计划](../planning/mcu-edge-contract-implementation-plan-2026-09-12.md)。
> 后续以现有 contracts 为唯一机器来源，兼容性审查后再定版本，不预先指定新主版本。
> 同时纠正本稿对串口所有权的描述：正常 UART 仍由业务进程独占，永久更新器管理许可和账本，
> 通信代理负责云端通信，不代发串口。详细文件、测试和切换条件见新计划；
> [首批实施记录](../../hardware/docs/review/mcu-core-first-batch-2026-09-12.md)覆盖 MCU 核心局部改造，不代表整体完成。
> 随后[启动/命令/结果 P0 模型](../../hardware/docs/review/mcu-session-p0-model-2026-09-12.md)取得条件性测试证据，
> 兼容性审查确定按原生 UART 2.0 设计。[身份契约 P1A](../../hardware/docs/review/uart2-identity-contract-p1a-2026-09-12.md)
> 后最新[会话基础 P1B](../../hardware/docs/review/uart2-session-foundation-p1b-2026-09-12.md)已推进至 Registry
> 2.0.0-rc.2：命令决定/查询、结果保存身份、MCU RAM 绑定/去重核心及三语言长输入解析修复通过。
> 随后[完整结果交接 P1C](../../hardware/docs/review/uart2-result-handoff-p1c-2026-09-12.md)已补213字节单帧结果、
> MCU独立结果保留模块和真实SQLite原子交接，Registry为2.0.0-rc.3、schema19。专用任务仍等待Pi分类，
> 后续[原业务查询 P1D](../../hardware/docs/review/uart2-work-query-p1d-2026-09-12.md)已完成原业务状态查询、
> MCU记录组合与Pi持久查询编号/单次只读发送，Registry为2.0.0-rc.4。核心仍未接主程序/真实串口；
> 一致设备事实、启动/动作发送及恢复未完成。旧制品冻结，后端旧清单/schema18仍不放行候选包，未发布。
> 最新[单投口设备事实 P1E](../../hardware/docs/review/uart2-device-facts-p1e-2026-09-12.md)已补实际C采集与Pi只读查询，
> Registry为2.0.0-rc.5；同次控制刷新门事实、原始读数时间与最后测量配置分别保留，查询不推进业务。
> 是局部事实而非完整安全快照；新查询未接真实入口，实际启动/动作发送、配置/采样与业务恢复仍待接线。
> 已链接门控/时钟的完整固件重编译通过；未部署/烧录/改HMI，schema19候选仍不可推送。

## 1. 结论与分工

建议升级为 MCU 原生提供真实事实的新版本串口协议；复用已有的帧解析和契约生成基础，
不继续给旧 AA/DD/EE/EF 固定帧补造启动编号、受理回执、稳定性或作业状态。
MCU 的重构是分离职责并逐段验证，不是一次性换掉全部底层驱动或引入操作系统。

| 执行主体 | 负责什么 | 不负责什么 |
|---|---|---|
| MCU（单片机） | 读引脚和称重模块、控制推杆/电磁锁、屏幕交互状态机、测量取值、保留原作业和结果 | 判断秤准确度、云端身份授权、钱包/订单、伪造物理门位 |
| 香橙派业务进程 | 独占正常串口，保存业务及证据、分配 MCU 启动编号、查询与恢复、照片、重量计算、可靠上报 | 根据串口写成功推定 MCU 已接受、根据零值填补缺失重量 |
| 永久管理层 | 动作许可及永久账本、云端通信、更新与业务互斥及串口维护交接 | 用解除账本限制代替查清原业务或机械状态；不代发正常串口命令 |
| 服务器后端 | 发起时登记业务与占用、接收事实、去重、应用订单/袋/基准、按既有规则审核结算 | 指挥每次 MCU 查询重试、根据未上报事实推断现场发生了什么 |
| HMI（串口屏） | 展示状态、发送明确的按钮意图 | 自行认定业务成功、凭跳回首页释放业务、把返回按钮伪装成完成 |

设备的现场动作恢复仍由香橙派本机协调。服务器开始时已知道有清运任务，实际经过只以
收到的报告为准。MCU 清理已交接结果，不等于服务器已完成换袋或设备已可接新单。

## 2. 已确认且不得改回的规则

1. MCU 启动回复分为接受、拒绝；无回复是是否执行未知。接受不是动作完成。
2. 香橙派发出的命令有唯一身份，重复命令不重复动作；失去回复先查原业务，不盲目重发。
3. MCU 重启先停输出，不续跑旧动作。启动编号归零，由香橙派保存并分配新非零编号；
   仅香橙派重启时不更换仍在运行的 MCU 编号，MCU 不为此写 Flash。
4. MCU 保留冻结结果，直到香橙派本地数据库提交成功后的保存确认；查询和补传不重新称重。
5. 业务门状态采用最近有效方向：按 PB6/PB7 顺序，01=关，10=开；实际输出另行记录。
   PB4 未接；PB5 只暂停关门输出，解除后继续仍有效的关门目标，开门忽略 PB5。
   PB5 不证明门位或故障，不单独阻止其他条件满足后的下一次投递开门。
6. MCU 重启且投递必要数据确实丢失：只留问题证据、不建可结算订单，香橙派随后发新的
   关门命令并检查。完整原结果已保存时仍处理原结果，不能因重启将其作废。
7. 清运必要数据丢失：保留原操作，由原清运员完成换袋和人工关门确认，重新核对袋子、
   建立真实新袋基准；旧重量缺失留异常，不因此要求另外的维修人员解锁。
8. 称重目标每 250 ms 读取；最近 5 次新读数最大值减最小值 ≤100 g 时取均值；最多等
   5 s，仍只是波动时取本次合法样本中位数继续业务，不仅因此转异常或禁止接单。
   无响应、损坏报文等真正无可用重量情况不取零或旧值冒充。准确度交人工。
9. 非重启的真正最终称重失败仍可形成系统异常订单、等待人工审核；与重启丢数据的
   问题记录分开。中位数是可用估算值，不冒充已稳定，不仅因使用中位数强制人工审核。

## 3. 当前源码核对及明确差异

| 位置 | 当前事实 | 本方案要求 |
|---|---|---|
| `hardware_mcu/USER/main.c` | 主循环混合屏幕、动作、称重和协议；DD/EF 发出后立即清理结果 | 按职责分离，结果交接后才清理 |
| `USER/mcu_runtime_logic.h` | PB4 限制关门、PB5 限制开门；负称重值截为零 | 按已确认方向改防夹，保留原始数值与有效性 |
| `USER/stm32f10x_it.c` | 100 ms 中断计到第 6 次触发采样，并把公共计数归零 | 使用持续时间基准，分别调度采样及其他计时 |
| `main.c` 清运锁计时 | 每次采样加一次，8 次约 4.8 s；直接改 250 ms 会变成约 2 s | 保持独立配置的锁通电时长，不随采样频率变化 |
| `USER/usart1.c` | RS485 读取检查 CRC，尚缺完整响应结构核对 | 单请求、完整结构校验、超时与新鲜样本身份 |
| `fixed_frame_mcu_adapter.py` | MCU 编号取自香橙派；握手为本地合成；查询返回空列表；写串口后返回本地下发状态 | 原生 MCU 事实，不把空列表或本地下发当受理/空闲 |
| `work_manager.py` | 固定帧兼容测量被标为稳定均值、样本数 1；另有允许不稳定均值的路径 | 所有取值方式来源真实且跨端一致 |
| `uart_link.py::_send_and_wait_ack` | 有自动重发未确认原帧的循环 | 机械命令不得直接继承通用重发；先查询 |
| `contracts/uart/uart-registry.yaml` | 已有 CRC/长度/有界解析；不稳定回退为最后四次或全部样本均值 | 复用帧基础，修订启动/恢复/中位数等语义 |
| `TrustedDeliveryCompletionService` | 正常入口仅接受首末均为 `STABLE_WINDOW_MEAN` 等条件 | 接受合法中位数；另支持真正称重失败，不伪装正常稳定 |
| `ApplyCleanCompleteService` | 前重量须稳定，只有最终稳定才允许新皮重 | 新中位数策略与缺失旧重量恢复分开接收 |
| `AbortEdgeRestartedWorkService` | 对应可信重启事件会中止未完清运、释放袋预留并建立限制 | 新协议设备区分 Pi/MCU 重启，不抢先破坏本机恢复上下文 |

审计材料也有年代差异：`hardware/docs/review/uart-protocol-audit.md` 正文针对早期文本
协议，不能把其中的问题逐条当成今日代码事实。`contracts/uart/mcu-review-checklist.md`
仍写着与本次共识相反的 GPIO 方向、自生成启动身份和不稳定均值，不能直接照其实现。

## 4. 协议方案：保留传输基础，替换虚构语义

### 4.1 版本与解析边界

继续使用 `contracts/uart/uart-registry.yaml` 作为唯一机器来源。先逐项判定兼容性，
再按契约版本规则选择版本，不预先命名为“最小事实协议 v2”。不兼容变化不得冒充旧协议，
也不能把规范 UART 版本与当前固定帧自身的 v2 名称混为一谈。

- 复用 `EC42 + 版本 + 类型 + 长度 + 序号 + 数据 + CRC` 的已有帧基础、生成工具和
  C/Python 编解码测试。具体字段布局及最大帧预算在协议细化时重新计算。
- 优先保持当前 256 字节帧上限；不能通过截断结果满足长度。若结果超长，按不可变结果
  身份分段，全部组装验证并落盘后才确认交接；不得按一片的接收确认清理整个结果。
- 一条串口只启用一种解析器。版本或必需能力不匹配时明确拒绝新作业，不自动降级到固定帧。
- 同时处理半帧、粘帧、错误长度、CRC 错误、超时和噪声；缓冲区有界，坏帧可重新找帧头。
- 不直接套用旧 UART 的“最多发送 3 次”：只读查询可重试，机械启动先查原业务；
  完成结果可补传同一份，保存确认可在核对本地原结果后重复发送，启动编号分配不可盲重发。
- 保留配置版本/摘要与固件能力查询。采样参数由明确配置表达，不能只在界面上改值而 MCU
  仍执行旧值；旧设备能力不支持时明确报告，不编造已应用。

### 4.2 必要身份与回复

| 身份/字段 | 用途 |
|---|---|
| MCU 启动编号 | 区分 MCU 的运行轮次；0 仅为未分配 |
| 业务编号及类型 | 对应原投递会话或清运操作，不能因 Pi 重启重新创建 |
| 命令编号及内容摘要 | 命令去重；同编号不同内容明确拒绝，不动作 |
| 查询编号 | 将回复与本次查询关联，拒绝旧查询回复冒充最新事实 |
| MCU 事件/本地动作序号 | 对同业务中的测量、按钮、动作和结果去重；数值相同不是同一事件 |
| 测量身份及阶段 | 区分首次开门前、轮次结束、最终重量、清运换袋后等 |
| 不可变结果身份/摘要 | 对应结果保存确认，防止确认错对象 |

位宽、序号溢出处理、命令去重记录容量和拒旧规则统一在机器契约中落实。特别是结果正文
删除后，也不能因旧命令从缓存淘汰就重新执行它：要保留交接标记或采用经过验证的序号
拒旧规则。查询不到与“从未执行”仍不等价。

### 4.3 最小消息能力

以下是消息职责，不是已冻结的 opcode 或已生成 API：

| 能力 | 发起方与回应 | 是否允许动作 |
|---|---|---|
| 查询当前启动/状态 | Pi 发带查询编号的请求；MCU 返回当前编号及一致状态 | 否 |
| 分配启动编号 | Pi 先保存候选编号，再按本次握手绑定并核实 | 否 |
| 启动投递/清运 | Pi 带原业务和新命令身份；MCU 明确接受或拒绝 | 接受且前置条件满足后 |
| 查询原业务 | Pi 指定原启动/命令/业务；MCU 返回进行中、完成待取、查不到 | 否 |
| 新的投递关门动作 | Pi 因明确业务需要发新命令，MCU 报告目标与输出事实 | 是，不重放旧开门 |
| 恢复清运上下文 | Pi 核对本地原操作后恢复清运交互，明确哪些旧测量缺失 | 本身不自动开锁 |
| 测量/本地阶段事实 | MCU 返回阶段、读数、取值方式、样本和状态 | 不追加动作 |
| 完成结果及保存确认 | MCU 保留原结果；Pi 提交数据库后确认 | 否 |
| 停止/更新控制及结果 | 沿用独立、受权限约束的控制能力 | 按其授权，不冒充正常完成 |

“收到有效帧”“接受命令”“输出已设定”“业务完成”“Pi 已保存”是不同事实。
协议必须能区分，不能用一个 ACK 同时表达全部含义。

### 4.4 启动握手是协议细化的重点验证项

已经接受的 Pi 分配、MCU 归零方向不变。必须补齐以下验证后，才能宣称能可靠识别重启：

1. Pi 使用新查询身份获取 MCU 当前状态；非零且匹配旧运行时继续原业务，不换号。
2. 读到 0 后先核对本地未完业务和已存结果，再持久保存新候选编号并执行当前握手绑定。
3. 绑定回复丢失只查询，不直接重发旧绑定报文；再次确认重启时废弃旧候选，不复用它。
4. 必须验证“MCU 在查询与绑定之间复位”“旧绑定报文延迟到复位后”“Pi 在保存/发送
   之间退出”等组合。单有查询编号不能证明迟到绑定报文不会在新启动中被接受。

具体的会话建立、启动接收状态和旧帧隔离机制尚需设计证明；不能在这里假称已完成。
这属于工程协议问题，不要求用户决定随机种子或报文字节，也不擅自增加 MCU Flash
启动计数、偷偷改成 MCU 自生成正式启动编号，或假设未接的 NRST/BOOT0 已可用。

## 5. MCU 重构方案

### 5.1 模块边界（名称为拟定）

| 模块 | 职责及约束 |
|---|---|
| `runtime_clock` | 连续时间基准；称重、锁通电、界面等待等独立定时 |
| `scale_reader` | USART2/RS485 单请求读取、校验、样本编号、读取故障 |
| `weight_measurement` | 按阶段积累新样本，5 点/100 g 均值，5 s 中位数，冻结测量 |
| `door_control` | 最近有效目标、实际 PB6/PB7、PB5 关门暂停/解除继续，其他停止优先 |
| `clean_lock` | PB8 真实输出和按时间自动断电；不报告物理门扇已关闭 |
| `work_runtime` | 单一业务槽、命令去重、投递/清运阶段、原始结果暂存与交接 |
| `hmi_controller` | 按钮解释、合法阶段检查、显示；界面跳转不是业务事实 |
| `edge_protocol` | 唯一 UART 解析器、真实状态/事件及结果回复，不直接夹带业务推断 |

先提取可纯 C 测试的逻辑，再替换调用点。保留可复用的 GPIO、USART、ADC、更新执行
驱动；核对 Keil 工程实际包含新源文件及生成编解码文件，不能以目录里已有 C 文件视为
固件已经编译它们。测量缓存、结果缓存和收发队列均设容量上限，检查链接后的 Flash/RAM。

### 5.2 投递控制与保护

- 建议启动时用 `targetValid=false` 表示尚无本次运行有效门目标，实际输出 00；不是
  增加运动中间态。新的有效命令建立 OPEN/CLOSE 目标，之后停止不篡改历史目标。
- 另有“该动作仍有效”标记；主动停止、取消、更新停机后 PB5 释放不能复活已取消动作。
- CLOSE 且 PB5 触发：输出 00、保留 CLOSE；PB5 解除且动作仍有效：恢复 01。
- OPEN：不因 PB5 触发阻断 10；由关改开后不得在 PB5 释放时恢复旧关门。
- 保留原业务上下文内的正常继续投递，不每轮重新要求扫码或服务器授权。
  空闲、恢复未核对或更新中，旧屏幕的裸开门字节不能绕过业务状态直接驱动推杆。
- MCU 本地按钮引起的动作有自己的本地动作序号和来源，不伪装为 Pi 下发的新启动命令；
  其去重与阶段约束需验证。永久账本仍按经许可登记、由业务进程下发的指令记账，
  不虚称覆盖所有 MCU 本地动作。

### 5.3 测量与结束

首重、轮次重量、末重和新袋基准使用同一取值算法，分别建立新测量阶段；不能将前一
阶段的缓存当成后一阶段样本。按已确认的 250 ms / 5 点 / 100 g / 5 s 执行。

- 可用状态分为“满足窗口条件的均值”和“纯波动超时的中位数”；取值来源如实保存。
- 无合法读数、持续通信或解析故障另列为无可用值；最少样本数、末次响应时效及断续
  通信判据需在实现细化中给出，不用一个很早的孤立值掩盖后来断线。
- 无效测量保留已有诊断，不把数值改为 0。整数克、负值和取整规则跨 C/Python/Java 一致。
- MCU 形成完成结果后进入待交接状态，禁止新业务覆盖结果；重复查询/完成按钮不重称。
- 仅称重无可用数据时也要能形成明确失败结果，不能像当前 DD/EF 条件不满足时不报告、
  一直占用。真正未执行开门的前置读取失败与用户已经投物后的最终读取失败分开。
- 显示可平滑，不能用平滑后的数据伪造稳定性；不因允许中位数自动修正秤的准确度。

## 6. 香橙派：清理适配层，但保留通信和持久化边界

### 6.1 适配层哪些保留，哪些退出

保留串口单一所有权、有界收发、编解码、队列、日志、超时管理、永久动作许可与账本、
业务状态机之间的清晰边界。它们是正常分层，不应为了“消除适配层”全部塞入 `main.py`。

新版本退出这些固定帧兼容行为：

- 用 Pi 启动身份冒充 MCU 启动身份；
- 在本地合成握手、接受回执、稳定性和采样数量；
- 把 `query_state=[]` 当作 MCU 的空闲或查不到；
- 把只有结果字节的 DD/EF 重新包装成未经 MCU 证实的完整事实。

复用 `uart_link.py` 的可用部分前必须改掉机械命令自动重发和旧握手假设。
切换期间保留旧生产制品用于成对回退；不是在同一个进程内并行识别新旧协议。

### 6.2 本地事务及恢复

以当前 SQLite 为基础，增量保存以下事实，不另造第二套业务数据库：

- MCU 已确认启动身份、尚未确认的分配意图；
- 原业务/命令关系、真实受理和阶段事实、恢复进度；
- 分别有身份的测量、原完成结果及摘要、保存确认发送记录；
- 只供问题记录的失败结果，以及与永久动作账本的明确关联。

关键顺序：先保存命令意图再下发；先提交完成结果再确认 MCU；先核对本地完整结果再
决定数据丢失；业务结束与结果接收使用同一串行化/事务边界，防止同时归档和正常建单。
照片与原会话保持关联，继续直传 COS，不能因恢复把旧照片附到下一用户。

恢复分支：

| 观察到的事实 | Pi 行为 | 新业务 |
|---|---|---|
| 同 MCU 启动、原业务进行中 | 继续跟踪，不重启机械动作 | 暂不开始 |
| 原结果仍在 MCU | 取回、落盘、确认，可靠上报同一份 | 再按业务接单条件检查 |
| 本地已保存完整结果 | 重发必要的保存确认/上报，不作废结果 | 不因一次 Pi 重启额外人工解锁 |
| 无回复 | 保留上下文与占用，继续本机查询，节制报告通信故障 | 不开始 |
| 查不到且证据矛盾 | 核对本地结果/当前启动，不能推定从未执行 | 不因查不到直接放行 |
| 确认 MCU 重启、未可靠保存本次最终结果包 | 不根据过程记录重建正常结果；全部已有数据按问题归档，随后受控关门与检查 | 条件满足后恢复 |
| 确认 MCU 重启、清运必要数据真丢失 | 恢复原清运交互，原清运员换袋/关门确认并重建基准 | 袋/基准/占用一致后恢复 |

“MCU 已确认 Pi 保存”只解决本地结果所有权，不删除后端袋关系/基准应用确认要求。
清运结果要可靠上报并确认服务器已应用相应袋与基准代次；不在旧袋投影下接受新投递。
网络等待由 Pi 可靠上报机制处理，不转成后端主动指挥串口恢复。

### 6.3 永久动作账本不得绕过

- 新协议仍由业务进程独占正常串口，发动作前通过永久更新器取得许可并登记账本；
  不额外迁移串口所有权，不另开串口路径绕过许可。维护烧录按既有流程交接串口。
- 新命令编码、动作摘要、实际下发和真实结果的关联同步适配 `job_safety` 及更新器本地接口。
- 自动数据丢失处理需要新的明确账本处理路径：记录原动作效果可能未知、数据已丢失及
  问题归档，不伪装原动作成功或机械已安全；恢复关门是另一个受控新动作。
- 不复用要求完整断电、人工检查和 F3 标志的已部署手动隔离操作来假扮自动恢复，也不
  直接把未核对账本条目设为成功。业务取消、物理结果未知、后续恢复完成应分别表达。
- 原手动远程隔离入口及“由操作者确认哪些现场条件”的调整另行评审；本方案不擅自
  放宽已部署检查、打开远程更新或删掉永久管理层。

## 7. 服务器与机器契约修改

后端不参与逐步恢复，但必须能正确接收这三种不同结果：

| 结果 | 保存与业务效果 |
|---|---|
| 正常投递，采用均值或认可的中位数 | 建同一笔订单，保留取值方式；不因中位数额外强制人工。其他审核/金额阈值仍生效 |
| 非重启、真正最终称重失败 | 保存原始证据并建系统异常订单；人工审核前不增加余额或自动提现 |
| MCU 重启且未取得本次最终结果包 | 问题型记录，保留已有重量/过程，不重建正常结果，不进入正常订单/钱包/自动提现链路 |

2026-09-13 用户已明确确认[投递重启缺包与迟到结果决定](delivery-restart-result-decisions-2026-09-13.md)：
归档前已可靠保存的完整结果优先；归档后的完整结果校验原业务身份后只追加证据，不改变原终态，
不恢复结算，不覆盖当前袋、皮重或后续业务。原因必须准确区分“未取得最终结果包”和实际缺失的数据。

清运另分为完整结果和“缺失旧重量、换袋事实及新基准已经确认”的恢复结果。后者不能
假填开门前重量，但需要允许真实新袋关系和基准生效；清运从来不产生用户奖励。

修改面包括：

1. UART registry 与生成物：新版本身份、结果保存确认、取值方式、状态快照和查询。
2. OneNet 事件 Schema、wire 映射及生成物模型：表达新结果和测量事实；是否复用事件加
   版本字段或新增事件，在契约细化时统一决定，不能直接改线上旧字段含义。
3. device 可信入站：`TrustedDeliveryCompletionService` 接收均值/中位数与真实失败；
   保持身份、配置、事件唯一性和照片槽校验；不能简单删除全部正常结果校验。
4. recycling：订单计算/异常/自动审核资格、`ApplyCleanCompleteService`、袋和基准
   原子应用；中位数不能被后续旧投影再次认作无效或强制审核。
5. `AbortEdgeRestartedWorkService` 与命令观察：按实际重启主体和新协议能力解释，不
   因 Pi 进程重启中止仍在 MCU 的原任务；新旧设备并存时按可信能力版本分流。
6. operations/integration：可信 inbox、去重、上报结果应用确认、问题展示。自动失败
   记录必须可关联原用户/会话和证据，但不能伪称有已确认物理投物事实。
7. 数据库字段/约束、JSON Schema、Java 类型、Python 校验及生成样例一并检查。需要
   新迁移时另分配版本，部署前核对真实 epoch；不在此草案中假定下一版本一定是 V70。
8. Web/小程序仅做必要显示：数据丢失原因、已有证据、无金额结果、中位数取值方式及
   设备处理中/恢复提示。不新建后端串口恢复控制台或重做整套页面。
9. 厂家热点页面接收真实测量状态，显示每一步取值和方法；不把业务采用中位数的规则
   自动等同于修改厂家参考重量验收标准，不删除现有人工参考测试。

## 8. HMI：保留布局，最小调整按钮与时序

本轮查看了桌面 `分层 文字` 目录的 page4～page8 截图；它们是既有工程截图，不是刚从
正在运行的屏读取。按钮脚本依据用户说明及既有 HIL 记录，未在上位机重新读取或保存。
因此下表是需要现场对照的修改清单，不宣称新屏幕已经下载成功。

| 位置 | 建议 | 原因 |
|---|---|---|
| page8.b0 打开清运门 | 保留 `printh 07` | 同次清运允许再次解锁；由 MCU 检查原业务及锁阶段 |
| page8.b1 清运完成 | 保留 `printh 05`，删除直接 `page page0` | 只请求完成；MCU 处理测量/状态后再决定显示 |
| page8.b2 返回 | 删除 `05` 和直接回首页脚本，改为独立返回请求；建议清运进行中禁用，无清运时由 MCU 允许回首页 | 不能和完成发相同字节，也不能让未开始清运的人被困在页面 |
| page4～page7 按钮/定时事件 | 逐项对照现有 00～06 字节；保留合法请求，取消自行认定测量已完成或业务已结束的跳转 | 等待最多 5 秒时不能先显示成功、退出当前业务 |
| 重量/金额显示 | 从 MCU/Pi 确认的当前阶段刷新；缺值显示等待/不可用，不展示旧值当本次值 | 截图中的“获得余额”也不能当成后端已入账事实 |

正常页面、图案、手机号登录等可保留。无需仅为 250 ms、均值/中位数修改 HMI 算法；
这些在 MCU 内完成。页面文案和等待显示可尽量利用现有控件，不先要求整套重绘。
返回请求的具体字节在 HMI 全部事件表核对后分配，不能与其他按钮冲突；禁止返回仅针对
未完清运，不把进入清运介绍页就当成已经执行清运，也不永久删除空闲时的导航能力。

如果屏仍无法烧录：核心协议、称重及 Pi/后端软件可先开发验证，但不能承诺仅 MCU
修改便消除“两个按钮同发 05”的语义歧义。运行时隐藏控件是否可用要按实际屏能力核实，
未验证不能作为正式替代。受控测试可以明确只按完成按钮，不能因此签署无人监管界面验收。
用户暂不需要操作上位机；实施阶段先给精确按钮脚本清单、保留原工程副本后再手动修改。

## 9. 分段实施与成对切换

以下是建议顺序，不是本轮授权任务；每段提交和验证后再进入下一段，不要求一次写完上线。

| 阶段 | 交付物 | 验证与进入下一段的条件 |
|---|---|---|
| A. 协议和状态模型 | 最新共识映射、版本/字段/长度预算、启动绑定、防重放、取值判据、结果分类 | 先解决下节关键缺口；跨 C/Python 样例与状态序列可验证 |
| B. MCU 核心逻辑 | 独立时间、方向/保护、称重取值、作业和结果保留的纯 C 模块 | 主机测试、Keil 构建、资源预算；尚不驱动现场设备 |
| C. MCU—Pi 纵向闭环 | 原生新协议、SQLite 恢复、永久账本、同业务查询/结果交接 | 模拟串口断帧/重复/双方进程退出，证明不重复动作或丢结果 |
| D. 云端结果闭环 | Schema/物模型候选、后端/数据库接收、必要界面显示 | 真 MySQL 事务/去重/袋基准/钱包隔离测试；中位数端到端可用 |
| E. 受控真机验收 | 用户刷 MCU、必要 HMI 修改、匹配 Pi 包及已部署云端接收能力 | 新的现场安全确认后测试投递/清运/断电/防夹，逐项留证 |

部署时先准备向后兼容的云端接收面和必要的物模型，再在设备无未完业务及账本状态核对
后进行 MCU/Pi 成对切换。单独刷了 MCU 但 Pi 未切换时，必须保持不接新业务，不自动
“试另一种协议”。上线前检查软件版本认可/允许列表，避免再次卡在设备版本未获认可。

回退同样成对进行，先核对进行中业务、已保存结果和账本；不能在有新协议未交接结果时
只回退其中一端。保留身份与历史数据，不采用重刷 TF 卡、清空数据绕过迁移。若后续需要
制作镜像，必须复用已有受控依赖缓存，仍验证源码/依赖摘要；本轮不构建镜像。

## 10. 验收清单

| 场景 | 必须观察到的结果 |
|---|---|
| 正常接受/明确拒绝/丢接受回复 | 三者区分；丢回复先查询，不重发旧开门 |
| 同命令重复、同编号不同内容、旧编号已清正文 | 不重复动作；冲突拒绝；缓存淘汰不能使旧命令重新有效 |
| Pi 退出，MCU 仍进行中或已完成 | 恢复原业务或取回原结果，不要求重新扫码 |
| 结果保存前退出、保存后确认前退出、确认丢失 | 不丢结果、不提前清 MCU、上报与建单恰好一次 |
| MCU 复位、持续读到 0、绑定消息延迟/丢失 | 准确核对代次，0 不重复算多次重启，不重灌旧编号掩盖重启 |
| 旧查询/旧完成/旧确认延迟到新业务 | 不改变新业务、清错结果或产生二次业务价值 |
| 投递丢数据 | 问题证据保留、无订单结算/余额/自动提现；新关门检查后才恢复 |
| 清运丢旧重量 | 原清运员确认、旧值为空、新袋真实基准可应用；无伪造清运净重 |
| 有完整结果后 MCU 才复位 | 原结果继续处理，不降格为无价值记录 |
| CLOSE 防夹、释放、暂停时改 OPEN | 输出暂停/继续符合最新规则；开门不受 PB5 阻断，无旧关门复活 |
| PB5 因关到底保持触发 | 不单独判故障，不阻止其他条件正常后的下一次开门 |
| 250 ms 采集与屏幕/锁/烟感同时运行 | 无计时串扰；锁保持独立时长；称重等待不阻塞防夹 |
| 5 点跨度恰好 100 g 或 101 g | 前者均值提前结束；后者继续积累或到 5 s 取中位数 |
| 重复读缓存、阶段切换、合法 0/负数、报文错误 | 缓存不凑样本，旧阶段不混入，缺值不变 0，数值语义一致 |
| 5 s 波动中位数、奇偶样本、读取失败 | C/Python/Java 结果一致；中位数可业务采用，真实读取失败分开 |
| 中位数经 OneNet 到后端 | 不被旧 STABLE 校验拒绝、不仅因此触发人工审核、取值方式保留 |
| 新袋结果上报/应用确认丢失 | 原袋/新袋/基准恰好应用一次，不在旧基准下接受下一笔 |
| 旧 HMI 返回/完成/定时跳转、更新中或无业务裸按钮 | 不误完成、不绕过状态驱动机构；页面不等于业务状态 |
| 新旧软件不匹配、失败回退 | 保持停止新业务且可诊断，不双解析、不删除账本或旧数据 |

纯软件测试不代表 PB5 实物覆盖、称重准确度或机械安全已经验收。必须在现场重新确认
安全范围后再做物理动作，不沿用几天前的 SSH 会话或动作授权。

## 11. 尚需完成的设计，不伪称已全部定稿

- 启动分配防旧帧机制、编号编码/溢出、受理与传输确认的区别、去重记录容量、结果分段预算。
- 250 ms 下真实响应期限、超时中位数最少合法样本数及末次采样时效、机械扰动等待阶段。
- 自动问题归档与永久账本处理的精确事务，以及后续新关门许可；禁止绕过现有管理层。
- 清运现场袋核对/确认入口、缺失旧重量结果的事务字段，以及本地完成与云端应用的衔接。
- 已确认的投递缺包归档/迟到证据规则按上述决定实施；原子裁定、关联证据、后端资金隔离
  与当前袋不回写仍需代码和事务测试证明，不再等待业务取舍确认。
- HMI 现用脚本与可下载能力核验。清运进行中禁用返回、空闲允许独立返回是建议，不是已经修改。

2026-09-12 已进入[具体分批计划](../planning/mcu-edge-contract-implementation-plan-2026-09-12.md)，
后续按明确实施授权推进。HMI 交互仍单列待确认；投递缺包及迟到结果取舍已于 2026-09-13 确认，
不再作为阻塞项。部署、物模型导入和真机操作仍需各自的发布条件与授权。
