<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.enveloping</groupId>
        <artifactId>EcoBin</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>ecobin-bootstrap</artifactId>
    <name>ecobin-bootstrap</name>
    <description>启动模块（空壳）：仅负责组装依赖、打包、启动</description>

    <dependencies>
        <!-- 组装所有模块 -->
        <dependency>
            <groupId>org.enveloping</groupId>
            <artifactId>ecobin-framework</artifactId>
        </dependency>
        <dependency>
            <groupId>org.enveloping</groupId>
            <artifactId>ecobin-module-identity</artifactId>
        </dependency>
        <dependency>
            <groupId>org.enveloping</groupId>
            <artifactId>ecobin-module-device</artifactId>
        </dependency>
        <dependency>
            <groupId>org.enveloping</groupId>
            <artifactId>ecobin-module-funds</artifactId>
        </dependency>
        <dependency>
            <groupId>org.enveloping</groupId>
            <artifactId>ecobin-module-recycling</artifactId>
        </dependency>
        <dependency>
            <groupId>org.enveloping</groupId>
            <artifactId>ecobin-module-operations</artifactId>
        </dependency>
        <dependency>
            <groupId>org.enveloping</groupId>
            <artifactId>ecobin-integration</artifactId>
        </dependency>

        <!-- MySQL Driver -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <excludes>
                    <!-- 迁移文件只供独立 Flyway Maven 作业读取，不进入运行制品。 -->
                    <exclude>db/p0-migration/**</exclude>
                </excludes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-maven-plugin</artifactId>
                <version>11.14.1</version>
                <configuration>
                    <locations>
                        <location>filesystem:${project.basedir}/src/main/resources/db/p0-migration</location>
                    </locations>
                    <baselineOnMigrate>false</baselineOnMigrate>
                    <cleanDisabled>true</cleanDisabled>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.flywaydb</groupId>
                        <artifactId>flyway-mysql</artifactId>
                        <version>11.14.1</version>
                    </dependency>
                    <dependency>
                        <groupId>com.mysql</groupId>
                        <artifactId>mysql-connector-j</artifactId>
                        <version>9.7.0</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

</project>
