# Copy this file to:
#   .ecobin/application-local-secrets.yml
#
# Keep the keys at the top level. The local PowerShell tools intentionally
# support this small, easy-to-audit YAML subset. The destination is Git-ignored.

# Local Docker MySQL. The backend itself ignores these two keys.
localMysqlDatabase: 'ecobin'
localMysqlRootPassword: 'replace-with-a-local-random-password'

# Backend database connection used by IDEA and run-backend.ps1.
dbUrl: 'jdbc:mysql://127.0.0.1:13306/ecobin?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai'
dbUsername: 'ecobin_app'
dbPassword: 'replace-with-the-runtime-database-password'

# Backend signing.
jwtSecret: 'replace-with-at-least-32-random-bytes'

# EB1 袋码 HMAC 密钥：至少 32 个随机字节经过 Base64 编码。
bagCodeKeyK1: 'replace-with-base64-of-at-least-32-random-bytes'

# 设备首次注册全局 K1：独立于袋码密钥，至少 32 个随机字节的 Base64。
deviceEnrollmentKeyK1: 'replace-with-a-different-base64-random-secret'

# Local Fake masks every external channel value below. Local Real requires them.
cosSecretId: ''
cosSecretKey: ''
cosRegion: 'ap-shanghai'
cosBucketName: ''
cosBaseUrl: ''
cosDurationSeconds: '1800'

iotAccessId: ''
iotSecretKey: ''
iotSubscriptionName: ''
onenetProductId: ''
onenetAccessKey: ''

# Only used when an enabled local profile finds no platform administrator.
# Set this ignored local value to the separately agreed bootstrap password.
defaultPlatformAdminPassword: 'replace-with-the-agreed-bootstrap-password'
