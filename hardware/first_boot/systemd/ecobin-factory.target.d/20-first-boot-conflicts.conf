[Unit]
# The AP and low-privilege portal intentionally coexist with enrollment and
# production runtime until a durable SEALED fact exists. UART ownership is
# isolated by ecobin-factory-test.service conflicts and the shared lock.
