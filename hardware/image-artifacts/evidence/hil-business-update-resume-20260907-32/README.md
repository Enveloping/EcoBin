# v32 业务程序断点续传修复镜像证据

> 2026-09-07 真机测试中发现的问题、已做处理和后续修复门禁见
> [`v32 业务程序远程更新真机问题与处理记录`](../../../docs/review/v32-remote-business-update-live-findings-2026-09-07.md)。

## 结论

2026-09-07 已从干净提交
`9cf015720b81bd91f7d18f48ae562c339f4dc1ea` 构建 v32 无秘密候选镜像和受控单卡 HIL
（硬件在环测试）镜像。该版本把已经在 v31 真机热部署并验证的业务发布包断点续传实现固化到
可重复镜像中：临时网络错误或下载授权到期保留可信片段，新授权到达后重新计算已有片段摘要并
从精确偏移使用 HTTP Range 续传；范围、大小、摘要、签名或制品身份不一致时失败关闭。

候选、HIL、永久通信代理、永久更新器、正式业务验签公钥和最低权限网络边界均通过离线审计。
项目负责人随后确认 Windows 磁盘 1，v32 已完成指定 TF 卡写入和完整范围回读；真实冷启动、
热点网页接入、厂家验收、封存、普通投递和清运也已完成。

设备完成一次性运行时切换后，真实私有 COS 健康发布 `0.3.0-rc.3` 已在 v32 上完成下载、验签、
数据库快照、安装和完整 30 分钟观察，终态为 `SUCCEEDED / COMPLETE`。断点续传、重复命令、
更新器重启恢复、过晚取消和健康失败自动回滚也取得真机证据。本轮随后修复了两个现场问题：
永久服务重启不再通过 systemd 强依赖连带停止业务；成功、回滚、延期、拒绝等可清理终态会删除
下载和解压目录，并在更新器重启后补偿中断的清理。修复已热部署到当前测试卡并通过分别重启、
整机重启和完整就绪审计，但尚未进入新的递增镜像。

## 固定身份

| 项目 | 值 |
|---|---|
| Git 提交 | `9cf015720b81bd91f7d18f48ae562c339f4dc1ea` |
| 镜像版本 | `0.1.0-single-card.20260907.32` |
| 硬件运行时 | `hardware-runtime-20260907-32` |
| 永久通信服务 | `communication-20260907-32` |
| 永久更新服务 | `updater-20260907-32` |
| 软件载荷 | `software-payload-20260907-32` |
| 源 bundle 字节数 | `54101658` |
| 源 bundle SHA-256 | `29dadae7b2473a5daefa4d1258845e0f2465ffef4b3eb099fc9b8f2b589469ae` |
| 运行时包字节数 | `58358924` |
| 运行时包 SHA-256 | `1823a66ec7ae4a2e367c1b501f93358e8514a4d12c29a5c6ce92e9be6a177903` |
| 软件载荷锁 SHA-256 | `bb337d94d73a47de546ff8bd1f0c8fa391d7ab53f9d2a4fe89884c7fc4217a9d` |
| 无秘密候选 SHA-256 | `1d6e95800f49b38a90c2725d7ba61bcb0ecf0d7036c7b7caf0dd35f8da942b78` |
| 单卡 HIL 镜像 SHA-256 | `a4bc19aecd227b2055d271cfaba7e997c2737d6e79c04583ae7408b70718df5f` |
| 候选及 HIL 镜像字节数 | `2571108352` |
| 业务验签公钥编号 | `business_2026` |
| 公钥 DER SHA-256 | `29a1145b54e884d4f382c5a953a7673d5e477a24eea04980180b23034e14b587` |
| HIL 受控输入来源 | v31 HIL，SHA-256 `b3fbeee34baac5ce32e406b7f287bbce387513271a69ee485a793b0b1c2a7f67` |

源 bundle 包含完整 Git 历史并把固定 `HEAD` 指向上述提交。镜像构建使用 Python 3.11 目标、
锁定的 Debian 快照、锁定的 ARM64 构建容器和固定 32 GB TF 卡布局；构建清单记录
`sourceDirty=false`。

## 候选镜像检查

- 业务运行时归档完成签名与信任验证，运行时阶段和软件载荷阶段均通过；
- 候选文件系统从锁定源镜像重建，原始磁盘布局、ext4 参数、inode 清单和确定性规范化通过；
- root 和 orangepi 账号保持锁定，自动登录关闭，候选中没有设备凭据和接入输入；
- 当前源码和 systemd 服务写入镜像后完成再次比对；
- 候选自身审计确认软件身份、首次启动洁净状态和固定硬件配置符合清单；
- 构建结束日志明确给出 `v32-candidate-build=PASS`，独立复算候选 SHA-256 与清单一致。

## HIL 镜像检查

HIL 构建以候选为业务和系统内容来源，只从摘要已重新核对的 v31 HIL 镜像继承当前单卡接入输入
和测试登录输入。构建输出没有披露这些输入的值或摘要。结果为：

- 候选复制前后 SHA-256 不变；v31 受控输入源复制前后 SHA-256 不变；
- 注入后重新规范化 ext4，`e2fsck` 五阶段通过，磁盘布局和 ext4 配置再次通过；
- HIL 镜像仍为 root 锁定、仅保留受控 orangepi 测试登录，自动登录关闭；
- 首次启动状态洁净，自动更新队列、断电测试钩子和业务数据库均未预置；
- 外部摄像头固定为当前 HSK，内部摄像头固定为当前 Generic USB Camera，旧型号回退逻辑仍在；
- 永久通信代理 10 个文件、永久更新器 24 个文件和 2 个 systemd 单元分别在候选与 HIL 中同
  当前源码一致；
- 正式公钥存在且旧 HIL 业务公钥不存在，镜像内没有私有签名材料；
- 通信代理和更新器均启用远程业务更新，下载地址固定为正式私有 COS 基础地址；更新器具备受限
  下载网络，仍无额外 capability、无设备节点访问并使用独立低权限身份；
- 专项审计最终输出 `v32-permanent-update-boundary=PASS`。

## 指定 TF 卡与基础业务

2026-09-07，固定写卡工具重新确认磁盘类型、非系统盘、序列号和容量后执行：

| 项目 | 结果 |
|---|---|
| Windows 磁盘 | `1` |
| TF 卡序列号 | `121220160204` |
| TF 卡容量 | `31268536320` 字节 |
| 实际写入 | `2571108352` 字节 |
| 完整回读 | `2571108352` 字节 |
| 回读 SHA-256 | `a4bc19aecd227b2055d271cfaba7e997c2737d6e79c04583ae7408b70718df5f` |
| 写卡结果 | `PASS` |

机器可读结果见 `v32-flash-result.json`。项目负责人随后完成热点网页验收、封存、普通投递和清运，
均反馈正常。

首次更新就绪审计发现设备仍在镜像内置业务链上运行。确认没有活动业务、维护锁或未确认事件后，
执行一次性运行时切换并激活作业门禁；再审计确认永久通信代理、永久更新器和业务程序均就绪，
结果为 `v32-device-remote-readiness=PASS`。切换和门禁输入分别见
`v32-runtime-cutover-evidence.json` 与 `v32-stage4-job-gate-activation-evidence.json`。

## v32 真实业务程序更新

正常路径使用正式私有 COS 中已批准并签名的 `0.3.0-rc.3`：

- 更新编号 `5dffd540-5ee1-4691-a67c-48e19a81a4c2`；
- 发布编号 `9d5cf9e6-7b09-4409-9dc4-54a291984e22`，发布序号 3；
- 包大小 53,755,880 字节，SHA-256 为
  `6c5276bfa83bfba00636322b028219ef5087ec24d8a58b33925a06ba3f592610`；
- 观察开始时间 `2026-09-07T03:37:00.872Z`，成功完成时间
  `2026-09-07T04:07:03.064Z`；
- 终态 `SUCCEEDED / COMPLETE`，业务准入开放，无错误码，无待协调状态；
- 13 条进度事件全部得到平台接收；后端最新软件事实为管理序号 28，识别目标版本正常运行、
  完全兼容并允许业务；
- 业务程序、通信代理和更新器最终均为 `active`，数据库 `quick_check=ok` 且无外键错误。

同轮真机还验证：

- 从 50 MiB 精确偏移向真实 COS 发起 Range 请求，收到 HTTP 206 并恢复完整包；
- 同一命令重复到达只返回重复接收，没有第二次安装；
- 更新器在观察期重启后保留原更新编号和观察起点；
- 激活后请求取消得到“取消时机已过”，原更新继续，结果已送达 OneNet；
- 显式重启通信代理造成目标业务停止时，第一次更新按设计自动回滚，更新前数据库与恢复后数据库
  同为 413,696 字节且 SHA-256 完全相同。

正常终态见 `v32-rc3-remote-hil-success-evidence.json`，其他子场景见
`v32-device-update-subtest-evidence.json`。本次命令直接送到设备，不存在权威后端部署记录，因此
后端收到的进度任务会因找不到部署而进入阻塞终态；这不会改变设备软件事实，但也不能算正式
平台计划闭环。

终态审计最初发现下载缓存保留两个完整发布包，第一次回滚还保留解压暂存目录，总计约 382 MB；
此外永久服务强依赖会连带停止业务。现场先保留证据，随后由修复后的更新器自动清理，而不是人工
删除。修复后通信代理和更新器分别重启时业务主进程保持，整机重启后再次复测仍保持；两个临时
目录为空，完整 v32 就绪审计恢复为 `PASS`。机器事实见
`v32-update-cleanup-dependency-hotfix-evidence.json`。

修复后的更新后业务复核继续得到以下结果：

- 真实投递会话打开期间重启通信代理，业务主进程保持 `1857`，投递随后正常完成；设备第 80 号
  投递完成事件已得到平台确认；
- 模拟 MCU 会在清运开始后很短时间内自动完成最终确认，无法依靠人工稳定停留在清运中间；
- 因此用日常业务账号在同一个永久控制接口上开始一条不派发串口动作的 `CLEAN`（清运）许可，
  在许可存续期间真实重启更新器。更新器进程从 `1727` 变为 `14780`，业务进程始终为 `1753`，
  重启后活动许可仍为 1；测试取消许可后活动许可恢复为 0，作业门重新开放；
- 随后完整执行一次真实清运：设备收到 `START_CLEAN_OPERATION`（开始清运）命令，模拟 MCU
  返回清运结果，第 122 号清运完成事件得到平台确认；后端清运操作和清运记录均完成，净移除
  重量 500 克且状态可信，4 张照片全部可用，新袋经过预留、旧袋移除和新袋安装后绑定投口；
- 清运结束后的完整审计再次输出 `v32-device-remote-readiness=PASS`，业务数据库为 4,652,560
  字节，磁盘可用 28,009,295,872 字节，域名解析正常且没有活动更新。

这组证据覆盖 systemd 停止传播、永久清运许可跨更新器重启和真实投递完成，但没有发出门锁或
称重命令，不能把可控许可测试表述为真实清运物理动作中途重启。更新后的普通投递和清运本身均
已形成设备及后端权威终态；模拟 MCU 的组合时序限制已写入机器证据。

## 源码和真机证据关系

断点续传下载器定向测试再次执行为 7 项全部通过；更新、安装事实和回滚相关的较宽 Python 3.11
套件此前为 `80 passed, 3 skipped`。生产 JDBC 数字类型修复的兼容计算测试再次执行为 18 项
全部通过。正式生产构建此前已完成 Maven 全部测试和 Web 生产构建。

相同断点续传源码此前已经原位安装到 v31 真机，并完成真实私有 COS Range 请求及真实蜂窝中断
恢复；同一真机还完成 rc.3 健康更新、30 分钟观察和故障发布自动回滚。v32 现已补齐自身写卡、
回读、冷启动、基础业务和上述远程更新真机证据。此前 v31 的详细事实见
[`V68 生产部署与香橙派业务程序更新真机记录`](../../../../docs/operations/application-v68-business-runtime-hil-deployment-2026-09-07.md)。

## 当前结论和后续门禁

生产厂家验收认可列表已保留 v31 并加入 `hardware-runtime-20260907-32`。当前设备实际运行
`0.3.0-rc.3`，后端兼容性和业务准入投影正常，生产业务程序远程下发总开关仍保持关闭。

后续按以下顺序收口：

1. 形成新的递增镜像并重复离线审计、写卡回读与真机回归；
2. 保持生产总开关关闭，另行执行有权威部署记录的单设备计划，再进入灰度验收；
3. 将来具备可控制停留时间的 MCU 时，再补做真实清运物理动作进行中重启更新器的组合场景。
