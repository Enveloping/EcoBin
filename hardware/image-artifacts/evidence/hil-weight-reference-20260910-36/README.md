# v36 热点称重标准与采样展示镜像

## 范围

本轮按用户要求制作香橙派单卡开发/HIL（硬件在环测试）镜像并覆盖已插入的 TF 卡。
v36 新增以整数克填写参考重量，以及空载、放物后、取下后的测量值、重量差与失败采样记录展示。
默认 500 克，支持 400、1000 克，验收仍比较重量差与参考值，容差保持 ±10 克。
不修改校准系数，不刷写 MCU 或串口屏，不把界面更新视为真实称重精度已经合格。

## 固定来源与自动化测试

- 版本：`0.1.0-single-card.20260910.36`。
- 原工作区基线：`8452c6398c964c7f0d3e7a4a29a1dd204674829e`。
- 独立干净快照：`0c336415f249ffa7824b1569dc2537e9cca561db`；原工作区未被提交或回退。
- 源 bundle：54,260,556 字节，SHA-256
  `10c85a8582b0914e90fa9b8c7b7f8fe25dc396f450587e2a6c2021a5ef90b731`。
- Python 3.11.15 / Linux 完整回归：2121 passed、9 skipped、5 subtests passed，316.54 秒。
  见 [测试证据](test-evidence.json) 与 [完整输出](python311-tests.txt)。
- 镜像工具专项：63 passed、1 skipped、26 subtests passed，2.64 秒。

最初 109 个失败由本地测试脚本的 `umask 077` 改变测试夹具文件权限引起。同一权限测试在
077 下失败、022 下通过；只调整测试子进程的 umask 后完整通过，未放宽产品权限检查。
沿用仅针对六个已同步时钟前提测试的夹具，专门验证不可信时钟的测试保持不变。

## 缓存约定

根据本轮用户补充要求，后续构建必须优先复用缓存。基础镜像和 uv 压缩包已使用固定摘要缓存；
本轮又从已核对摘要的 v35 运行时提取 16 个与当前锁文件完全一致的 wheel，共 58,597,977 字节。
旧本机构建的 crcmod wheel 不在当前发行文件锁中，未作为锁定缓存采用。
共享缓存位于 `/var/cache/ecobin-image-builder/arm64-py311-9d42ea99614e3763/`，保存 wheel、
pip、uv、APT 和目标 deb；不随临时容器删除，不复制进设备镜像。

载荷仍新建 Python 环境，从同一冻结依赖锁导出带摘要的要求，再优先安装本地包；不复用旧业务
源码或旧安装环境。验证发现仅 `--find-links` 仍可能优先从网络选包，因此保留已完成运行时包，
停止未完成的第一次载荷构建，将匹配项改为按锁定摘要直接指定本地 wheel 后重跑载荷；环境条件
保留不变。后续又复现 uv 0.12.5 对本地 URL 的多摘要拒绝；同一文件使用精确锁定摘要通过，
错误摘要仍失败。因此匹配项的允许摘要缩小为所选 wheel 在原锁中的唯一摘要，并非关闭检查。
最终全部 17 个运行时依赖在网络隔离环境中安装通过，最终载荷显式设置 `UV_OFFLINE=1`。
APT 的快照签名、固定版本、uv 压缩包的长度/摘要和最终镜像审计继续执行。
已开始的第一轮运行时下载不宣称为缓存命中；本轮缺少的构建工具和 18 个目标系统 deb 已补齐并留在缓存。
该缓存配方是本地 HIL 调整，不宣称已通过两次独立重建或正式生产发布资格。

完成载荷后，续跑包装脚本遗漏了上游文件名变量，在完整镜像生成前停止。修正该本地脚本后，
以已核对摘要的运行时和载荷直接续跑完整镜像，不重复下载或重建已完成软件。

## 完整镜像与离线检查

候选镜像及可写卡 HIL 镜像均已生成，大小各为 2,571,108,352 字节：

- 无秘密候选 SHA-256：`3bbad00d5f025329e6b0e85001589625a8deea2ad82e89e4d438bd6c86cdfd09`。
- 最终 HIL SHA-256：`898938884a3a1cd8a05279780bf8c6117b2de13801118aca7ad9aa19082490c6`。
- 运行时包 SHA-256：`2d7a8270859aac15e72dd2cb54ed93011d4687fa89fd3a963f54963abfccd4fb`。
- 软件载荷锁 SHA-256：`b5e8c82c7877e71f45e6fc3ad7e392416c3ee1fd0125596813d173f964b25936`。

固定来源、软件载荷、分区布局、文件系统、重建前后文件清单、安装软件和首次启动空白状态检查
全部通过。候选与 HIL 中的通信组件 10 个文件、更新器 24 个文件和 2 个服务单元均与冻结源码
一致；业务更新信任公钥、最小权限和空任务队列检查通过。旧 v35 仅作为受保护的厂家接入、
热点与开发登录输入来源，输入镜像本身未改变；root 密码仍锁定。

使用实际镜像内 Python 3.11.2 和程序，在 ARM64、禁止联网、镜像只读的环境中验证：400、500、
1000 克合成三阶段成功，400 克参考却只增加 320 克时拒绝并保留采样；实际网页文件与导入通过。
永久更新器的异常隔离账本和业务/通信组件导入也通过。合成数据不是实物精度或门控验收。

见 [构建事实](build-evidence.json)、[镜像离线审计](hil-offline-verification.txt)、
[永久组件审计](permanent-boundary.txt)、[镜像内执行](arm64-smoke.txt)。
构建事实里的 `deviceFlashed=false` 是 2026-09-10 09:47:41 UTC 写卡前的记录，不覆盖之后的写卡事实。

本轮 29 个构建绑定挂载已释放，无镜像循环设备或运行容器残留，原 WSL 交换空间已恢复；
未删除历史镜像或下载缓存，见 [清理证据](cleanup-evidence.json)。

## TF 卡烧录

2026-09-10 17:48:05～17:53:11（北京时间），已向 Windows 磁盘 1（USB、序列号
`121220160204`、31,268,536,320 字节）写入全部 2,571,108,352 字节，刷新写入后重新打开设备，
完整回读同一范围。回读 SHA-256 与上述最终 HIL 摘要一致，结果 PASS、进程退出码 0。
系统磁盘 0 未操作。见 [写卡结果](v36-flash-result.json) 和 [完整进度](v36-flash-progress.txt)。

最终本机文件：
`hardware/image-artifacts/local/factory-secret/builds/hil-factory-login-20260910-36-weight-reference/ecobin-orangepi-zero3-0.1.0-single-card.20260910.36-hil-factory-login.img`。
可以安全弹出 TF 卡，在香橙派断电状态下插入再通电。v36 尚未冷启动或完成实物称重验收。
普通镜像写入不会执行整卡安全擦除，未写入的卡尾区域不作数据不可恢复承诺。

## 使用与部署边界

- 按用户决定不迁移旧设备身份、业务数据库和动作账本；不清理服务器旧记录，重刷不等于旧业务异常已收口。
- 本轮不部署后端/Web、V69、OneNet 18 服务/22 事件，也不更新服务器的镜像版本认可配置。
- v36 仍保留之前的未知结果投递隔离实现，但现场启用依赖配套部署与真实证据，不能据本次构建宣称已可用。
- 含厂家接入和开发登录输入的最终 HIL 镜像仅存于本机忽略目录，按机密制品保管，不提交 Git 或公开上传。
- 真实称重问题须在新热点页记录三阶段读数后继续定位；放物/取下后至少等待 3 秒再采集。
