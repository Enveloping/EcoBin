# v37 自定义重量报告校验镜像

## 范围

用户已插入 TF 卡并明确要求构建、烧录。本轮把自定义参考重量报告的跨环节校验修复纳入
香橙派单卡开发/HIL（硬件在环测试）镜像。报告生成、首次启动、串口交接和封存共用同一
重量判定：采用本轮已知参考克数，保留 ±10 克、取下恢复空载、三阶段数值及采样一致性检查。
后端没有独立的“只能 500 克”门禁，本轮不修改或部署后端、Web、数据库、OneNet 物模型。

实际 500 克物体读成约 28 克的问题仍没有实物校准证据；填写参考值不能代替校准。
本轮不刷 MCU/HMI、不改变校准系数、不驱动机构，也不把防夹引脚接地作为正式修复。

## 固定来源与测试

- 版本：`0.1.0-single-card.20260910.37`。
- 原工作区基线：`8452c6398c964c7f0d3e7a4a29a1dd204674829e`。
- 独立干净快照：`c097673c23527a63e5c442a77cb19322e6774d42`；原工作区没有提交、回退或清理。
- 源 bundle：54,285,422 字节，SHA-256
  `17af3c06b4ef5bdad762b01c34c2c945193f5ac23c1760656d35fe331ac79ae8`。
- Python 3.11.15 / Linux 全量：**2180 passed、9 skipped、5 subtests passed**，333.23 秒。
  见 [测试证据](test-evidence.json) 与 [完整输出](python311-tests.txt)。
- 镜像工具测试：63 passed、1 skipped、26 subtests passed，2.99 秒。

完整回归使用既有、锁文件一致的 Linux 测试环境；沿用明确记录的六个已同步时钟前提夹具，
不修改系统时钟、生产时间判断或专门的不可信时间测试。测试子进程使用 0022 掩码，构建
私有目录仍保持 0700。新增报告测试经过实际生成端、首次启动、交接和封存消费者。

## 缓存复用

沿用 v36 的固定 ARM64 Docker 摘要、上游 7z、uv 压缩包及
`/var/cache/ecobin-image-builder/arm64-py311-9d42ea99614e3763/` 下载缓存。
16 个发行 wheel 逐项核对当前锁文件中的文件名、大小和 SHA-256，未采纳不在发行锁中的
旧构建 wheel；完整 17 个依赖已在断网环境安装通过，载荷显式使用离线安装。
APT 继续验证固定快照签名和准确版本，安装日志显示系统包归档命中缓存；不会把下载缓存、
旧虚拟环境、旧业务数据库或旧验收报告复制进镜像。

## 断电恢复

主机断电时，运行时包和完整软件载荷已完成，候选镜像停在目标系统软件包安装阶段，尚未
写卡。重启后重新核对源码快照和 bundle、运行时归档哈希及签名，并逐项验证软件载荷清单。
通过后只从原始上游镜像重新组装最终系统；未继续使用半成品根文件系统，原中断日志和
临时镜像保留在受控构建目录。恢复输入证据见 [power-loss-recovery.json](power-loss-recovery.json)。

## 完整镜像与写卡状态

完整构建和离线验证已通过，见 [build-evidence.json](build-evidence.json)。

- 候选与最终写卡镜像大小均为 2,571,108,352 字节。
- 无秘密候选 SHA-256：`1f9ef67dbd7172d6124ad454a43c14269b95811dc92c9b483491be5bda9341e6`。
- 最终 HIL 镜像 SHA-256：`e6cc95a2dfa3c0ec87c1d3d8fcfaedb577eacd0740c7178b55da1b8ceac6e956`。
- 运行时包 58,370,126 字节，SHA-256：`a655ea4aeb8fc2995f3f955bda6591a30f1169f0fc8945d913b22b73928eb5b9`。
- 软件载荷清单 SHA-256：`a41e9ad6e2668445cc9fa9a7d48312c8caee7eca875075870f2cd225492d967d`。

分区、文件系统、安装文件、首次启动空白状态、公共信任根及永久更新服务权限审计均通过。
在只读、断网的实际 ARM64 镜像内运行 Python 3.11.2：28/400/500/1000 克三阶段采样通过；
复放冻结源码实际生成的 8 份完整报告，报告消费者和首次启动事实均通过，错误码为 `NONE`。
同时确认“参考 500 克、只增加 28 克”仍被拒绝，失败读数和有界采样仍保留。
见 [ARM64 执行日志](arm64-smoke.txt) 和 [永久服务审计](permanent-boundary.txt)。

已释放本轮构建挂载并恢复原 WSL 交换分区，见 [cleanup-evidence.json](cleanup-evidence.json)。
2026-09-10 21:22:01（北京时间），指定 TF 卡写入及完整回读通过：磁盘 1，USB，序列号
`121220160204`，容量 31,268,536,320 字节，非启动盘/系统盘。写入与回读均覆盖
2,571,108,352 字节，回读 SHA-256 与上述最终镜像完全一致，进程退出码为 0。
磁盘 0 系统盘及另一只磁盘 2 U 盘未作为写入目标。
见 [写卡结果](v37-flash-result.json) 与 [写入/回读日志](v37-flash-progress.txt)。
`build-evidence.json` 的 `deviceFlashed=false` 是写卡前的构建时点快照，最终介质结果以上述
独立写卡记录为准。此时尚未取得 v37 冷启动、实物称重或整机验收证据。

## 随后后台认可配置与现场复验

用户启动设备后，云端报告两次仅因 `UNSUPPORTED_EDGE_SOFTWARE` 被拒绝：服务器文件及
运行后端认可列表仍到 v34。经用户明确确认，同日只追加 `hardware-runtime-20260910-37`
并重载既有应用，其他版本、配置和应用镜像均保持不变，备份及独立复核通过。
21:44:42.643（北京时间）新报告已判定 `PASSED`、失败项为空，资产验收代次为 1，
原两份失败报告保留。21:44:44.173 设备确认收到封存授权，当前为 `ACKNOWLEDGED`，
尚不是最终 `SEALED` 完成事实。没有手工修改验收结果或下发物理动作。

见 [现场证据](backend-allowlist-live-evidence.json)、[部署日志](backend-allowlist-deployment.txt)、
[独立复核日志](backend-allowlist-verification.txt) 和
[后台变更记录](../../../../docs/operations/orangepi-v37-backend-allowlist-deployment-2026-09-10.md)。
构建时点的 `backendOrOneNetDeployed=false` 不表示后来的认可配置更新未发生；本次确实没有
部署后端新代码或 OneNet 模型。云端机器验收通过仍不能证明实际称重精度已校准。

## 使用边界

- 按此前决定不迁移旧设备身份、待处理业务和动作账本；不删除服务器旧记录，重刷不等于
  原业务异常已经完成隔离收口。
- 写卡与完整回读覆盖镜像的 2,571,108,352 字节，不等于对整张卡进行安全擦除；不保证尾部
  历史扇区已被清零。
- 镜像制作阶段未更新服务器认可配置，随后仅完成上述 v37 认可配置更新；V69、OneNet
  18 服务/22 事件或异常隔离配套仍未部署。
- 最终含厂家接入及开发登录输入的 HIL 镜像仅保留在本机 Git 忽略目录，按机密制品保管。
- 本轮只能证明软件和介质写入质量，不能代替从空白状态冷启动、实物称重及整机安全验收。
