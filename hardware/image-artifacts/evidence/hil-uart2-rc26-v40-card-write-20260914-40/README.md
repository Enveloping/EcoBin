# v40实验TF卡写入证据

2026-09-14，项目负责人明确确认覆盖Windows磁盘1。受控工具在实际写入前核对了以下事实：

- 目标为USB磁盘1，序列号`121220160204`；
- 容量31,268,536,320字节；
- 不是Windows系统盘或启动盘，没有挂载盘符；
- 源镜像长度2,571,108,352字节；
- 源镜像SHA-256为
  `94e1bf8314310379c579ea469ae742949b7c121fefd8c6455ab0ca9869f2b84d`。

工具完成全部镜像字节写入并强制刷新，随后从物理磁盘起始位置完整回读相同字节数。回读
SHA-256与源镜像完全一致，结果为`PASS`。

本目录只记录介质写入事实。TF卡尚未在真实香橙派上冷启动；真实热点、UART、HMI、RS485、
机构、断电恢复、设备秘密、签名、封存和生产部署不在本次结论范围内。

- `v40-flash-result.json`：写入目标、字节数、摘要和最终结果；
- `v40-flash-progress.txt`：写入与全范围回读的逐段进度。
