# v26 业务程序本地更新与自动回滚真机证据

## 1. 结论和边界

本记录对应 `0.1.0-single-card.20260904.26` / `hardware-runtime-20260904-26`
香橙派，以及从仓库提交 `3eb5cd7fde5eff69cc9159741927d86996710681` 构建的两份
HIL（真实硬件联调）业务发布包。2026-09-05 的真机验证已经证明：

- 一份健康业务发布可以从镜像桥接基线完成安装、切换、健康检查和完整 30 分钟观察；
- 新程序启动失败时，永久通信代理和更新器保持运行，数据库恢复到更新前快照，上一健康业务
  版本重新接管，维护锁只在恢复身份核验通过后释放；
- systemd 自动重启与回滚停止动作相遇时，停止后的 `failed` 状态可以安全收敛，不会把已经
  终止的服务误判成仍在运行；如果清除失败标记后仍不能证明服务为 `inactive`，助手继续失败
  关闭；
- 发布目录受 helper 的 `UMask=0077` 影响时，root 目录仍被显式修正为
  `root:ecobin-business / 0750`，低权限业务账号可以进入并启动；
- 同一不可变发布的失败尝试可以由新更新编号重试，但发布编号、包摘要、版本和发布序号任一
  不同都会被拒绝。

这不是远程更新验收。设备端 `remoteTriggerEnabled=false`，后端当前设备投影仍为
`LEGACY_DIRECT`，生产数据库没有这台设备的业务软件部署记录；本轮使用 root 本地固定目录和
明确 HIL UUID 排队，未把设备资产编号伪装成部署编号。关键点断电恢复尚未执行，因此第六阶段
仍不能整体标记完成。

## 2. 镜像和发布包身份

v26 镜像的构建、指定 TF 卡写入与完整回读证据见
[`hil-runtime-readiness-cameras-20260904-26`](../hil-runtime-readiness-cameras-20260904-26/README.md)。
本轮沿用 HIL 公钥编号 `hil_business_single_card_20260904`；公开密钥文件的 SHA-256 为
`630073cfeef5ea1331cf42cdff9851b441304569f79c1eec8651c8d349cf96ff`。私钥只位于仓库外的
root 专用目录，没有写入本文、Git、业务包或设备。

| 角色 | 发布编号 / 版本 / 序号 | 源码提交 | 大小 | SHA-256 |
|---|---|---|---:|---|
| 健康包 | `3eb5cd7f-0000-4000-8000-000000000005` / `0.2.0-hil.20260905.5` / 5 | `3eb5cd7fde5eff69cc9159741927d86996710681` | 53,752,050 字节 | `1b1f1be77d60ac2d908c1c095fea793b638644f7f4c49bd85c035fd885405317` |
| 故意失败包 | `3eb5cd7f-0000-4000-8000-000000000006` / `0.2.0-hil.20260905.6` / 6 | `b85331be0d6796ab9ba01b7fc601b9e6252c9625` | 53,752,083 字节 | `cfee59e0c27dfe0b723de9e902153995b0494c88b2f3cd924a3971fa3c08d575` |

两包均独立通过外层签名与摘要、内层逐文件摘要、精确白名单、安全解包、平台、Python 3.11、
数据库结构和发布身份校验。第二个提交只在 `main.py` 启动入口抛出
`intentional v26 HIL failure: verify automatic business rollback`，不含其他业务改动，不得用于
正常业务。

## 3. 冷启动前提和更新前事实

本轮之前，项目负责人已在 v26 完成热点网页厂家验收、封存、正常投递和正常清运。设备重启后
再次核对：镜像桥接业务、永久通信代理、候选更新器和首次启动协调器均运行，启动日志没有
警告；封存目录和状态文件权限正确；更新器状态为就绪，作业闸门开放，无在途许可、维护或
更新，远程业务更新保持关闭。

生产后端只读查询显示该设备没有 `business software deployment` 记录，软件投影为
`LEGACY_DIRECT / FULLY_COMPATIBLE / ACCEPTING`。这说明本轮可以验证设备本地状态机，但不能
声称后端已经依据真实安装事实拒绝或恢复业务。

## 4. 首次健康安装暴露的问题和安全结果

首次健康尝试使用更新编号 `9f342fd2-26d1-466c-bafa-21ccbc500fb4`。发布包验签、作业暂停、
数据库快照和安装均已执行，但新业务服务在 systemd 的 `CHDIR` 步骤因权限被拒绝：发布根目录
实际为 `0700 root:ecobin-business`。原因是特权助手服务使用 `UMask=0077`，代码只在创建目录
时请求 `0750`，没有在 umask 生效后显式改回权限。

该错误没有开放新业务或留下故障版本运行。更新器恢复了数据库并重新启动镜像桥接业务，终态
为 `ROLLED_BACK`。随后仓库和设备热修复完成：

1. 创建和复用发布根目录时显式核对并设置 `0750 root:ecobin-business`；
2. 镜像桥接基线允许存在经过逐项身份校验的非活动失败发布目录；
3. 新更新编号只可复用身份完全一致的不可变发布；
4. 回滚期间 helper 回执短暂未知不再覆盖触发回滚的原始错误。

## 5. 健康发布完整观察

健康重试使用：

- 更新编号：`6539d93e-f2df-4dcf-93c0-7dcac280b204`；
- 本地 HIL 部署编号：`4eb3e811-6390-4b20-80f8-9cc3eee70c6a`；
- 命令编号：`262f7e2b-63f0-40b2-b368-0d24ffdc4cd8`。

重试重新校验并暂存同一包，把遗留发布根目录从 `0700` 修正为 `0750`，随后成功激活。观察从
`2026-09-05T03:17:16.997Z` 开始，在 `2026-09-05T03:47:19.100Z` 完成，未缩短 30 分钟
窗口。终态审计结果：

- 更新为 `SUCCEEDED / COMPLETE`，业务准入为 `ACCEPTING`，错误为空；
- `current` 指向发布 `...0005`，`previous` 尚不存在；
- 镜像桥接服务停止，可更新业务、通信代理、更新器和首次启动协调器运行；
- 业务经通信代理报告 `READY`、版本 `0.2.0-hil.20260905.5`、管理代次
  `LOCAL_PROXY`、云连接持有者 `COMMUNICATION_AGENT`、作业许可强制启用；
- 业务数据库和本轮快照均通过 SQLite `quick_check`，模式为 `0600`；
- 可更新业务、通信代理和更新器的观察期警告数均为 0。

本轮没有由自动化脚本触发投递、清运、门或电机动作。健康包观察证明进程、身份、代理、数据库
和更新器状态，不替代更新后一次人工投递与清运场景验收。

## 6. 故意失败、真机缺口和修复

第一次故意失败更新编号为 `94ef3ad4-fec5-4b45-932e-5247f6d37140`。候选程序按预期报错，
systemd 因 `Restart=always` 在 5 秒后准备重启；回滚同时执行停止。停止命令确实杀死了控制进程，
设备上也没有业务进程，但 systemd 把单元状态保留为 `failed`。旧实现只接受字符串
`inactive`，因而错误记录 `fixed business service did not stop` 并进入 `FAILED_LOCKED`。

失败锁定期间业务准入保持暂停，通信代理和更新器在线，维护围栏仍由同一更新持有，数据库和
快照均完整；系统没有猜测性开放业务。仓库先以真实时序新增失败测试，再做最小修复：停止后若
状态为 `failed`，只对固定业务单元执行 `systemctl reset-failed` 并重新读取状态；只有最终得到
`inactive` 才继续，否则维持原有 `SERVICE_CONTROL_FAILED`。

修复在设备上保存原文件和清单后原子替换。因为既有记录已是不可自动越过的失败锁定，本轮在
停止更新器写入后执行了一次 HIL 专用受控恢复：精确核对更新编号、阶段序号 22、管理序号 15、
围栏 13、当前/上一版本链接、无作业许可、无未核对物理动作和两份数据库完整性后，只把同一条
记录恢复到 `ROLLING_BACK / STOP_TARGET`。审计回执保存在设备 root-only 备份目录。随后真实
助手动作依次证明停止、数据库恢复、链接回切、旧版本启动和身份检查成功，终态为
`ROLLED_BACK`。

## 7. 全新故障更新的无人介入回归

为排除“只有人工续跑才能成功”，使用同一不可变故障发布创建全新更新：

- 更新编号：`be20922d-e88b-40c6-8386-2f401a4a7729`；
- 本地 HIL 部署编号：`92c121bb-9800-4e37-b203-9c924d13b788`；
- 命令编号：`918348f8-a1f4-4558-bd2d-11ae6efc9305`。

该更新从接收、验包、暂停业务、快照、复用已验证发布、激活、预设启动失败到回滚全程没有
人工修改状态。`2026-09-05T04:08:08.176Z` 到达 `ROLLED_BACK / COMPLETE`：

- 根错误保留为 `SERVICE_CONTROL_FAILED / fixed business service control failed`；
- `STOP_FOR_ROLLBACK` 返回 `INACTIVE`；
- `RESTORE_DATABASE` 返回 `RESTORED`；
- `ACTIVATE_ROLLBACK` 返回 `ROLLED_BACK`；
- `START_ROLLBACK` 返回 `ACTIVE`；
- `INSPECT_ROLLBACK` 确认当前发布为 `...0005`，镜像桥接服务未运行；
- `current` 指向健康发布 `...0005`，`previous` 指向非活动故障发布 `...0006`；
- 业务经代理再次报告 `READY / 0.2.0-hil.20260905.5`；
- 业务数据库与本轮更新前快照均为 536,576 字节，SHA-256 同为
  `0e43abd6971f269e8bbc08875496b4274ec43486af9e25697230d0de40ffe46c`，SQLite
  `quick_check=ok`；
- 作业闸门为 `OPEN`，维护为 `IDLE`，无活动更新、许可或未核对物理动作；终态后的业务、代理
  和更新器警告数均为 0。

操作流水共有 10 个成功回执、1 个预期启动失败和 15 个短暂未知回执。未知回执来自
systemd `Accept=yes` 一次性 helper 连接交替，所有需要完成的动作均随后以新动作编号得到明确
成功回执；它们没有覆盖根错误或导致动作越过身份与围栏校验。

## 8. 自动化验证和未完成项

最终受影响专项在 Linux / Python 3.11 下为 `80 passed, 1 skipped`，覆盖特权原语、helper
协议、业务更新日志、协调器和发布包。新增测试同时锁定：umask 权限、不可变重试、冲突拒绝、
桥接残留目录、回滚根错误保留、`failed` 状态清理成功和清理后仍非 `inactive` 的失败关闭。

本轮此前运行当前仓库 `hardware/tests` 全量得到 `1967 passed, 17 skipped, 13 failed, 5
subtests passed`。13 项失败位于未修改的维护载荷校验、时间敏感命令/照片/时钟、蜂窝权限夹具
和镜像安装 root 属主假设，不能将全量结果表述为通过；本次触及的五个专项文件均已通过。

仍需完成：

1. 在数据库快照完成后、活动链接切换后、新服务启动后和 30 分钟观察期内分别断电，验证重启
   后继续同一更新或回滚；
2. 健康发布激活后由人工完成一次投递和一次清运，确认业务行为而不只是健康接口；
3. 实现并部署后端到通信代理的远程更新命令、临时私有 COS URL 和设备实际安装事实回传，验证
   后端在更新期间拒绝业务、结束后只依据实际事实恢复；
4. 使用正式业务签名根重新构建递增镜像。当前设备上的修复是持久 rootfs 热修复，但原 v26
   镜像不含本轮仓库修改，重新写入 v26 会丢失修复。

以上完成前，不把第六阶段、远程业务更新或量产资格标记为完成。
