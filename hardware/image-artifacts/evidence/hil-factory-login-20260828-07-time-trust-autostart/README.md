# HIL 工厂母镜像 v7 单卡烧录及预检证据

本目录记录 `hil-factory-login-20260828-07` 单卡本地热修复候选镜像的
预检结果。镜像二进制包含批次共享的受保护输入，只保存在 Git 忽略的
`hardware/image-artifacts/local/factory-secret/builds/` 下，不纳入版本库。

v7 以完整回读通过的 v6 为只读母本，固化了两个新发现的问题：首次启动
服务现在确实由 `multi-user.target` 自动拉起；蜂窝协调器在 DNS 已通过后，
会给镜像原有的 `chrony` 留出受控的网络时间同步窗口，确认系统时间可信后
才重试 HTTPS。同步失败时仍恢复全断网规则。

没有安装第二套时间服务。镜像原有 `chrony 4.3-2+deb12u1` 已验证安装、启用、
配置完整，且状态目录为空；受保护输入只做了前后字节比较，没有记录内容或
摘要。v7 的完整 SHA-256 见同目录校验文件。

2026-08-28 已将 v7 写入序列号为 `121220160204` 的已确认单张 TF 卡。实际写入
和完整回读均为 `2571108352` 字节，回读 SHA-256 与源镜像一致，结果为 `PASS`。
原始过程和结构化结果分别保存在
`flash-progress-disk1-121220160204-v7.log` 与
`flash-result-disk1-121220160204-v7.json`。

这份证据不是正式生产发布证明。下一步仍需从这张卡的全新状态验证自动首次
启动、校时、HTTPS、一次性注册、工厂交接、运行时和模拟 MCU 成功验收。
OneNet 物模型的 4 个缺失字段也仍需在云端导入后才能关闭最终上报闸门。

以上“下一步”是 2026-08-28 采集 v7 预检时的历史状态。2026-08-29 项目负责人已经确认
当前 15 服务 / 18 事件候选此前完成目标产品导入，v8 也已经走完自动接入与封存流程；
因此物模型导入不再是当前待办，本历史证据不覆盖后续结论。

具体事实、测试统计和未完成闸门见
`hil-factory-login-time-trust-autostart-preflight-evidence.json`。
