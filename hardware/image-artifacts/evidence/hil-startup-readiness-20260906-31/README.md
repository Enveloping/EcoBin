# v31 启动就绪修复镜像证据

## 结论

2026-09-06 已从提交
`756ff54e78f1b28270f75eb5f0f17fc21481bf6d` 构建 v31 无秘密候选和受控单卡 HIL
（硬件在环）镜像。v31 固化了原始 v30 首次真实冷启动暴露的三个问题：永久更新器漏打包依赖、
日常业务服务读取软件事实时缺少必要身份，以及蜂窝网络只显示 DNS 失败、无法区分 SIM、运营商
注册和移动数据附着阶段。它还修正了首次启动协调服务重启时丢失共享验收摘要和停止超时的问题。

修复源码已经先原位安装到 v30 测试卡，并通过真实香橙派、真实蜂窝模块、真实 MCU UART 和
OneNet 连接复测。新生成的 v31 镜像通过离线文件系统与永久管理层审计，并已写入指定 TF 卡、
完成全写入范围回读。项目负责人随后完成 v31 冷启动、热点接入、封存、投递和清运；首次远程
更新门禁又发现业务启动预检仍要求旧的“禁止更新路由”姿态。修复已在当前卡受控热部署并通过
设备门禁，但不在原始 v31 镜像内，因此 v31 不能改判为可重复的远程更新合格镜像，下一张镜像
必须递增版本并包含该修复。

## 固定身份

| 项目 | 值 |
|---|---|
| Git 提交 | `756ff54e78f1b28270f75eb5f0f17fc21481bf6d` |
| 镜像版本 | `0.1.0-single-card.20260906.31` |
| 硬件运行时 | `hardware-runtime-20260906-31` |
| 永久通信服务 | `communication-20260906-31` |
| 永久更新服务 | `updater-20260906-31` |
| 软件载荷 | `software-payload-20260906-31` |
| 源 bundle SHA-256 | `133ac9c7abeb074aac040508cfa2247f538ed1625b8c308494833cfbb9e49ddb` |
| 运行时包 SHA-256 | `9b22afa3c8cadbb190dd755b2cb7b57aca6626256889dee7acb0991b54727dac` |
| 软件载荷锁 SHA-256 | `11182536c907c7e067a7ba75a3d432ca4df548cf09585ad1e2ffdc1a79a30f69` |
| 无秘密候选 SHA-256 | `cbce4ee390714ac23dedd01901260f50d3ab9e249481dfb46c2e5b01d7494e84` |
| 单卡 HIL 镜像 SHA-256 | `b3fbeee34baac5ce32e406b7f287bbce387513271a69ee485a793b0b1c2a7f67` |
| 镜像字节数 | `2571108352` |
| 业务验签公钥编号 | `business_2026` |
| 公钥 DER SHA-256 | `29a1145b54e884d4f382c5a953a7673d5e477a24eea04980180b23034e14b587` |

HIL 镜像只从受控 v30 测试镜像继承设备接入输入和开发登录输入。构建日志不披露这些输入的值或
摘要；镜像内没有业务签名私钥、预置业务更新命令、断电钩子或业务数据库。

## 真实设备复测事实

当前 v30 测试卡原位安装修复后，通过 COM8 串口取得以下事实：

- 厂家阶段所需的八个服务全部处于运行状态，包括永久通信服务、永久更新服务、设备日常业务
  服务、蜂窝联网和两个本机控制接口；
- 永久更新器补齐的三个文件与工作区摘要一致，使用更新器低权限账号从独立安装目录导入成功，
  本机只读状态请求成功；
- 蜂窝状态使用第 3 版结构，模块、AT 状态通道、SIM、运营商注册、移动数据附着、IP、默认路由、
  后台域名解析和后台 HTTPS 九项检查全部通过；
- 日常业务程序打开真实 MCU 串口、完成传感器查询、连接 OneNet 并收到三个订阅确认；
- 热点网页实际提供九项联网中文说明，以及“设备更新服务”“设备日常业务服务”状态；
- 首次启动协调服务重启后，共享验收摘要保持不变，重启耗时约 2 秒且流程回到原等待节点。

受影响的本地自动化测试共 `260 passed, 1 skipped`。这些结果验证修复逻辑能在当前硬件工作，
但原位热修卡不是可重复量产证据。

## v31 镜像离线检查

- 候选镜像通过原始磁盘布局、ext4 配置、软件清单、来源提交和首次启动洁净状态检查；
- HIL 镜像通过受控输入注入、inode 清单与规范化、`e2fsck`、软件身份、摄像头角色配置和
  候选镜像未变化检查；
- 永久层专项检查确认 10 个通信服务文件、24 个更新服务文件和 2 个 systemd 单元与源码一致；
- 永久更新服务具备固定私有 COS 地址所需的受限网络能力，仍保持无额外 capability、关闭设备
  访问、只读信任根和独立低权限身份；
- 自动更新队列为空，正式验签公钥存在，私有签名材料不存在；
- 审计结束后没有残留构建容器、挂载点或 loop 设备。

## 后端认可版本

生产后端已把 `hardware-runtime-20260906-30` 精确替换为
`hardware-runtime-20260906-31`。部署和独立核验确认服务器文件、后端容器实际环境、应用健康、
Web 回环请求和运行秘密隔离均正常；远程业务程序下发仍为关闭。认可版本只允许后端在厂家验收
时接受设备真实上报的 v31 身份，不会安装镜像或主动发送更新命令。
后端机器验收聚焦测试 13 项通过，明确验证 v30 被拒绝、v31 被接受。

## 写卡与完整回读

项目负责人明确确认覆盖 Windows 磁盘 1 后，写卡工具再次核对目标为 USB TF 卡、序列号
`121220160204`、容量 `31268536320` 字节，且不是系统盘或启动盘。结果为：

- 写入开始：`2026-09-06T10:50:32Z`；
- 写入并强制刷新：`2571108352` 字节；
- 同范围完整回读：`2571108352` 字节；
- 回读 SHA-256：`b3fbeee34baac5ce32e406b7f287bbce387513271a69ee485a793b0b1c2a7f67`；
- 源镜像与回读摘要一致，写卡结果和独立结果解析均为 `PASS`；
- 镜像保持 root 密码锁定，单卡开发登录输入仍按受控 HIL 边界存在。

Windows 写卡后识别到磁盘 1 只有镜像内的 Linux 分区，起始偏移 `4194304`，大小
`2566914048` 字节，没有分配盘符。

## v31 冷启动、切换和远程门禁事实

项目负责人已在本卡完成热点网页验收、租户和机构分配，并实际完成正常投递与正常清运。随后
通过 COM8 只读确认旧业务库结构为 18、工作槽为空、维护锁为空、42 条业务事件均已确认、8 张
照片均已上传并清理本地文件；封存事实有效且验收代次为 1。一次性运行时切换使用操作编号
`b63a9249-b2d4-496a-b252-7ec5493f4d73`，其证据见
[`v31-runtime-cutover-evidence.json`](v31-runtime-cutover-evidence.json)，摘要为
`889d9723e1b95df441e4cfffae4d4fb26335dcecccf816340d13dc479e4c2831`。切换完成后旧直连业务、
通信和更新服务保持停止，新业务数据完整性检查通过。

首次启动低权限业务时，真实通信代理正确上报 `BUSINESS_RUNTIME_ONLY`（只把业务程序更新命令
路由给设备更新器），但 v31 预检仍只接受旧候选阶段的 `DISABLED`，导致业务服务反复失败。
仓库先增加失败复现，再把通信代理与更新器的远程更新事实一起纳入失败关闭检查；Python 3.11
聚焦测试为 `5 passed, 7 skipped`。当前卡保留原文件摘要
`c4d80ef300fd05a016531c9ccca49cd0d75c11868d09c36a3ecf8007548d2a7f` 的 root-only 备份，并原子
安装摘要为 `e42fc95396e7a392b0ae44740cc399ca72a64d559dd86dc944606e9dab55bed5` 的修复文件。

运行时切换后，永久作业闸门按设计处于“需要凭证激活且禁止新业务”。专用只读查询证明没有
活动许可、维护锁、人工锁或未核对物理动作，并明确给出 `initialActivationEligible=true`；随后
使用独立操作编号 `9596fc0c-fd68-4e1c-84f1-55daaa24de68` 和
[`v31-stage4-job-gate-activation-evidence.json`](v31-stage4-job-gate-activation-evidence.json)
执行一次性激活。设备持久记录 `INITIAL_ACTIVATION / ACCEPTED`，闸门收敛为 `OPEN`，四个日常
服务全部运行。

完整 v31 设备门禁最终通过：通信代理已连接 OneNet，业务更新只交给更新器，业务程序通过本机
代理运行，远程更新能力已启用但没有活动更新，正式公钥和私有 COS 下载域名正确，暂存目录为空，
可用空间 `28305276928` 字节，大于本轮所需 `376360912` 字节。生产后端只读门禁也通过：
V66/131 张领域表、v31 认可、v30/v29 排除、健康发布 `0.3.0-rc.3`、零未结束计划、双容器健康
均符合预期，且真实下发开关仍为关闭。

## 后续真机结果与剩余边界

2026-09-07，当前 v31 测试卡又完成以下设备侧验收：

- 正式健康发布 `0.3.0-rc.3` 从真实私有 COS 下载、验签、切换并完成 30 分钟观察；
- 真实蜂窝下载中断后保留可信片段并恢复完成，另一次真实 COS Range 探针从固定偏移返回精确
  `206 Content-Range`，最终大小和摘要一致；
- 一份正式签名但故意无法启动的独立测试发布触发自动回滚，恢复 rc.3 和更新前 SQLite 数据库；
  快照与恢复后数据库的 SHA-256 完全相同；
- 后端修复生产 JDBC 数字类型读取问题后收到设备第 47 号实际安装事实，当前为完全兼容并允许
  新业务；生产远程下发总开关已经核对为关闭。

上述命令通过受控设备直达 HIL（硬件在环测试）提交，证明永久更新器执行链，但没有创建假的
后端计划，所以不能替代权威部署记录闭环。仍须由平台管理员创建正式单设备计划，补测工作中
等待与安全取消、准确 URL 到期换授权、设备离线和后端暂时收不到事实，并核对设备、通信、部署
记录和业务准入四层一致。后续灰度批次不在本轮范围。

断点续传修复已经进入提交 `9cf015720b81bd91f7d18f48ae562c339f4dc1ea`。递增 v32 只做离线
构建和审计；在重新写卡、完整回读和断电冷启动前，不能把 v31 原位热部署结果记成 v32 现场结果。

完整生产部署和 HIL 事实见
[`V68 生产部署与香橙派业务程序更新真机记录`](../../../../docs/operations/application-v68-business-runtime-hil-deployment-2026-09-07.md)。

详细顺序见
[`香橙派业务程序远程更新验收手册`](../../../../docs/operations/orangepi-business-runtime-remote-update-readiness-2026-09-06.md)。
