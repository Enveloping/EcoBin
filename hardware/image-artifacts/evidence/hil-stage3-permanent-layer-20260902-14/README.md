# 第三阶段永久设备管理层 v14 镜像离线证据

本目录记录从干净提交
`832d0226e3cd62b6b09876b5693a2db57bdf080b` 构建的
`0.1.0-single-card.20260902.14` 单卡开发/真机测试镜像。它用于验证第三阶段永久通信代理、
永久更新器、三个低权限用户和两个固定特权助手的镜像安装边界；不表示第三阶段真机退出，
也不是正式量产发布。

受控构建使用锁定的 Orange Pi Debian 1.0.4 基础制品、锁定的 ARM64 构建器、精确 apt
包版本和已接受的指定单卡容量证据。源码通过完整 Git bundle 送入受控目录，构建目录中的
HEAD 和镜像清单均绑定上述提交，工作树为空。

构建产物身份如下：

- 签名 ARM64 运行时 `hardware-runtime-20260902-14` SHA-256：
  `0b1c0117c1df6dba4832ecb473b6a06a1a38a20d5b6a8061403c795b5c529d86`；
- schema v2 软件载荷锁 SHA-256：
  `2f40783f6268df020c8ae55a9be0304a2ccd7528853400e46c4438400ea07d9c`；
- 无秘密候选镜像 SHA-256：
  `6cb441f5ca2bce5d8a925583cba1ea5ada73522513637929e8cc152bd224dd12`；
- 注入既有受控测试输入后的 HIL 镜像 SHA-256：
  `c4d4f44104eacf095a7ec575f92141de20d48fb5f766ccc4a1ef0cde5be0796b`；
- 两份镜像大小均为 `2571108352` 字节。

schema v2 载荷精确包含七个组件身份：业务运行时、接入、远程维护、出厂测试、首次启动、
永久通信代理和永久更新器。永久更新器的第四阶段候选开关没有写入生产 systemd 启动参数，
两个特权助手继续拒绝变更操作；本镜像不能触发业务程序或 MCU 远程更新。

HIL 副本只从 v13 已有受保护镜像逐字节继承同一份批次 K1、设置热点密钥和开发登录密码
散列。受保护值及其单独摘要没有写入本证据或构建日志。root 密码登录保持锁定；该副本仅供
受控单卡真机测试，原始无秘密候选保持未修改。

构建后又独立完成一次完整摘要复算和只读挂载审计，确认：

- 原始分区布局和 ext4 构建配置匹配锁定输入，`e2fsck` 无错误；
- 镜像软件清单、七个版本化组件、systemd 单元和 Git 提交一致；
- 无秘密候选不存在 K1、热点密码、设备凭据、注册状态、业务数据库、machine-id 或 SSH
  主机密钥；
- HIL 副本的 K1、热点密钥和开发登录散列与受控 v13 来源逐字节一致，权限正确；
- 首次启动、出厂测试、业务、远程维护和设备唯一状态均为空；
- 外摄仍为 HSK/UNIQUESKY，内摄仍为 icSpring；
- 第四阶段作业闸门和更新变更能力没有被生产启动路径启用。

构建前设备侧和镜像工具完整回归结果为：`1334 passed, 94 skipped, 29 subtests passed`。
构建输入验证、候选审计、HIL 注入后审计和独立离线复验均通过。

截至记录时，Windows 只识别到系统 NVMe，没有识别到指定 TF 卡，因此没有执行写卡。后续
必须先确认唯一 USB 整盘同时匹配序列号 `121220160204`、容量 `31268536320` 字节且不是
系统盘/启动盘，再用受控写卡程序完整写入和复读。写卡成功也只证明字节一致；仍需把卡装回
香橙派，完成三个低权限用户、UART、双摄、目录、三个主要本机接口、两个助手接口、权限缺失
负例和永久服务独立重启的真机验收。

正式发布策略仍为 `UNLOCKED / UNQUALIFIED`，仓库外正式信任策略、根文件系统资格证据、
双构建者证明及正式签名角色均未配置。因此本产物不得称为正式发布或量产镜像。
