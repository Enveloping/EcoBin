# v34 业务程序下载容错镜像证据

## 结论

2026-09-08 已从提交 `adb66043a1da6c4e9ea792f4405266a49b97f319` 构建 v34
无秘密候选镜像和受控单卡 HIL（硬件在环测试）镜像。v34 把正式单设备验收期间发现的蜂窝下载
读停顿修复固化进包外永久更新器：单条 HTTP 连接允许的连续无数据时间由 30 秒调整为 120 秒，
发布级 30 分钟总下载期限、3 次尝试上限、断点续传和 30 分钟目标观察窗口均保持不变。

同一份更新器源码在当前 v32 测试卡上先完成热部署，并通过正式后端计划、OneNet 下行、设备离线
等待、更新器重启、临时授权刷新、Range 断点续传、验签、业务排空、数据库备份、原子切换及
30 分钟观察。设备最终从 `0.3.0-rc.4` 更新到 `0.3.0-rc.5`，设备和后端均收敛为成功。
完整权威链路见
[正式单设备业务更新验收记录](../../../docs/review/authoritative-single-device-business-update-acceptance-2026-09-08.md)。

v34 镜像随后写入指定 TF 卡并完成全范围回读，回读 SHA-256 与镜像完全一致。该卡冷启动后完成
热点厂家验收、租户和机构分配、正常投递与正常清运；随后受控切换到包外永久通信代理、更新器和
可替换业务程序，开启作业安全门并重启。重启后永久服务、业务程序、OneNet 连接、域名解析和更新
准备检查全部通过，后端将设备判定为在线、完全兼容且允许接受新业务。因此 v34 已通过这张指定
单卡的真机基础镜像验收。

## 固定身份

| 项目 | 值 |
|---|---|
| Git 提交 | `adb66043a1da6c4e9ea792f4405266a49b97f319` |
| 镜像版本 | `0.1.0-single-card.20260908.34` |
| 硬件运行时 | `hardware-runtime-20260908-34` |
| 永久通信服务 | `communication-20260908-34` |
| 永久更新服务 | `updater-20260908-34` |
| 软件载荷 | `software-payload-20260908-34` |
| 源 bundle 字节数 | `69,139,918` |
| 源 bundle SHA-256 | `5455f860592899e397309ff5d6cc39c04834b6d8e312f4f48442994699ca3520` |
| 运行时包字节数 | `58,358,885` |
| 运行时包 SHA-256 | `7d1a78691adc2bfcceedbdb1fc3ba5bdd0e9650bfada4d2e8b87e6f927f755bf` |
| 软件载荷锁 SHA-256 | `c40bbc38cabd1ba3a94cb95d4a0fdef8f8d7e578e4c7366f01841f82501946a6` |
| 无秘密候选 SHA-256 | `d23ae002599ca2b6eff055ec939d96b8ef38aa1e82febf7836eb028a66405347` |
| 单卡 HIL 镜像 SHA-256 | `06286842051a037bd8e86c4c4c5eaecc9c8be05e2e28c0070ac7d0905501b9d5` |
| 候选及 HIL 镜像字节数 | `2,571,108,352` |
| 业务验签公钥编号 | `business_2026` |
| 公钥 DER SHA-256 | `29a1145b54e884d4f382c5a953a7673d5e477a24eea04980180b23034e14b587` |
| HIL 受控输入来源 | v33 HIL，SHA-256 `289f1b45a32cb90d09183b22a4bd30103cd9c749f3b36d8756dabb686c4c914c` |

源 bundle 通过 `git bundle verify`，包含完整历史并把构建使用的分支头固定到上述提交。构建器从
bundle 建立干净源码快照；工作区中未提交的验收文档和本机脚本没有进入镜像。

## 构建与离线镜像审计

无秘密候选构建结果为 `v34-candidate-build=PASS`。目标 Python 3.11、Debian 软件源快照、
ARM64 构建容器、上游镜像、软件载荷锁和 32 GB TF 卡布局均由已有锁文件约束。候选完成账户锁定、
自动登录关闭、根文件系统净化、原始布局、ext4 配置、inode 清单、确定性元数据规范化、五阶段
文件系统检查和软件清单审计。

HIL 镜像只从摘要已核对的 v33 HIL 继承现有单卡接入输入和受控测试登录输入，系统及程序内容全部
来自 v34 候选。注入前后候选与 v33 来源摘要均未改变。HIL 再次通过布局、`e2fsck`、ext4、软件
清单、root 锁定、开发登录、洁净首次启动状态和当前 HSK/Generic USB Camera 双摄配置检查，最终
输出 `v34-hil-image=PASS`。

永久程序边界审计同时只读挂载候选和 HIL，确认：

- 永久通信代理 10 个文件、永久更新器 24 个文件和 2 个 systemd 单元与固定提交逐字节一致；
- 两项服务均启用业务更新协议，更新器的私有下载域名和正式公钥正确；
- 镜像不存在正式签名私钥或旧 HIL 公钥，更新器没有额外 Linux capability 或设备节点权限；
- 自动更新队列为空；候选与 HIL 均输出 `v34-permanent-boundary=PASS`。

## ARM64 用户空间执行

在无网络、只读镜像、无新增权限的 ARM64 容器中，直接执行镜像内文件得到：

- Python 3.11.2 可以运行；
- 永久更新器、永久通信代理和首次启动业务桥接模块均可导入；
- 镜像内 `DOWNLOAD_IO_STALL_TIMEOUT_SECONDS` 的实际值为 120；
- 最终任务仅清理自己的下载与暂存树，活动任务和失败锁定任务保留诊断材料；
- 硬链接越界清理被拒绝，首次清理失败后更新器重启能够完成补偿。

Docker Desktop 在容器退出后短暂保留一个指向已删除临时状态目录的 bind mount。复核确认没有对应
容器或 loop 设备后，仅对该精确 v34 临时挂载点执行卸载；最终没有容器、loop 或挂载残留。再次
复算候选和 HIL SHA-256，与构建输出完全一致。

## 写卡、冷启动与业务验收

2026-09-08 将 HIL 镜像写入 Windows 当时识别为磁盘 1、介质序列号 `121220160204` 的指定
TF 卡。共写入并完整回读 `2,571,108,352` 字节，回读 SHA-256 为
`06286842051a037bd8e86c4c5eaecc9c8be05e2e28c0070ac7d0905501b9d5`，结果为 `PASS`。

设备冷启动后报告的镜像、硬件运行时、永久通信服务、永久更新服务和软件载荷身份均为 v34。
项目负责人完成热点厂家验收，并在设备永久分配租户和机构后完成一次正常投递和一次正常清运。
本轮厂家诊断使用固定帧 revision 2 的模拟 MCU 来源，因此这些结果证明流程和香橙派侧链路可用，
不替代真实 MCU 外设质量验收；双摄像头角色检查通过。

切换前只读检查确认封存有效、SQLite schema 为 18、`quick_check` 为 `ok`、没有外键错误、活动
作业、维护锁、未终态事件或活动照片。受控切换操作
`7ab08d04-8d50-4efd-9d49-0c4642451e4d` 完成业务数据库复制与核验；随后按当前管理代次执行
作业安全门激活操作 `b1f49440-9c6a-421f-a4d0-c784c3c0ee20`。切换后重启确认三项旧服务保持
关闭，永久通信代理、永久更新器和可替换业务程序均正常运行。

最终更新准备检查确认更新器、通信代理、业务程序、OneNet 连接和域名解析全部正常，没有活动更新，
可用空间大于更新所需空间。后端设备投影为 `ONLINE`、`FULLY_COMPATIBLE` 和 `ACCEPTING`，即设备
在线、三端版本完全兼容并允许接受新业务；活动业务更新计划和 MCU 更新部署均为 0。结构化证据见
[`v34-flash-result.json`](v34-flash-result.json)、
[`v34-runtime-cutover-evidence.json`](v34-runtime-cutover-evidence.json)、
[`v34-stage4-job-gate-activation-evidence.json`](v34-stage4-job-gate-activation-evidence.json) 和
[`v34-live-qualification-evidence.json`](v34-live-qualification-evidence.json)。

## 后端认可列表与安全开关

生产目标 `VM-0-16-ubuntu` 的只读预检确认现有认可列表精确包含 v31、v32、v33，且 v34 尚未
出现。部署仅在末尾追加 `hardware-runtime-20260908-34`，保留修改前的 root 私有备份，不恢复任何
已撤销版本。生产预检、运行配置分发、后端与 Web 健康检查、回环请求和运行时密钥探针均通过；
独立复核确认现用配置和备份均为 `root:root/0600`，备份保留原 v33 列表。

业务程序远程下发总开关在正式单设备计划全部终态后已经关闭，独立复核得到
`businessReleaseRemoteDispatchEnabled=false`。加入厂家验收认可列表不会自行创建发布、计划或
设备下行。

## 仍未覆盖的边界

- v34 没有再创建并执行一条远程业务包更新计划；相同更新器源码已经在 v32 热修复设备上完成
  rc.4 到 rc.5 的权威真机更新与失败安全验收，因此该重复测试不是 v34 基础镜像资格的阻塞项；
- 厂家验收和本轮投递、清运使用模拟 MCU 来源，不把结果写成真实 MCU 外设质量通过；
- 未测试永久通信代理或永久更新器自身更新，本阶段仍明确把二者视为包外长期组件；
- 本轮不包含 MCU 固件变更或灰度批次推进。

生产业务程序远程下发总开关继续保持关闭。上述边界不影响已经完成的正式单设备业务程序更新验收，
也不要求为了发布下一份业务包而重新写卡。
