# 运行目录可穿越修正 v16 镜像离线证据

本目录记录从干净提交
`42cc922c7449df2c81aaf14fe7aa4f993fd095f9` 构建的
`0.1.0-single-card.20260903.16` 单卡开发/硬件在环测试镜像。v16 替代尚未写卡的 v15
候选，补入运行时发布包和整机软件载荷的目录权限修正；它不表示镜像已经在真实设备上启动，
也不是正式量产发布。

受控构建使用锁定的 Orange Pi Debian 1.0.4 基础制品、锁定的 ARM64 构建器、精确 apt
包版本和已接受的指定单卡容量证据。源码由完整 Git bundle 送入受控目录，构建目录中的
HEAD、软件载荷锁和镜像清单均绑定上述提交，源码工作树为空。

## 制品身份

- 签名 ARM64 运行时 `hardware-runtime-20260903-16` SHA-256：
  `af13c489665a26288f95f3bb6a20f2e437547138306dc0eac74e5afe4b1c9d61`；
- schema v2 软件载荷锁 SHA-256：
  `063892d9654e0ab79de4e039c483728e50667315cc791314c38c90b25b797368`；
- 无秘密候选镜像 SHA-256：
  `035e67ba48665c30d7486ace24ccc30f8c3c3fe856538b1d320d46e7416c12a7`；
- 注入既有受控测试输入后的 HIL 镜像 SHA-256：
  `dca81fef082d315a5917504e1a4ab94bfbaea9d52e8af380d0408b114895e1de`；
- 两份镜像大小均为 `2571108352` 字节。

schema v2 软件载荷包含业务运行时、接入、远程维护、出厂测试、首次启动、永久通信代理和
永久更新器七个组件。本次修正使运行时发布包在私有临时根目录中解包时，仍把其下真实目录
规范为 `0755`，并让软件载荷校验及镜像审计拒绝服务组件中的不可穿越目录。离线审计已经
确认镜像内对应目录的所有者和权限符合规则；这项静态结果不能代替最终低权限用户实际启动
服务并访问目录的真机测试。

第四阶段代码继续保留在默认关闭状态：永久更新器不执行远程更新，业务程序不启用永久作业
闸门，两个特权助手也不接受变更操作。因此，本镜像没有因为目录权限修正而开放远程业务
程序更新、MCU 固件更新或新的硬件动作路径。

HIL 副本只从 v13 已有受保护镜像继承同一批次接入材料、设置热点配置和开发登录状态。本证据
不记录这些受保护值或它们的单独摘要；原始无秘密候选保持未修改。该副本仅供受控单卡测试。

## 离线审计结果

候选构建内置审计、HIL 注入后审计和第二次独立只读复验均以退出码 0 通过，已确认：

- 候选构建结果为 `v16-candidate-build=PASS`，提交、运行时、载荷锁和镜像清单相互一致；
- 原始分区布局和 ext4 构建配置匹配锁定输入，`e2fsck` 未发现错误；
- 软件载荷和镜像软件审计通过，七个组件、systemd 单元、递归目录权限及 Git 提交一致；
- HIL 副本的三项受保护输入与既有 v13 来源一致，首次启动和设备唯一状态保持为空；
- 外摄配置仍为 HSK/UNIQUESKY，内摄配置仍为 icSpring；
- 无秘密候选和受保护输入来源在注入及独立复验前后均未改变；
- 注入路径结果为 `v16-hil-image=PASS`，独立复验结果为
  `v16-hil-verification=PASS`。

## 待完成的真机门禁

截至 2026-09-03，v16 尚未写入指定 TF 卡，未完成全写入范围复读，也未从断电状态冷启动。
因此，当前证据不能证明三个低权限服务身份能够实际穿越目录并启动，不能证明三个主要本机
接口、两个助手接口、UART、双摄像头、GPIO、MCU 或蜂窝网络在 v16 上正常，也没有完成从
首次接入、封存到真实投递和清运的完整硬件流程。

后续必须先按介质身份安全写卡并完成全范围复读，再在断电冷启动的香橙派上执行低权限正例、
权限缺失负例、永久服务独立重启和完整业务回归。第四阶段还需另行完成 MCU 更新迁移、断电
恢复、真实动作防重与回滚测试；这些证据完成前不得启用第四阶段。

正式发布策略仍为 `UNLOCKED / UNQUALIFIED`。仓库外正式信任策略、根文件系统资格证据、
双构建者证明及正式签名角色仍未配置，所以本产物不得称为正式发布或量产镜像。
