# v24 业务运行时持久切换 HIL 候选证据

本目录记录 `0.1.0-single-card.20260904.24` 的离线构建与独立审计事实。v24 把 v23 真机试切换
暴露出的三项问题纳入镜像：新旧业务链选择具有持久事实，共用运行目录在服务切换时不会被
删除，低权限目录校验不再把无访问位的主组差异误判为越权。

测试后端已经接纳 v24 并完成独立健康核验；项目负责人随后确认 Windows 磁盘 1，v24 受控 HIL
镜像已完成写入及完整写入范围回读。v24 尚未冷启动，也没有在香橙派上执行所有权切换、签名
业务包、故意失败回滚或断电恢复。本文不能用于宣称第五、六阶段已经完成，更不能表示生产
远程下发已经开放。

## 1. 源码与构建修复

一次性切换事务、首次启动选择器和 systemd 互斥条件由提交
`c14fa0d33884ee6b6d805c3ae014bacc3c783725` 实现。第一次受控构建在生成可刷写镜像前由软件
载荷精确白名单门禁停止：载荷声明要求永久设备更新器包含 `business_runtime_cutover.py` 和
`business_runtime_cutover_state.py`，但 shell 构建入口漏复制了这两个文件。

提交 `0aa61fd2ea34c711b1d786309882dd5ced9443de` 补齐两个文件并增加镜像工具回归断言；镜像工具与
安装器专项结果为 `61 passed, 5 skipped, 24 subtests passed`。失败产物只保留在本机 WSL
诊断目录，没有候选镜像、没有写卡，也没有加入后端允许列表。v24 随后从 `0aa61fd2` 的干净
快照完整重建。

源码 Git bundle 大小为 `53,673,670` 字节，SHA-256 为
`0ac879bd2ccee1a49efe6a380b64a553f429f17e6afb5ffc2ca343d1f8af9b40`；bundle 验证为完整历史，
唯一构建 HEAD 为 `0aa61fd2ea34c711b1d786309882dd5ced9443de`。

## 2. 制品身份

| 制品 | 大小 | SHA-256 |
|---|---:|---|
| ARM64 业务运行时 `hardware-runtime-20260904-24` | 58,355,114 字节 | `46f0232dd6dfe5d5d051a8c69a917f5575aa5b264cc2b1c16ca85b4e179231c3` |
| `software-payload-20260904-24` 锁文件 | 1,366,325 字节 | `49f870cc4b3723384000ed4b8bdc3c5ac31d1da309f7861ea7c73711889805f9` |
| 无秘密候选镜像 | 2,571,108,352 字节 | `5dbdd34de9afed4b0f805ff94147d214ea32a808eef3b1a9b54f0c6a074bab55` |
| 受控 HIL 镜像 | 2,571,108,352 字节 | `d21f54a76796a8c4ea930d856588b373b5fcb63ef575f548acd6b3b70adcfee2` |

镜像清单为 schema v2，制品类别为 `UNSIGNED_NO_SECRET_CANDIDATE`，`sourceDirty=false`，
Python 目标为 3.11。七个组件身份全部使用 `20260904-24` 后缀：业务运行时、设备接入、远程
维护、厂家测试、首次启动、永久通信代理和永久设备更新器不会混用旧构建身份。

## 3. HIL 专用输入与签名包

无秘密候选不含设备接入密钥、热点密钥、开发登录口令、设备正式凭据、业务数据库、
`machine-id` 或 SSH 主机密钥。受控 HIL 副本只从摘要为
`c9c4318d91f03f7be3d91033cbad3fdfb29fd223e178d0b54056a0a6ec9be07c` 的 v23 HIL 镜像继承
既有单卡测试输入；构建和本文均不读取、拆分或记录其值。

业务验签仍使用 HIL 公钥 `hil_business_single_card_20260904`，公钥 SHA-256 为
`630073cfeef5ea1331cf42cdff9851b441304569f79c1eec8651c8d349cf96ff`。本目录保存公开副本；私钥
位于仓库外 root-only 目录，没有进入 Git、bundle、运行时、载荷或任何镜像。

HIL 副本复用两份已经独立验签的业务包：

| 角色 | 发布编号 / 版本 / 序号 | 大小 | SHA-256 |
|---|---|---:|---|
| 成功迁移包 | `22a00000-0000-4000-8000-000000000001` / `0.2.0-hil.20260904.1` / 1 | 53,750,778 字节 | `b954c6fd8c7052e4b3ce06f63324cfd24a1dd9f9be78a719c076afb9d51835d4` |
| 故意不健康包 | `22a00000-0000-4000-8000-000000000002` / `0.2.0-hil.20260904.2` / 2 | 53,750,801 字节 | `4656e95b86d7027bfc6beef41687fc4cc7be85a95a37d1e35cfeaf5dccc0fb06` |

两份包仅以 `root:root / 0600` 位于
`/root/ecobin-hil-business-packages/<updateUid>/`，目录为 `0700`；永久更新器的实际队列为空，
因此开机不会自动执行。第二份包只用于证明自动回滚，不能投入正常业务。

## 4. 构建内审与第二套独立审计

受控构建已经通过：

- 锁定 Debian 快照、ARM64/Python 3.11 离线依赖重建及运行时签名；
- 软件载荷精确文件、权限、目录、组件身份和三类互不相同的公钥信任库检查；
- 原始分区布局、ext4 参数、文件系统清单、确定性元数据归一化和 `e2fsck`；
- 无秘密候选账号锁定、首次启动空状态、设备数据清除和精确软件安装审计；
- HIL 注入后的开发登录边界、root-only 测试包、自动队列为空及私钥缺失检查。

随后用独立脚本重新计算 bundle、运行时、载荷锁、候选/HIL 和两份业务包摘要，重新解包验签
两份业务包，并把候选与 HIL 分别只读挂载。第二次审计确认：

- 更新器中的 `business_runtime_cutover.py`、`business_runtime_cutover_state.py`，厂家应用中的
  状态读取模块，以及首次启动协调器均与 `0aa61fd2` 源码逐字节一致；
- 切换完整性门、新业务目标、三项旧链单元和四项新链单元共九个 systemd 文件逐字节一致；
- 两张镜像都没有“正在准备”或“已启用”切换标记，新业务目标没有默认启用；
- 无秘密候选没有 HIL 包，HIL 的两份包仍为 root-only，远程触发关闭、自动队列为空，镜像内
  没有业务签名私钥；
- 候选和 HIL 最终摘要在审计前后保持不变，没有遗留只读挂载或 loop 设备。

独立结论为
`v24-runtime-cutover-independent-audit=PASS`。

## 5. 当前真机边界与下一步

写入 v24 前，项目负责人已经再次确认当时的 v23 设备完成热点网页接入、封存、正常投递和正常
清运；串口只读核对表明卡上实际仍是 `hardware-runtime-20260904-23`，旧业务、旧通信代理和旧
更新器运行，新业务目标及候选服务未启动，切换活动标记不存在。因此该结果只是 v23 旧链基线，
不是 v24 或业务更新通过证据。

项目负责人随后明确确认覆盖 Windows 磁盘 1。写卡工具在打开裸磁盘前重新核对该设备为 USB
可移动磁盘、不是系统盘或启动盘、序列号为 `121220160204`、容量为 31,268,536,320 字节，并
重新计算源镜像摘要。实际写入和完整回读均为 2,571,108,352 字节，回读 SHA-256 为
`d21f54a76796a8c4ea930d856588b373b5fcb63ef575f548acd6b3b70adcfee2`，与源镜像一致。机器可读结果见
[`flash-result-disk1-121220160204-v24.json`](flash-result-disk1-121220160204-v24.json)。

`hardware-runtime-20260904-24` 已通过配置提交 `31ef4551` 追加到测试后端允许列表，部署和独立
核验记录见
[`v24 后端允许列表部署记录`](../../../../docs/operations/orangepi-v24-backend-allowlist-deployment-2026-09-04.md)。
下一步将已核验的 TF 卡装回香橙派并冷启动，随后依次验收：

1. 从空白状态完成接入、封存，并确认后端依据设备实际报告的 v24 运行时通过机器验收；
2. 设备空闲时执行一次性持久切换，重启后确认只运行永久通信代理、候选更新器和低权限业务；
3. 经代理链完成投递、清运、双摄照片和状态上报；
4. 明确激活更新许可并排队成功包，验证数据库快照、首次迁移、健康检查、观察和业务恢复；
5. 排队故意不健康包，验证拒绝新业务、恢复数据库和上一稳定业务版本；
6. 在快照后、链接切换后、新服务启动后和观察期分别断电，验证幂等恢复及新旧版本互斥；
7. 核对后端在更新期间拒绝新投递/清运，只在收到设备实际安装事实后恢复准入。

这些现场事实完成前，第五、六阶段保持未完成；v24 只是单卡开发/HIL 候选，不是正式签名或
量产镜像，远程 URL 下发仍保持关闭。
