# v8 单卡模拟外设验收有效证据（2026-08-29）

## 结论

序列号为 `121220160204` 的指定 TF 卡在当前 HSK/UNIQUESKY 箱外摄像头、icSpring
箱内摄像头和 `factory-sim-1.0.0` MCU 模拟固件下，完成了一轮配置绑定正确的 P7
离线验收。最终持久化状态为 `PASSED / COMPLETE`，修订号为 45。

本证据有效证明验收程序、UART revision 2、真实双摄采集、稳定重量门槛及投递/清运
安全状态机能够闭环；它不证明模拟 MCU 背后的真实传感器、屏幕、门锁、电机、电磁阀或
门位质量合格，也不构成正式量产放行。

## 配置与报告绑定

- 完成时当前配置 SHA-256：
  `6f7a1c838e759b841b60e555a2a3cb69f187be1b01d0541bf3b3b6e28f9894f6`；
- 状态绑定配置 SHA-256：同上；
- 最终报告绑定配置 SHA-256：同上；
- 三者逐值比较结果：`true`；
- 最终报告字节数：`1967`；
- 最终报告 SHA-256：
  `343231537be6e8b0124809d3d5416e9a04b2eb057b32cd153ccbc970485fd315`。

箱外来源指纹 `bb95c6ca2854` 对应当前 HSK/UNIQUESKY 稳定设备路径；箱内来源指纹
`54df17bf3b4e` 对应当前 icSpring 稳定设备路径。两路均实际生成非空照片并由现场操作员
确认角色，照片未进入生产照片目录。

## 验收结果

| 项目 | 结果 | 事实 |
| --- | --- | --- |
| MCU | 通过（模拟固件） | revision 2，F3 身份和 F1 自检通过 |
| 稳定重量 | 通过（模拟返回值） | 空载 0 g、加载 500 g、移除后 0 g |
| 双摄像头 | 通过 | 两路真实 V4L2 设备采集非空并完成人工角色确认 |
| MCU 远程升级线路 | 不适用 | 仅连接 RX、TX、GND，BOOT0/NRST 未安装，未执行 ROM 写入 |
| 投递流程 | 通过（模拟外设） | 开始命令一次，0 g → 500 g，动作后现场安全确认完成 |
| 清运流程 | 通过（模拟外设） | 开始命令一次，500 g → 0 g，门关闭确认完成 |

## 修复与剩余边界

首次网页轮次在 DECXIN 配置下开始，中途切换 HSK 后曾形成配置错绑报告；该报告已单独
保存在[无效验收证据](../hil-v8-camera-config-drift-20260829-01/README.md)，不得用于放行。
提交 `dd0482b6` 增加了配置漂移失效机制，并在设备上部署后重新开始本轮。完整硬件测试集
结果为 `979 passed, 46 skipped, 5 subtests passed`。

本轮仍有以下明确限制：

- MCU 外设证据模式为 `SIMULATED_PERIPHERALS`；H-03 真实机构验收仍未完成；
- BOOT0/NRST 未安装，报告明确记录远程 MCU 升级能力为 `false`；
- 设备时间仍不可信，报告只绑定单调时钟；
- 云端正式验收/封存仍为 `CLOUD_ACCEPTANCE_REQUIRED`；
- v8 写卡后又应用了 HSK 配置和运行时热修，因此不能把本轮写成原始 v8 镜像的
  位级正式发布资格。

原始脱敏报告见 [report.json](report.json)，独立核验摘要见
[verification.json](verification.json)。
