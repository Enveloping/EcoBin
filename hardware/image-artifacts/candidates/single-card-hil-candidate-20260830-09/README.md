# v9 单卡无密钥候选镜像

本目录保存 `single-card-hil-20260830-09` 的可复现构建元数据，不保存镜像二进制。
候选镜像从锁定的 Orange Pi Debian 1.0.4 原始镜像和干净提交
`e3455c848ecd9b6471b16143351219e439d3997e` 完整重建，不是从 v8 热修镜像继续修改。

候选镜像的 SHA-256 为
`d9972cbe34f36e175173cbcbef72b8a13fbf07324a8a5d9104efd49015958e78`，大小为
`2571108352` 字节。构建清单确认根文件系统采用确定性重建，当前包锁、软件负载锁和
源码提交均已记录，且不含 K1、设置热点密钥、设备身份或设备运行状态。

镜像二进制只保存在 Git 忽略的
`hardware/image-artifacts/local/candidates/single-card-hil-candidate-20260830-09/`。
当前单卡开发/HIL 写卡应使用另行生成的含 K1 工厂镜像，而不是本无密钥候选镜像。
