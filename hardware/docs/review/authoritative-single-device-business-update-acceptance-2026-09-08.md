# 业务程序远程更新：正式单设备闭环验收记录

> 日期：2026-09-08（北京时间）  
> 状态：单设备业务程序远程更新闭环通过；v34 递增镜像已完成指定单卡真机基础验收
> 范围：后端正式发布控制面、OneNet 下行、香橙派永久通信代理、设备更新器、可替换业务程序。  
> 不包含：MCU 固件变更、灰度批次推进、永久通信代理或设备更新器自身升级。  
> 安全说明：本文不保存管理员凭据、设备密钥、签名私钥或 COS 临时下载地址。

## 当前设备与测试边界

- 串口 `COM8` 上当前物理设备由永久通信代理认证为
  `ECM0-3Y16JJF0KMXKAKDJS7FJFP4ZSW`，后端资产编号为
  `16a774cf-c2c4-4126-a32f-fb2c4a2effcd`。
- 历史记录中的 `ECM0-VESK6FJP81C97P30H7KCE93Q8Z` 当前离线，不是本轮接在电脑上的
  设备。第一份发布的批准说明误引用了该历史编号；发布包本身不绑定设备，正式计划的创建原因已明确
  更正，且计划只冻结当前串口与 OneNet 双重确认的设备。
- 更新前设备运行 `0.3.0-rc.3`（发布顺序 3），生命周期正常、厂家验收通过、租户与机构已分配，
  三端兼容结论为完全兼容，业务准入为接受新业务；没有活动的业务程序或 MCU 更新计划。
- 设备管理层仍报告 `communication-20260907-32` 与 `updater-20260907-32`。本轮所需的三项
  更新器热修复和两份 systemd 服务单元已经逐文件核对 SHA-256，与 v32 后续修复预期完全一致；
  因而本轮不要求重写 TF 卡。

## 正常更新发布与权威记录

| 事实 | 值 |
|---|---|
| 目标版本 | `0.3.0-rc.4`，发布顺序 4 |
| 发布编号 | `4ec13947-23d9-4363-b293-d9db8bb7b0a4` |
| 源码提交 | `e8e5bb4db1b53f078fcac487d6a38de5dfcbffd5`，与 rc.3 相同 |
| 包大小 | 53,755,881 字节 |
| 包 SHA-256 | `a809902d98e7fb4add59ae78d7c6a22ea606a70c218e6c4e2fa000e03679599b` |
| 签名密钥标识 | `business_2026` |
| 正式计划编号 | `71fb7335-ae50-49fb-b579-b3b0f70e5bed` |
| 设备部署编号 | `771ce642-4e55-4b59-8647-df418ee59366` |
| 设备更新编号 | `dde9e2a8-b02e-43f5-be9b-8751db743f2c` |

该发布通过正式平台接口依次完成草稿创建、制品上传、私有 COS 回读校验和平台管理员批准。
本地构建先验证源码提交、清单身份、SHA-256 与 Ed25519 签名；后端随后独立回读并再次验证，
最终发布状态为“可下发”。生产业务更新总开关只在单设备受控测试开始前开启。

正式计划开始后，后端立即创建可靠下行任务，并把该资产的业务准入投影暂停。设备永久通信代理
收到的命令编号、部署编号、更新编号、目标发布编号和包摘要均与后端记录一致，说明这不是设备直达
测试或数据库补写出来的结果。

## 目前已经观察到的真实阶段

1. 设备在 21:13:09 UTC 接受正式命令，旧业务仍可运行，更新器处于等待私有下载授权阶段。
2. 后端可靠任务只产生 1 次 OneNet 提交并得到 HTTP 200；临时 URL 在这次实际发送前才生成，
   没有写入冻结任务快照。设备随后在同一份仍有效的授权内进行了 3 次 HTTP 下载尝试，最终利用同一
   不可变对象成功完成下载并进入验包。这证明设备侧临时失败重试有效，但本次正常路径本身没有触发
   “URL 已过期后重新授权”，两者不能混为一谈。
3. 21:18:40 UTC 更新器关闭新业务入口并开始排空；当时没有活动作业，随后备份本地 SQLite 数据库、
   安装并启动 rc.4。
4. 安装与启动阶段累计执行了 2 次目标动作尝试。启动特权动作曾短暂记录
   `PRIVILEGED_ACTION_RESULT_UNKNOWN`，含义是更新器一度无法确认辅助
   进程的返回值，并不等同于动作失败。更新器没有盲目重复硬切换，而是读取安装事实进行对账；约
   7 秒后错误自行清除，rc.4 身份、包摘要和运行健康均得到确认。
5. 21:19:47 UTC 进入 30 分钟连续观察。当前业务服务活动，`/opt/ecobin/business/current` 指向
   rc.4，SQLite `PRAGMA quick_check` 返回 `ok`；后端最新实际安装事实同样是 rc.4、进程就绪、
   三端完全兼容。观察期间，后端继续以“业务程序更新进行中”为依据阻止平台新业务，即使设备本地
   作业门已经重新开放，也不会提前恢复平台业务准入。

## 正常路径终态与验收中发现的后端缺陷

rc.4 在 21:49:49 UTC 完成连续观察，设备最终上报 `SUCCEEDED`，后端于下一秒完成可信进度
入库。正式计划收敛为“更新计划已完成”，设备部署为“业务程序更新成功”，可靠下行任务为完成；
设备仍运行 rc.4，数据库未发生恢复，更新暂存目录为空，业务服务和 SQLite 检查均正常。

终态核对同时发现一项真实控制面缺陷：部署与计划虽然已经结束，但设备兼容性投影仍保留
`BUSINESS_RUNTIME_UPDATE_ACTIVE`，业务准入持续为“暂停”。原因不是设备事实缺失，而是后端
只在收到软件事实时计算“活动更新锁”；终态进度会结束部署，却没有用最后一条实际安装事实重新
计算投影。由于观察开始时已经写入 rc.4 的健康事实，后续管理状态没有变化，设备也没有理由伪造
一条新事实来替后端解锁。

修复遵循“实际安装事实优先”的边界：部署成功、回滚、失败或安全取消进入终态后，后端在同一事务
内锁定并读取投影指向的最后一条持久化软件事实，重新执行完整兼容性、业务就绪和设备作业门判断，
同时重新检查是否还有别的活动更新。这样既会移除已经结束的临时更新原因，也不会把事实未知、
协议不兼容、业务进程未就绪或仍有其他更新的设备硬编码成可接单。修复前新增的集成断言稳定得到
“期望接受、实际暂停”，修复后发布控制面与兼容计算 30 项、整个设备模块 261 项测试全部通过。
修复已经过正式构建并部署到生产。部署后原 rc.4 计划、设备部署与可靠任务均保持成功终态，
兼容性重新计算为完全兼容，后端业务准入恢复为接受新业务；后续 rc.5 异常分支也均以该修复后的
准入投影作为前置条件。

## 验收中发现的蜂窝下载容错问题

第一条 rc.5 等待空闲计划（计划编号 `2b533249-1054-471c-bf3e-0aaf25ce9c62`、设备部署编号
`f3a29e8f-f225-449e-a68f-3d71b604f93a`、设备更新编号
`607af55b-0121-4814-9d2b-e9b0266b6534`）在真实蜂窝链路上暴露了下载容错不足。设备通过
Range 断点续传进行了 3 次连接尝试，最后一次已经取得 51,205,766/53,755,865 字节，但每次
连续 30 秒未读到数据都会消耗一次发布级重试；最终部署以
`BUSINESS_DOWNLOAD_RETRY_EXHAUSTED` 安全延后。设备始终保持作业门开放、旧版 rc.4 运行，未创建
数据库快照、未进入维护态，也未留下包或暂存目录。

修复不改变已经确认的 30 分钟总下载期限、3 次连接尝试和 30 分钟观察窗口，只把一次 HTTP
连接允许的无数据间隔从 30 秒调整为 120 秒。针对性下载测试 11 项全部通过。首次现场热替换时，
测试故意保留的活动作业许可使永久更新器在重启后按设计锁闭作业门，部署脚本检测到非空闲状态并
自动恢复原文件；业务进程没有重启。随后使用原许可的固定完成身份安全结束测试作业，作业门自动
恢复开放，再在完全空闲状态安装热修复。设备文件摘要、Python 编译、更新器 READY 状态和空闲作业门
均已核对。

## 等待空闲与安全取消

`0.3.0-rc.5`（发布编号 `aeeb399e-3bbd-47eb-89c8-b922c1ac92e7`、顺序 5、
包 SHA-256 `e423f125d715e7486b4c2b97da5a7a29579c5821440da745c4d0196816fb5b77`）
同样取自上述健康源码提交，已经完成正式签名、上传、后端回读校验和批准。

等待空闲计划编号为 `f51fc0a5-7d46-469f-bd2d-71c95d6f39e5`，设备部署编号为
`b91c685d-fd66-460a-bd88-480dbde797f4`，设备更新编号为
`bf899edd-474b-46b9-af76-bf8cf5b72c01`。测试通过永久更新器的正式作业许可创建一条不涉及
订单和资金的受控在途作业。设备完成下载后只进入 `WAITING_FOR_IDLE`：作业门切换为排空，
旧版 rc.4 的进程与当前链接均未改变，目标动作次数为 0，数据库快照尚未创建。固定完成身份结束
该作业后，流程具备继续切换条件。

在仍处于最后安全取消点时，平台以操作编号
`efd2e7a3-0681-48f7-b698-65f76f83d13e` 正式发起取消。设备接受取消并清除包、暂存和快照目录，
作业门恢复开放，当前版本保持 rc.4。该路径同时暴露了一项后端可靠收件箱路由缺陷：取消结果事件
已经可靠入库，但允许列表遗漏 `BUSINESS_RUNTIME_UPDATE_CANCEL_RESULT`，任务重试 10 次后进入
阻塞，导致计划没有自动收敛。修复将该事件加入仅设备资产事实可使用的显式允许列表，并增加
schema v2 回归测试；不放宽其他事件或目标类型。正式部署后通过平台的人工恢复接口确认原因已修复，
原任务以第 11 次处理成功，计划最终为 `STOPPED`、部署为 `CANCELLED`，取消状态为
`CANCELLED`，设备与后端准入均恢复开放。

上述设备下载容错与可靠收件箱修复由提交
`adb66043a1da6c4e9ea792f4405266a49b97f319` 固化。生产发布
`20260907231251-adb66043a1da` 完成后，后端、Web 健康检查、运行时密钥探针和回环检查均通过。
下载器针对性测试 11 项、可靠收件箱工作器测试 9 项均通过；正式发布构建留下的 Surefire 报告
合计 963 项测试、70 项按环境跳过、0 项失败、0 项错误，其中设备模块为 261 项全部通过。

## 设备离线、授权刷新和断点续传

最终成功计划编号为 `e1d4af06-14e8-4e78-9eac-a3d7bb50032c`，设备部署编号为
`a2d8ae27-b37f-4117-8d15-a6d36f271830`，设备更新编号为
`c34c999a-5944-47fb-baa3-a84ed8d93218`，可靠下行任务编号为
`552be605-3969-4f4f-b3d1-8eab54d2c796`。

计划启动前主动停止永久通信代理。后端确认设备离线后，部署保持 `QUEUED`，可靠任务保持
`PENDING`，尝试序号为 0、原因为 `DEVICE_OFFLINE`；没有 OneNet 提交记录，也没有设备更新进度。
这证明离线不会被误判为下发成功，恢复通信不需要重新创建发布或计划。

恢复通信后，第一次正式下行得到 HTTP 200，设备开始下载且旧版业务仍正常运行。当分片达到
23,068,672 字节后重启包外更新器，停止完成前分片最终增长到 25,165,824 字节。重启后的恢复逻辑
保留分片，并把授权序号 1 持久化为 `DOWNLOAD_AUTHORIZATION_LOST`；后端收到权威进度后自动签发
授权序号 2，可靠任务第二次 OneNet 提交同样得到 HTTP 200。设备以 Range 从原分片继续下载，
没有重新从零开始，累计下载尝试为 2。整个链路没有把临时 URL 写入设备数据库、后端冻结任务或
本文证据。

完整包在 23:28:14 UTC 进入验包，23:29:21 UTC 关闭新业务入口并进入维护，23:29:25 UTC
完成数据库快照。特权安装调用一度以 `PRIVILEGED_ACTION_RESULT_UNKNOWN` 表示返回值尚不能确认，
更新器没有盲目重复切换，而是在安装辅助单元完成后依据释放目录、当前链接和业务健康事实对账。
23:30:20 UTC，rc.5 当前链接、发布身份、包摘要和业务进程全部得到确认，SQLite
`PRAGMA quick_check` 返回 `ok`，作业门重新开放并开始 30 分钟观察。此时后端部署为
`OBSERVING`，下载尝试 2 次、目标动作 3 次、回滚 0 次、数据库未恢复。

## 终态与生产开关复核

设备在 00:00:23 UTC 自行完成 30 分钟观察并持久化 `SUCCEEDED`，后端在下一秒把计划收敛为
`COMPLETED`、部署收敛为 `SUCCEEDED`、可靠任务收敛为 `DONE`。最终下载尝试 2 次、目标动作
3 次、回滚 0 次，安装版本为 rc.5，数据库未恢复且无错误码。设备与后端的业务准入均为开放；
`/opt/ecobin/business/current` 指向 rc.5，永久通信代理、设备更新器和业务服务均为 active，
SQLite `PRAGMA quick_check` 返回 `ok`，该更新的下载目录及全局暂存目录均为空。
后端最后一次独立设备投影查询同时确认：设备在线、rc.5 进程为 `RUNNING/READY`、三端结论为
`FULLY_COMPATIBLE`、业务准入为 `ACCEPTING`，活动业务程序计划和活动 MCU 部署均为 0。

所有非终态计划清零后，通过受控服务器脚本关闭生产业务程序远程下发总开关。脚本在原子修改后
重新执行生产预检、后端与 Web 健康检查、运行时密钥探针和回环检查；独立复核随后再次得到
`business-release-dispatch-verification=PASS dispatch=false`，平台就绪接口也返回
`remoteDispatchEnabled=false`。制品存储和签名能力保持可用，但在管理员再次明确开启前不会向设备
创建新的远程更新下行。

本轮已经证明业务程序发布不要求重写 TF 卡：同一张已完成厂家验收的卡能够在旧业务继续运行时
接收正式计划，经历设备离线等待、更新器重启、临时授权刷新、断点续传、验签、排空、数据库备份、
原子切换和 30 分钟观察，并由后端权威记录闭环。写镜像只用于把包外更新器的下载读停顿修复带入
以后新装或重装的设备，不是每次业务程序发布的前置步骤。镜像构建与离线审计也不能反向替代上述
已经完成的真机在机更新证据。

## v34 递增镜像

本轮下载读停顿修复已从固定提交 `adb66043a1da6c4e9ea792f4405266a49b97f319` 固化为
`0.1.0-single-card.20260908.34`。无秘密候选和受控单卡 HIL 镜像的 SHA-256 分别为
`d23ae002599ca2b6eff055ec939d96b8ef38aa1e82febf7836eb028a66405347` 与
`06286842051a037bd8e86c4c4c5eaecc9c8be05e2e28c0070ac7d0905501b9d5`。两张镜像均通过布局、
文件系统、软件清单、正式业务公钥、永久程序源码和最小权限审计；只读 ARM64 用户空间验证还直接
确认镜像内更新器的 HTTP 无数据容忍值为 120 秒，关键模块和更新清理补偿可以运行。

生产厂家验收认可列表已在保留 v31、v32、v33 的同时追加
`hardware-runtime-20260908-34`，部署和独立复核均通过，业务程序远程下发开关继续为关闭。
v34 HIL 镜像随后写入指定 TF 卡并完成全范围回读，回读摘要与镜像一致。设备冷启动后报告全部
组件为 v34，项目负责人完成热点厂家验收、租户和机构分配、正常投递与正常清运。之后受控完成
永久运行层切换和作业安全门激活；重启后永久通信代理、永久更新器与可替换业务程序正常运行，
旧服务保持关闭。设备侧更新准备检查通过，后端投影为在线、完全兼容并允许接受新业务。

本次 v34 验收使用模拟 MCU 来源，不能替代真实 MCU 外设质量证明。v34 自身也没有重复创建远程
业务包计划；相同更新器源码已经在前述 v32 热修复设备上通过 rc.4 到 rc.5 的权威真机更新、
失败安全和 30 分钟观察，所以这不是 v34 基础镜像资格的阻塞项。详细证据见
[v34 业务程序下载容错镜像证据](../../image-artifacts/evidence/hil-business-update-download-resilience-20260908-34/README.md)。
