# UART rc.26能力、串口屏批次、工厂投影与终态失败收口

日期：2026-09-14。状态：本地跨端候选；未部署、未烧录，尚未通过真实串口屏、RS485、
门机构、清运机构或断电硬件在环（HIL）验收。

当前候选为UART `2.0.0-rc.26`、OneNet `2.4.0`、EdgeStore schema 40、数据库V83，
UART注册表共73种消息。发布状态仍为`MCU_REVIEW_REQUIRED`和
`SIMPLIFIED_BUSINESS_INTEGRATION_NOT_RELEASED`，本记录不能替代现场发布门槛。

## 1. MCU身份和能力是实际事实

`DEVICE_IDENTITY_REPLY`现在必须返回MCU当前启动编号、当前启动下已经消费的最高命令序号和
真实能力位。香橙派及业务发布校验要求能力位覆盖`0x8100`，不再根据固件版本、协议模式或
配置猜测MCU能力。能力缺失、未知或不满足要求时拒绝进入正常业务；身份查询本身不驱动机构、
不称重，也不创建业务。

工厂到生产入口仍只移交准确匹配的启动编号、结果序号和工作编号高水位。它不移交用户、资金、
业务所有权，也不允许生产入口重放工厂动作。

## 2. 串口屏的唯一软件可信边界

项目负责人确认：系统信任“写出指令即串口屏显示”，否则现有硬件无法对任何串口屏页面、数值
或二维码提供更强保证。这里的“写出”有且只有一个可检查的定义：**一条完整、未截断的高层
显示指令，或属于同一页面更新的完整指令组，已经通过一次队列头发布原子写入UART3软件发送
队列，即视为串口屏已经显示。**

系统不等待USART发送完成位，不要求串口屏返回应用确认，也不读取屏幕当前页面或控件内容。
真实屏幕观察只用于发现接线、组件名称、文本容量或HMI工程视觉兼容问题，不改变协议成功定义。

当前实现同时满足以下约束：

- `page0`、`page4`、`page8`切页，以及`page6/page7`的切页和一次性控件初始化，分别作为完整
  批次全有或全无地进入FIFO（先入先出）发送队列；
- 实时倒计时和重量也按完整批次入队；只有批次成功后才推进显示时间和已显示样本序号；
- 队列空间不足时发布零字节，不推进页面阶段、控件状态或样本序号，下一主循环继续重试；
- 后来的页面或数值指令只能追加，不能清空、覆盖或撤销已经入队的二维码或其他显示指令；
- 二维码`DEVICE_ENTRY_URL_APPLY_RESULT=APPLIED`仍只表示完整
  `page0.qr0.txt="..."`指令已经原子入队，不表示取得了屏幕回执。

这次没有增加HMI业务状态、逐控件确认或读回协议；串口屏接收方向仍只承担现有按钮事件。

## 3. uart-v2原生工厂验收和热点公开事实

P7工厂验收现在强制使用`uart-v2 / 115200 / 单投口`原生入口。验收以当前MCU身份、配置应用、
设备事实、一次START、完整结果可靠保存和`RESULT_SAVED`确认作为证据，不再把历史固定帧
`F0/F1`、`DD/EF`流程冒充当前量产验收。

热点页面及最终硬件报告现在展示而不只展示“通过/失败”：

- 称重空载、加载、卸载样本及操作者填写的参考克数；
- 超声波距离、阈值、遮挡判断和采集状态，或历史数字红外事实；
- MCU固件身份、启动编号、最高命令序号和能力位；
- 投递关门控制已经下发，同时明确“没有独立门位反馈”，不把控制信号伪装成机械到位；
- 清运锁已断电、锁健康未知、清运员人工确认门扇关闭等真实依据。

原生工厂动作的恢复仍只查询原命令和原结果；不能因为进程或设备重启构造替代START。固定帧
工厂模拟器仅保留为历史工具，不能作为rc.26量产P7通过来源。

## 4. 控制通信失败的跨端终结

香橙派确认原生投递START控制通信失败并上报
`FAILED / MCU_COMMUNICATION_UNAVAILABLE`后，后端在同一事务中把原投递会话终结为
`DEVICE_ABORTED`，保存准确错误原因，并且只释放仍属于该投递会话的占用。如果占用已经属于
后续业务，事务必须失败回滚，不能破坏新业务。

该投递终结不创建订单，不增加余额，不建立或触发提现。迟到的完整结果只可作为问题证据关联，
不能自动恢复结算，也不能覆盖当前袋、皮重或后续业务状态。仅通信超时仍不等于MCU重启，
也不自动套用重启归档结论。

清运不能照搬投递的直接释放规则：清运发生同类控制失败时结束为`ABORTED`，但已经进入换袋
风险范围的袋状态和人工恢复联锁继续保留，按既定“两袋人工确认与独立空袋皮重”规则恢复。

## 5. 本地验证事实

- 完整机器契约：598项通过、1750个子测试通过；113份生成文件一致；25项完整契约校验通过；
- 普通HMI批次、二维码和原生板级入口专项：25项通过；
- 工厂验收投影及首启校验专项：106项通过、2项按环境跳过；
- 前端失败文案专项34项通过，Web架构19项通过，小程序/Web TypeScript检查及OpenAPI生成检查通过；
- Java九模块Maven完整构建成功；Surefire报告共1323项，其中1126项实际通过、197项按外部
  MySQL等环境条件跳过，0失败、0错误；
- 硬件全量首次运行4258项通过、123项按Windows或真实设备环境跳过、5个子测试通过，另有1项
  已知Windows SQLite强杀恢复测试出现`SQLITE_IOERR_TRUNCATE (1546)`；该失败项随后单独复跑
  1项通过。不能把两次运行描述为一次全绿；
- ARMCC5完整原生固件构建成功：RO 60,432字节，ROM/bin 60,588/65,536字节，
  RAM 12,960/20,480字节，分别剩余4,948和7,520字节。

上述首轮验证当时没有连接现场设备、修改OneNet线上配置、运行真实MySQL迁移、部署后端或
香橙派、制作镜像、写TF卡、烧录MCU/HMI，也没有驱动真实机构。此后新增的本地ARM64候选与
安装检查见第7节；它仍不替代真实串口屏、RS485称重、投递/清运机构、掉电组合HIL和实际部署
验收。

## 6. 侧向审查后的终态补口

在首个rc.26检查点之后，侧向只读审查发现并修复四个会造成“事实已经明确，但软件仍卡住”
的问题；没有扩展机械恢复模型：

- MCU对投递、清运或空袋基准START明确返回`REJECTED`时，香橙派按该原回复终结对应业务并释放
  准确的本地占用；它不是通信超时，不建立通信故障锁。清运START尚未执行时也不虚构换袋风险；
- 普通投递/清运的过程事件是可选诊断。完整结果已释放后，旧的可选过程事件不得永久占用MCU
  单槽邮箱、阻止后续独立空袋基准；MCU可丢弃该诊断，但绝不丢弃尚未确认保存的基准结果；
- 主循环连续遇到不同阻断异常时只保留第一个`native_blocking_fault`及其时间，后续异常写日志，
  不覆盖人工正在处理的原故障身份；
- 清运因断网超过10分钟已经释放使用占用、但原结果仍待补报时，小程序继续查询同一操作；普通
  `RECOVERY_REQUIRED`仍停在人工恢复页面，不无限轮询。

出厂手册、首次启动说明及写卡清单已经同步为rc.26原生身份/能力、配置、设备事实、一次START、
完整结果保存和`RESULT_SAVED`链。F3/F1、AA/DD、EE/EF和`factory_sim`只保留为历史兼容说明。
称重网页填写值是本轮验收比较标准，报告同时保留实际样本；流程通过不等于称重硬件准确。

本次全量Python 3.11硬件套件实际执行结果为4261项通过、123项按环境跳过、5个子测试通过、
2项失败。两项失败均为测试硬编码上一固件身份，而实际编译的是当前
`1.0.2-hil.5 / 10005`；测试随后改为读取本次构建使用的生成身份头，两个失败项单独复跑均通过。
不能把这两次执行合并描述为一次全绿。相关业务/MCU扩大子集仍需在最终提交前再次运行。

另已生成一个基于补口前检查点`f5427911`的ARM64无秘密候选，用于证明缓存构建链可工作；它不含
上述补口，因此不是最终候选，也不得部署或写卡。最终候选必须从补口后的新提交重新构建。

## 7. 补口后v39本地候选与hil.6固件

补口代码已保存为提交`5b2f7fde1f675d9408c9b68c22d0de1ae0c103fb`，并由该提交重新生成
完整历史源码包`hardware/image-artifacts/local/source-bundles/repository-v39-20260914.bundle`。
包内`HEAD`精确指向该提交，SHA-256为
`808DC58DC8333418457B05ED09EC5F7B04E6196831BCFA63F61B7744C94A5D93`。

配套MCU人工烧录产物位于
`hardware/image-artifacts/local/mcu-uart2-rc26-20260914-hil6`，使用身份
`1.0.2-hil.6 / 10006 / 55e09a9352c3b025`，发布编号为
`d16d1621-5fbd-4278-ae78-748534d95f20`。ARMCC5完整构建结果为RO 60,520字节、
ROM/bin 60,676/65,536字节、RAM 12,960/20,480字节；BIN、HEX、AXF的SHA-256分别为：

- `8D30CAF7FA6C530F13BEE4EB59B259C2568DCE48F56E8A14E6D7D9777A7BFDB3`；
- `3EA8E4956922EE1228A5834F4D08095083F6C3E76CCEEADFD847647875060405`；
- `F8274F3F9770BBCA93AAB815FA7E8387E405651EBFBFDB8CAAA0B6E94E7E0157`。

`ECOBIN_UART`原生固件仍不支持当前固定帧远程升级包格式，因此没有生成`.efw`；不能把这个缺失
解释成打包失败，也不能用旧升级器远程下发hil.6。

ARM64候选输出证据目录为
`/var/lib/ecobin-image-factory/hil-v39-20260914-rc26/candidate-single-card-hil-20260914-39`，
精确镜像文件名为`ecobin-orangepi-zero3-0.1.0-single-card.20260914.39.img`，文件长度
2,571,108,352字节，版本`0.1.0-single-card.20260914.39`，镜像SHA-256为
`bb44f07e0439ff4f8f3f9dca29941be2d61543e0edbe4290d03dd4a77f9d44d8`。应用载荷锁摘要为
`9579bccd4cce9bffd2223fe96d407f8ef5d13d6f2eb70354717efd00ef7c0bb1`，运行包摘要为
`a9b7625c5efe35c8fb71bd1169c28a0f12442ece168fa63ca3b0c66021558d67`；对应文件分别是
`software-payload-20260914-39/software-payload.lock.json`和
`runtime/hardware-runtime-20260914-39/ecobin-hardware-hardware-runtime-20260914-39.tar.gz`。
同一受控目录保留`build-v39-candidate.log`、镜像`.sha256`和manifest。构建复用了锁定的
ARM64基础镜像、APT包和Python wheel缓存；三轮APT安装下载量均为0字节。

候选在构建结束后又单独运行一次`verify-image.sh --candidate`，镜像摘要、只读布局、ext4配置、
无设备秘密、账号锁定、软件安装审计和manifest复核均通过。这只是验证命令与构建步骤分开执行，
不是第二次独立构建，也不证明生产发布所需的双构建字节一致、签名或封存。随后
`run-v39-arm64-smoke.sh`在ARM64容器中以断网、镜像只读、仅`/tmp`合成状态可写的方式验证：

- Python 3.11.2，`/dev/ttyS5 / 115200 / uart-v2 / 单投口`；
- UART注册表`2.0.0-rc.26`共73种消息，Edge/MCU必需能力掩码均为`0x8100`；
- EdgeStore schema 40、OneNet映射2.4.0、二维码发送入口和首次阻断故障锁存；
- 由合成输入生成的8份完整验收报告fixture，以及28、400、500、1000克四种参考重量、热点
  网页称重样本展示和共同报告校验门；它不是现场放置砝码取得的数据，也不涉及串口屏显示。

固件相关定向回归83项通过；ARM64 smoke全部通过。以上只证明本地软件产物可以安装、导入和
处理合成数据，准确说是“ARM64容器内镜像安装/导入smoke完成”，真实香橙派启动和安装尚未完成；
它也不证明真实热点能启动、串口屏实际显示、RS485称重准确、门机构动作或掉电恢复。v39仍是
`UNSIGNED_NO_SECRET_CANDIDATE`，没有写TF卡、烧录、部署或连接现场设备。取得新的明确授权后，
它只能写入明确识别的实验卡、配合人工烧录hil.6做受控HIL，不能作为生产部署镜像；真实HMI、
RS485、投递/清运机构、掉电组合HIL、第二次独立构建一致性、签名和封存仍是生产发布门槛。

## 8. v40镜像可复现性补口

v39第二次隔离构建没有改变提交、版本、锁定输入或依赖缓存。两轮运行时签名压缩包一致，但软件
载荷锁和raw镜像摘要不同。只读载荷比较显示，两份锁均为5,442条，顶层字段、路径集合、类型、
权限和组件版本全部相同；唯一34条原始差异是硬件运行时虚拟环境中17个包各自的
`uv_cache.json`和对应`RECORD`。`uv 0.12.5`在安装时写入当前秒/纳秒，`RECORD`只因引用其
摘要而变化。镜像逐文件、权限、属主、链接、扩展属性和4 KiB块比较另发现唯一独立差异为
`/var/cache/ldconfig/aux-cache`；它是可再生辅助索引，真正的`/etc/ld.so.cache`两轮一致。

修复分成两个提交：

- `156a45e3`在所有不可变虚拟环境加固入口统一删除`uv_cache.json`，并用CSV语义从相邻
  `RECORD`删除唯一精确对应行；对缺失、重复、畸形、链接、硬链接、尺寸或属主异常执行失败
  关闭。两个不同安装时钟的单元夹具归一化后字节一致。
- `31a6b6fc`在最终候选清理时删除`/var/cache/ldconfig/aux-cache`，并让镜像验证器明确拒绝
  该文件；`/etc/ld.so.cache`不删除。

用于构建的完整提交为`31a6b6fca319c252b30574a2cad5bdfdfcf50a76`；源码包
`hardware/image-artifacts/local/source-bundles/repository-v40-20260914.bundle`的SHA-256为
`8EF19065B4C752726E56F21BDCF314DA234A9BE16CDBA4DFC89E2AEEA1D08283`。以相同
`0.1.0-single-card.20260914.40`版本、发布编号和全部锁定输入，在以下两个隔离目录完整构建：

- `/var/lib/ecobin-image-factory/hil-v40-20260914-repro1`；
- `/var/lib/ecobin-image-factory/hil-v40-20260914-repro2`。

两轮所有APT依赖下载量均为0字节，并得到以下逐字节一致结果：

- 运行包SHA-256：`b1528daf53f8cd588aac9a105f0a699b638c88cb4519d7d45e7948a5d590fde4`；
- 软件载荷锁SHA-256：`5456fb02a6e01ae3644452cd4b5ebb533102bd226d30d983ac95f411b137d614`；
- manifest逐字节一致；
- raw镜像文件`ecobin-orangepi-zero3-0.1.0-single-card.20260914.40.img`长度
  2,571,108,352字节，SHA-256均为
  `94e1bf8314310379c579ea469ae742949b7c121fefd8c6455ab0ca9869f2b84d`。

两份载荷均没有`uv_cache.json`，两份镜像都通过新增的辅助缓存拒绝检查。构建期间`/dev/sdc`
仅作为WSL交换分区临时停用，结束后已恢复，未写TF卡。v40只更改构建确定性；MCU继续配套既有
`1.0.2-hil.6 / 10006`，未重新烧录。该镜像仍是`UNSIGNED_NO_SECRET_CANDIDATE`，不能因为
双构建一致就视为已完成真实香橙派启动/安装、热点、HMI、RS485、机构、掉电HIL、秘密注入、
正式签名、封存或部署。

第一份v40随后复用v39的只读ARM64 smoke约束再次验证：镜像只读挂载、容器断网、仅`/tmp`
合成状态可写，8份完整报告fixture、28/400/500/1000克参考重量、隔离动作账本、通信模块导入、
UART rc.26的73种消息和`0x8100`能力、EdgeStore schema 40、OneNet 2.4.0、二维码入口及首次
阻断故障锁存全部通过。该smoke明确输出`actualHardwareAccess=false`，仍不代表真实香橙派、
串口屏、RS485秤或门机构验证。
